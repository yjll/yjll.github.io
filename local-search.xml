<?xml version="1.0" encoding="utf-8"?>
<search>
  
  
  
  <entry>
    <title>Spring编程式事务</title>
    <link href="/post/65703737.html"/>
    <url>/post/65703737.html</url>
    
    <content type="html"><![CDATA[<table><thead><tr><th>类型</th><th>说明</th><th>举例</th></tr></thead><tbody><tr><td>PROPAGATION_REQUIRED</td><td>支持当前事务 如果不存，就建一个（默认的）</td><td>A,B，如果A有事务，则B使用A事务。如果A没有事务，则B开启一个新的事务。（A,B在同一个事务中）</td></tr><tr><td>PROPAGATION_SUPPORTS</td><td>支持当前事务 如果不存在，就不使用事务</td><td>A,B，如果A有事务，则B就使用A的事务。如果A没有事务，则B就不使用事务</td></tr><tr><td>PROPAGATION_MANDATORY</td><td>支持当前事务 如果不存在，抛出异常</td><td>A,B，如果A有事务，则B就使用A的事务。如果A没有事务，则B抛出异常</td></tr><tr><td>PROPAGATION_REQUIRES_NEW</td><td>如果有事务存在 挂起当前事务，创建一个新的事务</td><td>A,B，如果A有事务，B将A的事务挂起，并重新创建一个事务（A，B不在一个事务中，两个事务互不影响）</td></tr><tr><td>PROPAGATION_NOT_SUPPORTED</td><td>以非事务方式运行 如果有事务存在，挂起当前事务</td><td>A,B，非事务方式运行，A有事务就挂起事务</td></tr><tr><td>PROPAGATION_NEVER</td><td>以非事务方式运行 如果有事务存在，抛出异常</td><td>总是以非事务运行</td></tr><tr><td>PROPAGATION_NESTED</td><td>如果当前事务存在，则嵌套事务执行</td><td>基于SavePonit技术。（保存点） A,B，A有事务，A执行之后，将A事务执行之后的内容保存到SavePonit。B事务有异常的情况，用户需要自己设置事务是提交还是回滚到保存点</td></tr></tbody></table><div class="hljs"><pre><code class="hljs java"><span class="hljs-meta">@Autowired</span><span class="hljs-keyword">private</span> DataSourceTransactionManager dstManager;<span class="hljs-meta">@Autowired</span><span class="hljs-keyword">private</span> UserDao userDao;<span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">updateTest</span><span class="hljs-params">()</span></span>&#123;  DefaultTransactionDefinition def = <span class="hljs-keyword">new</span> DefaultTransactionDefinition();  def.setPropagationBehavior(TransactionDefinition.PROPAGATION_REQUIRES_NEW);  TransactionStatus transaction= dstManager.getTransaction(def);  <span class="hljs-keyword">try</span>&#123;    userDao.updateTest();  &#125;<span class="hljs-keyword">catch</span>(Exception e)&#123;    log.error(e.toString(),e);    dstManager.rollback(transaction);  &#125;  dstManager.commit(transaction);&#125;</code></pre></div><p><code>DataSourceTransactionManager</code>是<code>PlatformTransactionManager</code>的一个实现。<code>PlatformTransactionManager</code>是Spring事务处理抽象层中的管理器。</p><p>统一一致的事务抽象是Spring框架的一大优势，无论是全局事务还是本地事务，JTA、JDBC、Hibernate还是JPA，Spring都使用统一的编程模型，使得应用程序可以很容易地在全局事务与本地事务，或者不同的事务框架之间进行切换。下图是Spring事务抽象的核心类图：</p><p><img src="https://img.yjll.art/img/PlatformTransactionManager.png" srcset="/img/loading.gif" alt="PlatformTransactionManager"></p><p>PlatformTransactionManager：事务管理器<br>getTransaction方法：事务获取操作，根据事务属性定义，获取当前事务或者创建新事物；<br>commit方法：事务提交操作，注意这里所说的提交并非直接提交事务，而是根据当前事务状态执行提交或者回滚操作；<br>rollback方法：事务回滚操作，同样，也并非一定直接回滚事务，也有可能只是标记事务为只读，等待其他调用方执行回滚。</p><p>TransactionDefinition：事务属性定义<br>getPropagationBehavior方法：返回事务的传播属性，默认是PROPAGATION_REQUIRED；<br>getIsolationLevel方法：返回事务隔离级别，事务隔离级别只有在创建新事务时才有效，也就是说只对应传播属性PROPAGATION_REQUIRED和PROPAGATION_REQUIRES_NEW；<br>getTimeout方法：返回事务超时时间，以秒为单位，同样只有在创建新事务时才有效；<br>isReadOnly方法：是否优化为只读事务，支持这项属性的事务管理器会将事务标记为只读，只读事务不允许有写操作，不支持只读属性的事务管理器需要忽略这项设置，这一点跟其他事务属性定义不同，针对其他不支持的属性设置，事务管理器应该抛出异常。<br>getName方法：返回事务名称，声明式事务中默认值为“类的完全限定名.方法名”。</p><p>TransactionStatus：当前事务状态<br>isNewTransaction方法：当前方法是否创建了新事务（区别于使用现有事务以及没有事务）；<br>hasSavepoint方法：在嵌套事务场景中，判断当前事务是否包含保存点；<br>setRollbackOnly和isRollbackOnly方法：只读属性设置（主要用于标记事务，等待回滚）和查询；<br>flush方法：刷新底层会话中的修改到数据库，一般用于刷新如Hibernate/JPA的会话，是否生效由具体事务资源实现决定；<br>isCompleted方法：判断当前事务是否已完成（已提交或者已回滚）。</p><div class="hljs"><pre><code class="hljs java"><span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">final</span> TransactionStatus <span class="hljs-title">getTransaction</span><span class="hljs-params">(@Nullable TransactionDefinition definition)</span> <span class="hljs-keyword">throws</span> TransactionException </span>&#123;    <span class="hljs-comment">// 创建DataSourceTransactionObject</span>Object transaction = doGetTransaction();<span class="hljs-comment">// Cache debug flag to avoid repeated checks.</span><span class="hljs-keyword">boolean</span> debugEnabled = logger.isDebugEnabled();<span class="hljs-keyword">if</span> (definition == <span class="hljs-keyword">null</span>) &#123;<span class="hljs-comment">// Use defaults if no transaction definition given.</span>definition = <span class="hljs-keyword">new</span> DefaultTransactionDefinition();&#125;    <span class="hljs-comment">// 已有事务逻辑，通过handleExistingTransaction方法处理</span><span class="hljs-keyword">if</span> (isExistingTransaction(transaction)) &#123;<span class="hljs-comment">// Existing transaction found -&gt; check propagation behavior to find out how to behave.</span><span class="hljs-keyword">return</span> handleExistingTransaction(definition, transaction, debugEnabled);&#125;<span class="hljs-comment">// Check definition settings for new transaction.</span><span class="hljs-keyword">if</span> (definition.getTimeout() &lt; TransactionDefinition.TIMEOUT_DEFAULT) &#123;<span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> InvalidTimeoutException(<span class="hljs-string">"Invalid transaction timeout"</span>, definition.getTimeout());&#125;    <span class="hljs-comment">// 以下是当前无事务的情况</span>    <span class="hljs-comment">// - PROPAGATION_MANDATORY：当前事务不存在，抛出异常</span><span class="hljs-comment">// No existing transaction found -&gt; check propagation behavior to find out how to proceed.</span><span class="hljs-keyword">if</span> (definition.getPropagationBehavior() == TransactionDefinition.PROPAGATION_MANDATORY) &#123;<span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> IllegalTransactionStateException(<span class="hljs-string">"No existing transaction found for transaction marked with propagation 'mandatory'"</span>);&#125;    <span class="hljs-comment">// 无事务便创建事务</span><span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span> (definition.getPropagationBehavior() == TransactionDefinition.PROPAGATION_REQUIRED ||definition.getPropagationBehavior() == TransactionDefinition.PROPAGATION_REQUIRES_NEW ||definition.getPropagationBehavior() == TransactionDefinition.PROPAGATION_NESTED) &#123;SuspendedResourcesHolder suspendedResources = suspend(<span class="hljs-keyword">null</span>);<span class="hljs-keyword">if</span> (debugEnabled) &#123;logger.debug(<span class="hljs-string">"Creating new transaction with name ["</span> + definition.getName() + <span class="hljs-string">"]: "</span> + definition);&#125;<span class="hljs-keyword">try</span> &#123;<span class="hljs-keyword">boolean</span> newSynchronization = (getTransactionSynchronization() != SYNCHRONIZATION_NEVER);DefaultTransactionStatus status = newTransactionStatus(definition, transaction, <span class="hljs-keyword">true</span>, newSynchronization, debugEnabled, suspendedResources);doBegin(transaction, definition);prepareSynchronization(status, definition);<span class="hljs-keyword">return</span> status;&#125;<span class="hljs-keyword">catch</span> (RuntimeException | Error ex) &#123;resume(<span class="hljs-keyword">null</span>, suspendedResources);<span class="hljs-keyword">throw</span> ex;&#125;&#125;    <span class="hljs-comment">// 无事务不需创建</span><span class="hljs-keyword">else</span> &#123;<span class="hljs-comment">// Create "empty" transaction: no actual transaction, but potentially synchronization.</span><span class="hljs-keyword">if</span> (definition.getIsolationLevel() != TransactionDefinition.ISOLATION_DEFAULT &amp;&amp; logger.isWarnEnabled()) &#123;logger.warn(<span class="hljs-string">"Custom isolation level specified but no actual transaction initiated; "</span> +<span class="hljs-string">"isolation level will effectively be ignored: "</span> + definition);&#125;<span class="hljs-keyword">boolean</span> newSynchronization = (getTransactionSynchronization() == SYNCHRONIZATION_ALWAYS);<span class="hljs-keyword">return</span> prepareTransactionStatus(definition, <span class="hljs-keyword">null</span>, <span class="hljs-keyword">true</span>, newSynchronization, debugEnabled, <span class="hljs-keyword">null</span>);&#125;&#125;</code></pre></div><div class="hljs"><pre><code class="hljs java">org.springframework.jdbc.datasource.DataSourceTransactionManager#doGetTransaction<span class="hljs-function"><span class="hljs-keyword">protected</span> Object <span class="hljs-title">doGetTransaction</span><span class="hljs-params">()</span> </span>&#123;DataSourceTransactionObject txObject = <span class="hljs-keyword">new</span> DataSourceTransactionObject();txObject.setSavepointAllowed(isNestedTransactionAllowed());ConnectionHolder conHolder =(ConnectionHolder) TransactionSynchronizationManager.getResource(obtainDataSource());txObject.setConnectionHolder(conHolder, <span class="hljs-keyword">false</span>);<span class="hljs-keyword">return</span> txObject;&#125;org.springframework.jdbc.datasource.DataSourceTransactionManager#isExistingTransaction<span class="hljs-function"><span class="hljs-keyword">protected</span> <span class="hljs-keyword">boolean</span> <span class="hljs-title">isExistingTransaction</span><span class="hljs-params">(Object transaction)</span> </span>&#123;DataSourceTransactionObject txObject = (DataSourceTransactionObject) transaction;<span class="hljs-keyword">return</span> (txObject.hasConnectionHolder() &amp;&amp; txObject.getConnectionHolder().isTransactionActive());&#125;</code></pre></div><p><code>TransactionSynchronizationManager</code> 是事务同步管理器，TransactionSynchronizationManager通过ThreadLocal对象在当前线程记录了resources和synchronizations属性。resources是一个HashMap，用于记录当前参与事务的事务资源，方便进行事务同步，在DataSourceTransactionManager的例子中就是以dataSource作为key，保存了数据库连接，这样在同一个线程中，不同的方法调用就可以通过dataSource获取相同的数据库连接，从而保证所有操作在一个事务中进行。</p><p>org.springframework.transaction.support.AbstractPlatformTransactionManager#handleExistingTransaction</p><div class="hljs"><pre><code class="hljs java"><span class="hljs-function"><span class="hljs-keyword">private</span> TransactionStatus <span class="hljs-title">handleExistingTransaction</span><span class="hljs-params">(</span></span><span class="hljs-function"><span class="hljs-params">TransactionDefinition definition, Object transaction, <span class="hljs-keyword">boolean</span> debugEnabled)</span></span><span class="hljs-function"><span class="hljs-keyword">throws</span> TransactionException </span>&#123;   <span class="hljs-comment">// PROPAGATION_NEVER 直接抛出异常</span><span class="hljs-keyword">if</span> (definition.getPropagationBehavior() == TransactionDefinition.PROPAGATION_NEVER) &#123;<span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> IllegalTransactionStateException(<span class="hljs-string">"Existing transaction found for transaction marked with propagation 'never'"</span>);&#125;  <span class="hljs-comment">// PROPAGATION_NOT_SUPPORTED suspend挂起当前事务，当前方法不使用事务</span><span class="hljs-keyword">if</span> (definition.getPropagationBehavior() == TransactionDefinition.PROPAGATION_NOT_SUPPORTED) &#123;<span class="hljs-keyword">if</span> (debugEnabled) &#123;logger.debug(<span class="hljs-string">"Suspending current transaction"</span>);&#125;Object suspendedResources = suspend(transaction);<span class="hljs-keyword">boolean</span> newSynchronization = (getTransactionSynchronization() == SYNCHRONIZATION_ALWAYS);<span class="hljs-keyword">return</span> prepareTransactionStatus(definition, <span class="hljs-keyword">null</span>, <span class="hljs-keyword">false</span>, newSynchronization, debugEnabled, suspendedResources);&#125;  <span class="hljs-comment">// PROPAGATION_REQUIRES_NEW suspend挂起当前事务，doBegin创建一个新事务</span><span class="hljs-keyword">if</span> (definition.getPropagationBehavior() == TransactionDefinition.PROPAGATION_REQUIRES_NEW) &#123;<span class="hljs-keyword">if</span> (debugEnabled) &#123;logger.debug(<span class="hljs-string">"Suspending current transaction, creating new transaction with name ["</span> +definition.getName() + <span class="hljs-string">"]"</span>);&#125;SuspendedResourcesHolder suspendedResources = suspend(transaction);<span class="hljs-keyword">try</span> &#123;<span class="hljs-keyword">boolean</span> newSynchronization = (getTransactionSynchronization() != SYNCHRONIZATION_NEVER);DefaultTransactionStatus status = newTransactionStatus(definition, transaction, <span class="hljs-keyword">true</span>, newSynchronization, debugEnabled, suspendedResources);doBegin(transaction, definition);prepareSynchronization(status, definition);<span class="hljs-keyword">return</span> status;&#125;<span class="hljs-keyword">catch</span> (RuntimeException | Error beginEx) &#123;resumeAfterBeginException(transaction, suspendedResources, beginEx);<span class="hljs-keyword">throw</span> beginEx;&#125;&#125;  <span class="hljs-comment">// PROPAGATION_NESTED 嵌套事务</span><span class="hljs-keyword">if</span> (definition.getPropagationBehavior() == TransactionDefinition.PROPAGATION_NESTED) &#123;<span class="hljs-keyword">if</span> (!isNestedTransactionAllowed()) &#123;<span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> NestedTransactionNotSupportedException(<span class="hljs-string">"Transaction manager does not allow nested transactions by default - "</span> +<span class="hljs-string">"specify 'nestedTransactionAllowed' property with value 'true'"</span>);&#125;<span class="hljs-keyword">if</span> (debugEnabled) &#123;logger.debug(<span class="hljs-string">"Creating nested transaction with name ["</span> + definition.getName() + <span class="hljs-string">"]"</span>);&#125;<span class="hljs-keyword">if</span> (useSavepointForNestedTransaction()) &#123;<span class="hljs-comment">// Create savepoint within existing Spring-managed transaction,</span><span class="hljs-comment">// through the SavepointManager API implemented by TransactionStatus.</span><span class="hljs-comment">// Usually uses JDBC 3.0 savepoints. Never activates Spring synchronization.</span>DefaultTransactionStatus status =prepareTransactionStatus(definition, transaction, <span class="hljs-keyword">false</span>, <span class="hljs-keyword">false</span>, debugEnabled, <span class="hljs-keyword">null</span>);status.createAndHoldSavepoint();<span class="hljs-keyword">return</span> status;&#125;     <span class="hljs-comment">// PROPAGATION_REQUIRED、PROPAGATION_SUPPORTS、PROPAGATION_MANDATORY 使用当前事务</span><span class="hljs-keyword">else</span> &#123;<span class="hljs-comment">// Nested transaction through nested begin and commit/rollback calls.</span><span class="hljs-comment">// Usually only for JTA: Spring synchronization might get activated here</span><span class="hljs-comment">// in case of a pre-existing JTA transaction.</span><span class="hljs-keyword">boolean</span> newSynchronization = (getTransactionSynchronization() != SYNCHRONIZATION_NEVER);DefaultTransactionStatus status = newTransactionStatus(definition, transaction, <span class="hljs-keyword">true</span>, newSynchronization, debugEnabled, <span class="hljs-keyword">null</span>);doBegin(transaction, definition);prepareSynchronization(status, definition);<span class="hljs-keyword">return</span> status;&#125;&#125;<span class="hljs-comment">// Assumably PROPAGATION_SUPPORTS or PROPAGATION_REQUIRED.</span><span class="hljs-keyword">if</span> (debugEnabled) &#123;logger.debug(<span class="hljs-string">"Participating in existing transaction"</span>);&#125;<span class="hljs-keyword">if</span> (isValidateExistingTransaction()) &#123;<span class="hljs-keyword">if</span> (definition.getIsolationLevel() != TransactionDefinition.ISOLATION_DEFAULT) &#123;Integer currentIsolationLevel = TransactionSynchronizationManager.getCurrentTransactionIsolationLevel();<span class="hljs-keyword">if</span> (currentIsolationLevel == <span class="hljs-keyword">null</span> || currentIsolationLevel != definition.getIsolationLevel()) &#123;Constants isoConstants = DefaultTransactionDefinition.constants;<span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> IllegalTransactionStateException(<span class="hljs-string">"Participating transaction with definition ["</span> +definition + <span class="hljs-string">"] specifies isolation level which is incompatible with existing transaction: "</span> +(currentIsolationLevel != <span class="hljs-keyword">null</span> ?isoConstants.toCode(currentIsolationLevel, DefaultTransactionDefinition.PREFIX_ISOLATION) :<span class="hljs-string">"(unknown)"</span>));&#125;&#125;<span class="hljs-keyword">if</span> (!definition.isReadOnly()) &#123;<span class="hljs-keyword">if</span> (TransactionSynchronizationManager.isCurrentTransactionReadOnly()) &#123;<span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> IllegalTransactionStateException(<span class="hljs-string">"Participating transaction with definition ["</span> +definition + <span class="hljs-string">"] is not marked as read-only but existing transaction is"</span>);&#125;&#125;&#125;<span class="hljs-keyword">boolean</span> newSynchronization = (getTransactionSynchronization() != SYNCHRONIZATION_NEVER);<span class="hljs-keyword">return</span> prepareTransactionStatus(definition, transaction, <span class="hljs-keyword">false</span>, newSynchronization, debugEnabled, <span class="hljs-keyword">null</span>);&#125;</code></pre></div><p><a href="https://zhuanlan.zhihu.com/p/54067384" target="_blank" rel="noopener">【技术干货】Spring事务原理一探</a></p><p><a href="https://blog.csdn.net/oDeviloo/article/details/101757711" target="_blank" rel="noopener">spring事务传播性源码解析</a></p><p><a href="https://my.oschina.net/marvelcode/blog/3195379" target="_blank" rel="noopener">Spring事务源码解析</a></p>]]></content>
    
    
    <categories>
      
      <category>Java</category>
      
    </categories>
    
    
    <tags>
      
      <tag>Spring</tag>
      
      <tag>Transaction</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>Python通过跳板机连接远程MySQL</title>
    <link href="/post/adfcf5ef.html"/>
    <url>/post/adfcf5ef.html</url>
    
    <content type="html"><![CDATA[<p>一般公司的数据库在一个远程服务器里，而这个服务器需要链接跳板机才可以访问，常用的数据库客户端工具都支持配置SSH隧道，可以通过SSH隧道连接远程服务。但python脚本如何访问数据库呢，我们可以借助<code>sshtunnel</code>这个库来实现。</p><a id="more"></a><div class="hljs"><pre><code class="hljs python"><span class="hljs-keyword">from</span> sshtunnel <span class="hljs-keyword">import</span> SSHTunnelForwarder<span class="hljs-keyword">import</span> records<span class="hljs-keyword">with</span> SSHTunnelForwarder(        (<span class="hljs-string">'jump-server'</span>, <span class="hljs-number">22</span>),        ssh_username=<span class="hljs-string">"root"</span>,        ssh_pkey=<span class="hljs-string">r"C:\Users\LH\.ssh\id_rsa"</span>,        remote_bind_address=(<span class="hljs-string">'target-server'</span>, <span class="hljs-number">3306</span>),        local_bind_address=(<span class="hljs-string">'127.0.0.1'</span>, <span class="hljs-number">3306</span>)        ) <span class="hljs-keyword">as</span> server:        db = records.Database(<span class="hljs-string">f'mysql+pymysql://root:pwd@<span class="hljs-subst">&#123;server.local_bind_address[<span class="hljs-number">0</span>]&#125;</span>:<span class="hljs-subst">&#123;server.local_bind_address[<span class="hljs-number">1</span>]&#125;</span>/code'</span>)    result = db.query(<span class="hljs-string">'select name from base_areas limit 3'</span>)    print(result.as_dict())</code></pre></div>]]></content>
    
    
    <categories>
      
      <category>Python</category>
      
    </categories>
    
    
    <tags>
      
      <tag>Python</tag>
      
      <tag>sshtunnel</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>聊一聊Mybatis-Spring都帮我们做了什么</title>
    <link href="/post/112d5fa0.html"/>
    <url>/post/112d5fa0.html</url>
    
    <content type="html"><![CDATA[<p>MyBatis-Spring 会帮助我们将 MyBatis 代码无缝地整合到 Spring 中。它将允许 MyBatis 参与到 Spring 的事务管理之中，创建映射器 mapper 和 SqlSession 并注入到 bean 中，本文将跟随Mybatis-Spring 的源码，一步步探究是它是如何实现的。</p><a id="more"></a><p>我们先进行一个必要的bean配置。</p><div class="hljs"><pre><code class="hljs java"><span class="hljs-meta">@Configuration</span><span class="hljs-meta">@MapperScan</span>(basePackages = &#123; <span class="hljs-string">"com.xxx.goods.dao"</span> &#125;, sqlSessionFactoryRef = <span class="hljs-string">"goodsSqlSessionFactory"</span>)<span class="hljs-keyword">public</span> <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">GoodsDataSourceConfig</span> </span>&#123;    <span class="hljs-meta">@Resource</span>    <span class="hljs-keyword">private</span> Environment env;    <span class="hljs-meta">@Bean</span>(name = <span class="hljs-string">"goodsDataSource"</span>)    <span class="hljs-meta">@ConfigurationProperties</span>(prefix = <span class="hljs-string">"spring.datasource.goods"</span>)    <span class="hljs-function"><span class="hljs-keyword">public</span> DataSource <span class="hljs-title">initDataSource</span><span class="hljs-params">()</span> </span>&#123;        <span class="hljs-keyword">return</span> DataSourceBuilder.create().build();    &#125;        <span class="hljs-meta">@Bean</span>(name = <span class="hljs-string">"goodsTransactionManager"</span>)    <span class="hljs-meta">@Primary</span>    <span class="hljs-function"><span class="hljs-keyword">public</span> DataSourceTransactionManager <span class="hljs-title">transactionManager</span><span class="hljs-params">(DataSource dataSource)</span> </span>&#123;        <span class="hljs-keyword">return</span> <span class="hljs-keyword">new</span> DataSourceTransactionManager(dataSource);    &#125;    <span class="hljs-meta">@Bean</span>(name = <span class="hljs-string">"goodsSqlSessionFactory"</span>)    <span class="hljs-meta">@Primary</span>    <span class="hljs-function"><span class="hljs-keyword">public</span> SqlSessionFactory <span class="hljs-title">initSqlSessionFactory</span><span class="hljs-params">(DataSource dataSource)</span> <span class="hljs-keyword">throws</span> Exception </span>&#123;        <span class="hljs-keyword">final</span> SqlSessionFactoryBean sessionFactory = <span class="hljs-keyword">new</span> SqlSessionFactoryBean();        sessionFactory.setDataSource(dataSource);        sessionFactory.setMapperLocations(<span class="hljs-keyword">new</span> PathMatchingResourcePatternResolver()                .getResources(env.getProperty(<span class="hljs-string">"mybatis.goods.mapper-locations"</span>)));        <span class="hljs-keyword">return</span> sessionFactory.getObject();    &#125;&#125;</code></pre></div><h3 id="Mybatis原生写法"><a href="#Mybatis原生写法" class="headerlink" title="Mybatis原生写法"></a>Mybatis原生写法</h3><p>我这里引用<a href="https://mybatis.org/mybatis-3/zh/getting-started.html" target="_blank" rel="noopener">Mybatis官方文档</a>中的说明</p><ul><li><code>SqlSessionFactory</code> SqlSessionFactory 一旦被创建就应该在应用的运行期间一直存在，没有任何理由丢弃它或重新创建另一个实例。 使用 SqlSessionFactory 的最佳实践是在应用运行期间不要重复创建多次，多次重建 SqlSessionFactory 被视为一种代码“坏习惯”。因此 SqlSessionFactory 的最佳作用域是应用作用域。 有很多方法可以做到，最简单的就是使用单例模式或者静态单例模式。</li></ul><ul><li><code>SqlSession</code> 每个线程都应该有它自己的 SqlSession 实例。SqlSession 的实例不是线程安全的，因此是不能被共享的，所以它的最佳的作用域是请求或方法作用域。 绝对不能将 SqlSession 实例的引用放在一个类的静态域，甚至一个类的实例变量也不行。 也绝不能将 SqlSession 实例的引用放在任何类型的托管作用域中，比如 Servlet 框架中的 HttpSession。 如果你现在正在使用一种 Web 框架，考虑将 SqlSession 放在一个和 HTTP 请求相似的作用域中。 换句话说，每次收到 HTTP 请求，就可以打开一个 SqlSession，返回一个响应后，就关闭它。 这个关闭操作很重要，为了确保每次都能执行关闭操作，你应该把这个关闭操作放到 finally 块中。 下面的示例就是一个确保 SqlSession 关闭的标准模式：</li></ul><div class="hljs"><pre><code class="hljs java"><span class="hljs-keyword">try</span> (SqlSession session = sqlSessionFactory.openSession()) &#123;  <span class="hljs-comment">// 你的应用逻辑代码</span>&#125;</code></pre></div><ul><li><code>Mapper</code> 映射器是一些绑定映射语句的接口。映射器接口的实例是从 SqlSession 中获得的。虽然从技术层面上来讲，任何映射器实例的最大作用域与请求它们的 SqlSession 相同。但方法作用域才是映射器实例的最合适的作用域。 也就是说，映射器实例应该在调用它们的方法中被获取，使用完毕之后即可丢弃。 映射器实例并不需要被显式地关闭。尽管在整个请求作用域保留映射器实例不会有什么问题，但是你很快会发现，在这个作用域上管理太多像 SqlSession 的资源会让你忙不过来。 因此，最好将映射器放在方法作用域内。就像下面的例子一样：</li></ul><div class="hljs"><pre><code class="hljs java"><span class="hljs-keyword">try</span> (SqlSession session = sqlSessionFactory.openSession()) &#123;  BlogMapper mapper = session.getMapper(BlogMapper<span class="hljs-class">.<span class="hljs-keyword">class</span>)</span>;  <span class="hljs-comment">// 你的应用逻辑代码</span>&#125;</code></pre></div><h3 id="依赖注入框架"><a href="#依赖注入框架" class="headerlink" title="依赖注入框架"></a>依赖注入框架</h3><p>依赖注入框架可以创建线程安全的、基于事务的 SqlSession 和映射器，并将它们直接注入到你的 bean 中，因此可以直接忽略它们的生命周期。</p><div class="hljs"><pre><code class="hljs java"><span class="hljs-meta">@Service</span><span class="hljs-keyword">public</span> <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">UserService</span> </span>&#123;    <span class="hljs-meta">@Autowired</span>    <span class="hljs-keyword">private</span> UserMapper userMapper;&#125;</code></pre></div><p>我们分析一下<code>Mybatis-Spring</code>如何做到的</p><p>Mybatis配置时我们使用了<code>@MapperScan</code>注解，底层会使用<code>MapperScannerConfigurer</code>进行扫描并将Mapper注册到Spring容器中，核心代码要看<code>org.mybatis.spring.mapper.ClassPathMapperScanner#processBeanDefinitions</code>这个方法。</p><div class="hljs"><pre><code class="hljs java"><span class="hljs-function"><span class="hljs-keyword">private</span> <span class="hljs-keyword">void</span> <span class="hljs-title">processBeanDefinitions</span><span class="hljs-params">(Set&lt;BeanDefinitionHolder&gt; beanDefinitions)</span> </span>&#123;  GenericBeanDefinition definition;  <span class="hljs-keyword">for</span> (BeanDefinitionHolder holder : beanDefinitions) &#123;    definition = (GenericBeanDefinition) holder.getBeanDefinition();    <span class="hljs-comment">// 这两句代码时关键，这里将 beanClass 进行了重新设置，设置为了 MapperFactoryBean，并且将原来接口 class 设置为构造方法参数</span>    definition.getConstructorArgumentValues().addGenericArgumentValue(definition.getBeanClassName()); <span class="hljs-comment">// issue #59</span>    definition.setBeanClass(<span class="hljs-keyword">this</span>.mapperFactoryBean.getClass());    definition.getPropertyValues().add(<span class="hljs-string">"addToConfig"</span>, <span class="hljs-keyword">this</span>.addToConfig);    <span class="hljs-comment">// 设置 sqlSessionFactory，如果有配置</span>    <span class="hljs-keyword">boolean</span> explicitFactoryUsed = <span class="hljs-keyword">false</span>;    <span class="hljs-keyword">if</span> (StringUtils.hasText(<span class="hljs-keyword">this</span>.sqlSessionFactoryBeanName)) &#123;      definition.getPropertyValues().add(<span class="hljs-string">"sqlSessionFactory"</span>, <span class="hljs-keyword">new</span> RuntimeBeanReference(<span class="hljs-keyword">this</span>.sqlSessionFactoryBeanName));      explicitFactoryUsed = <span class="hljs-keyword">true</span>;    &#125; <span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span> (<span class="hljs-keyword">this</span>.sqlSessionFactory != <span class="hljs-keyword">null</span>) &#123;      definition.getPropertyValues().add(<span class="hljs-string">"sqlSessionFactory"</span>, <span class="hljs-keyword">this</span>.sqlSessionFactory);      explicitFactoryUsed = <span class="hljs-keyword">true</span>;    &#125;    <span class="hljs-comment">// 设置 sqlSessionTemplate，如果有配置</span>    <span class="hljs-keyword">if</span> (StringUtils.hasText(<span class="hljs-keyword">this</span>.sqlSessionTemplateBeanName)) &#123;      definition.getPropertyValues().add(<span class="hljs-string">"sqlSessionTemplate"</span>, <span class="hljs-keyword">new</span> RuntimeBeanReference(<span class="hljs-keyword">this</span>.sqlSessionTemplateBeanName));      explicitFactoryUsed = <span class="hljs-keyword">true</span>;    &#125; <span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span> (<span class="hljs-keyword">this</span>.sqlSessionTemplate != <span class="hljs-keyword">null</span>) &#123;      definition.getPropertyValues().add(<span class="hljs-string">"sqlSessionTemplate"</span>, <span class="hljs-keyword">this</span>.sqlSessionTemplate);      explicitFactoryUsed = <span class="hljs-keyword">true</span>;    &#125;    <span class="hljs-comment">// 如果 sqlSessionFactory &amp; sqlSessionTemplate 都没有指定，那么启用类型自动注入</span>    <span class="hljs-keyword">if</span> (!explicitFactoryUsed) &#123;      definition.setAutowireMode(AbstractBeanDefinition.AUTOWIRE_BY_TYPE);    &#125;  &#125;&#125;</code></pre></div><p>方法中<code>this.mapperFactoryBean.getClass()</code>会固定返回<code>org.mybatis.spring.mapper.MapperFactoryBean</code>，<code>definition.getBeanClassName()</code>返回的Mapper的类名作为<code>MapperFactoryBean</code>的属性。<br>Spring初始化进行装配时会调用<code>MapperFactoryBean</code>的<code>getObject()</code>。</p><div class="hljs"><pre><code class="hljs java"><span class="hljs-function"><span class="hljs-keyword">public</span> SqlSession <span class="hljs-title">getSqlSession</span><span class="hljs-params">()</span> </span>&#123;    <span class="hljs-keyword">return</span> <span class="hljs-keyword">this</span>.sqlSession;&#125;  <span class="hljs-meta">@Override</span><span class="hljs-function"><span class="hljs-keyword">public</span> T <span class="hljs-title">getObject</span><span class="hljs-params">()</span> <span class="hljs-keyword">throws</span> Exception </span>&#123;    <span class="hljs-keyword">return</span> getSqlSession().getMapper(<span class="hljs-keyword">this</span>.mapperInterface);&#125;</code></pre></div><p>看到这，就知道了怎么将Mapper声明成bean，但是getSqlSession()返回的是属性中的sqlSession，Mybatis原生的写法中sqlSession不是线程安全的，那Mybatis-Spring如何做到用一个sqlSession处理多个线程的请求？我们继续看这个sqlSession有什么特别的。</p><div class="hljs"><pre><code class="hljs java"><span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">setSqlSessionFactory</span><span class="hljs-params">(SqlSessionFactory sqlSessionFactory)</span> </span>&#123;  <span class="hljs-keyword">if</span> (!<span class="hljs-keyword">this</span>.externalSqlSession) &#123;    <span class="hljs-keyword">this</span>.sqlSession = <span class="hljs-keyword">new</span> SqlSessionTemplate(sqlSessionFactory);  &#125;&#125;</code></pre></div><p>此处使用的<code>sqlSession</code>并非是Mybatis的<code>DefaultSqlSession</code>，而是<code>Mybatis-Spring</code>提供的<code>SqlSessionTemplate</code>。</p><div class="hljs"><pre><code class="hljs java"><span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-title">SqlSessionTemplate</span><span class="hljs-params">(SqlSessionFactory sqlSessionFactory, ExecutorType executorType,</span></span><span class="hljs-function"><span class="hljs-params">    PersistenceExceptionTranslator exceptionTranslator)</span> </span>&#123;  notNull(sqlSessionFactory, <span class="hljs-string">"Property 'sqlSessionFactory' is required"</span>);  notNull(executorType, <span class="hljs-string">"Property 'executorType' is required"</span>);  <span class="hljs-keyword">this</span>.sqlSessionFactory = sqlSessionFactory;  <span class="hljs-keyword">this</span>.executorType = executorType;  <span class="hljs-keyword">this</span>.exceptionTranslator = exceptionTranslator;  <span class="hljs-keyword">this</span>.sqlSessionProxy = (SqlSession) newProxyInstance(      SqlSessionFactory<span class="hljs-class">.<span class="hljs-keyword">class</span>.<span class="hljs-title">getClassLoader</span>(),</span>      new Class[] &#123; SqlSession.class &#125;,      <span class="hljs-keyword">new</span> SqlSessionInterceptor());&#125;<span class="hljs-meta">@Override</span><span class="hljs-keyword">public</span> &lt;T&gt; <span class="hljs-function">T <span class="hljs-title">selectOne</span><span class="hljs-params">(String statement)</span> </span>&#123;  <span class="hljs-keyword">return</span> <span class="hljs-keyword">this</span>.sqlSessionProxy.&lt;T&gt; selectOne(statement);&#125;</code></pre></div><p><code>SqlSessionTemplate</code>虽然是<code>SqlSession</code>的实现，但内部真正调用并非是他自己，而是生成了一个代理类，我们再看一下代理。</p><div class="hljs"><pre><code class="hljs java"><span class="hljs-keyword">private</span> <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">SqlSessionInterceptor</span> <span class="hljs-keyword">implements</span> <span class="hljs-title">InvocationHandler</span> </span>&#123;  <span class="hljs-meta">@Override</span>  <span class="hljs-function"><span class="hljs-keyword">public</span> Object <span class="hljs-title">invoke</span><span class="hljs-params">(Object proxy, Method method, Object[] args)</span> <span class="hljs-keyword">throws</span> Throwable </span>&#123;    SqlSession sqlSession = getSqlSession(        SqlSessionTemplate.<span class="hljs-keyword">this</span>.sqlSessionFactory,        SqlSessionTemplate.<span class="hljs-keyword">this</span>.executorType,        SqlSessionTemplate.<span class="hljs-keyword">this</span>.exceptionTranslator);    <span class="hljs-keyword">try</span> &#123;      Object result = method.invoke(sqlSession, args);      <span class="hljs-keyword">if</span> (!isSqlSessionTransactional(sqlSession, SqlSessionTemplate.<span class="hljs-keyword">this</span>.sqlSessionFactory)) &#123;        <span class="hljs-comment">// force commit even on non-dirty sessions because some databases require</span>        <span class="hljs-comment">// a commit/rollback before calling close()</span>        sqlSession.commit(<span class="hljs-keyword">true</span>);      &#125;      <span class="hljs-keyword">return</span> result;    &#125; <span class="hljs-keyword">catch</span> (Throwable t) &#123;      Throwable unwrapped = unwrapThrowable(t);      <span class="hljs-keyword">if</span> (SqlSessionTemplate.<span class="hljs-keyword">this</span>.exceptionTranslator != <span class="hljs-keyword">null</span> &amp;&amp; unwrapped <span class="hljs-keyword">instanceof</span> PersistenceException) &#123;        <span class="hljs-comment">// release the connection to avoid a deadlock if the translator is no loaded. See issue #22</span>        closeSqlSession(sqlSession, SqlSessionTemplate.<span class="hljs-keyword">this</span>.sqlSessionFactory);        sqlSession = <span class="hljs-keyword">null</span>;        Throwable translated = SqlSessionTemplate.<span class="hljs-keyword">this</span>.exceptionTranslator.translateExceptionIfPossible((PersistenceException) unwrapped);        <span class="hljs-keyword">if</span> (translated != <span class="hljs-keyword">null</span>) &#123;          unwrapped = translated;        &#125;      &#125;      <span class="hljs-keyword">throw</span> unwrapped;    &#125; <span class="hljs-keyword">finally</span> &#123;      <span class="hljs-keyword">if</span> (sqlSession != <span class="hljs-keyword">null</span>) &#123;        closeSqlSession(sqlSession, SqlSessionTemplate.<span class="hljs-keyword">this</span>.sqlSessionFactory);      &#125;    &#125;  &#125;&#125;</code></pre></div><p><code>sqlSessionProxy</code>运行时，每次都会判断当前事务中是否有SqlSession，没有就重新创建，这样可以保证多线程访问不会出现问题，也可以保证事务的传递。</p><h3 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h3><p>Mybatis-Spring代码量不大，但实现比较巧妙，如果对Mapper注入和Spring事务感兴趣可以深入了解一下。</p><h3 id="参考文档"><a href="#参考文档" class="headerlink" title="参考文档"></a>参考文档</h3><p><a href="https://mybatis.org/mybatis-3/" target="_blank" rel="noopener">MyBatis.org</a></p><p><a href="http://mybatis.org/spring/zh/getting-started.html" target="_blank" rel="noopener">mybatis-spring</a></p><p><a href="https://my.oschina.net/u/174366/blog/1788966" target="_blank" rel="noopener">mybatis MapperScannerConfigurer 源码剖析</a></p><p><a href="https://juejin.im/post/6844904033610629128" target="_blank" rel="noopener">Mybatis源码系列3-三种SqlSession的区别</a></p>]]></content>
    
    
    <categories>
      
      <category>Java</category>
      
    </categories>
    
    
    <tags>
      
      <tag>Java</tag>
      
      <tag>Spring</tag>
      
      <tag>Mybatis</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>Python logging模块日志流处理流程</title>
    <link href="/post/2c25ac60.html"/>
    <url>/post/2c25ac60.html</url>
    
    <content type="html"><![CDATA[<h3 id="logging模块的四大组件"><a href="#logging模块的四大组件" class="headerlink" title="logging模块的四大组件"></a>logging模块的四大组件</h3><table><thead><tr><th>组件名称</th><th>对应类名</th><th>功能描述</th></tr></thead><tbody><tr><td>日志器</td><td>Logger</td><td>提供了应用程序可一直使用的接口</td></tr><tr><td>处理器</td><td>Handler</td><td>将logger创建的日志记录发送到合适的目的输出</td></tr><tr><td>过滤器</td><td>Filter</td><td>提供了更细粒度的控制工具来决定输出哪条日志记录，丢弃哪条日志记录</td></tr><tr><td>格式器</td><td>Formatter</td><td>决定日志记录的最终输出格式</td></tr></tbody></table><p>logging模块就是通过这些组件来完成日志处理的，上面所使用的logging模块级别的函数也是通过这些组件对应的类来实现的。</p><a id="more"></a><p>这些组件之间的关系描述：</p><p>日志器（logger）需要通过处理器（handler）将日志信息输出到目标位置，如：文件、sys.stdout、网络等。不同的处理器（handler）可以将日志输出到不同的位置。日志器（logger）可以设置多个处理器（handler）将同一条日志记录输出到不同的位置。每个处理器（handler）都可以设置自己的过滤器（filter）实现日志过滤，从而只保留感兴趣的日志。每个处理器（handler）都可以设置自己的格式器（formatter）实现同一条日志以不同的格式输出到不同的地方。</p><p>简单点说就是：日志器（logger）是入口，真正干活儿的是处理器（handler），处理器（handler）还可以通过过滤器（filter）和格式器（formatter）对要输出的日志内容做过滤和格式化等处理操作。</p><h3 id="使用yaml进行日志配置"><a href="#使用yaml进行日志配置" class="headerlink" title="使用yaml进行日志配置"></a>使用yaml进行日志配置</h3><div class="hljs"><pre><code class="hljs yaml"><span class="hljs-attr">version:</span> <span class="hljs-number">1</span><span class="hljs-attr">formatters:</span>  <span class="hljs-attr">simple:</span>    <span class="hljs-attr">format:</span> <span class="hljs-string">'%(asctime)s - %(name)s - %(levelname)s - %(message)s'</span><span class="hljs-attr">handlers:</span>  <span class="hljs-attr">console:</span>    <span class="hljs-attr">class:</span> <span class="hljs-string">logging.StreamHandler</span>    <span class="hljs-attr">level:</span> <span class="hljs-string">DEBUG</span>    <span class="hljs-attr">formatter:</span> <span class="hljs-string">simple</span>  <span class="hljs-attr">file:</span>    <span class="hljs-attr">class:</span> <span class="hljs-string">logging.FileHandler</span>    <span class="hljs-attr">filename:</span> <span class="hljs-string">logging.log</span>    <span class="hljs-attr">encoding:</span> <span class="hljs-string">utf-8</span>    <span class="hljs-attr">level:</span> <span class="hljs-string">INFO</span>    <span class="hljs-attr">formatter:</span> <span class="hljs-string">simple</span>  <span class="hljs-attr">loggers:</span>  <span class="hljs-attr">simpleLogger:</span>    <span class="hljs-attr">level:</span> <span class="hljs-string">INFO</span>    <span class="hljs-attr">handlers:</span> <span class="hljs-string">[console,file]</span>    <span class="hljs-attr">propagate:</span> <span class="hljs-literal">no</span><span class="hljs-attr">root:</span>  <span class="hljs-attr">level:</span> <span class="hljs-string">INFO</span>  <span class="hljs-attr">handlers:</span> <span class="hljs-string">[console]</span></code></pre></div><div class="hljs"><pre><code class="hljs python"><span class="hljs-keyword">import</span> logging.config<span class="hljs-keyword">import</span> yamlconfig = <span class="hljs-literal">None</span><span class="hljs-keyword">with</span> open(<span class="hljs-string">'log.yaml'</span>, <span class="hljs-string">'r'</span>) <span class="hljs-keyword">as</span> f:    config = yaml.safe_load(f.read())    logging.config.dictConfig(config)<span class="hljs-function"><span class="hljs-keyword">def</span> <span class="hljs-title">getLogger</span><span class="hljs-params">()</span>:</span>    <span class="hljs-keyword">return</span> logging.getLogger(<span class="hljs-string">'simpleLogger'</span>)<span class="hljs-keyword">if</span> __name__ == <span class="hljs-string">"__main__"</span>:    logger = getLogger()        logger.debug(<span class="hljs-string">'debug'</span>)    logger.info(<span class="hljs-string">'info:%s'</span>,<span class="hljs-string">'message'</span>)    logger.error(<span class="hljs-string">'error'</span>)</code></pre></div><p><code>simpleLogger</code>配置的日志级别为INFO，debug的日志没有打印</p><div class="hljs"><pre><code class="hljs angelscript"><span class="hljs-number">2020</span><span class="hljs-number">-11</span><span class="hljs-number">-02</span> <span class="hljs-number">16</span>:<span class="hljs-number">22</span>:<span class="hljs-number">09</span>,<span class="hljs-number">194</span> - simpleLogger - INFO - info:message<span class="hljs-number">2020</span><span class="hljs-number">-11</span><span class="hljs-number">-02</span> <span class="hljs-number">16</span>:<span class="hljs-number">22</span>:<span class="hljs-number">09</span>,<span class="hljs-number">195</span> - simpleLogger - ERROR - error</code></pre></div><h3 id="参考文档"><a href="#参考文档" class="headerlink" title="参考文档"></a>参考文档</h3><p><a href="https://www.cnblogs.com/yyds/p/6901864.html" target="_blank" rel="noopener">Python之日志处理（logging模块）</a></p>]]></content>
    
    
    <categories>
      
      <category>Python</category>
      
    </categories>
    
    
    <tags>
      
      <tag>Python</tag>
      
      <tag>log</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>DataX同步全量Mqsql数据到Elasticsearch</title>
    <link href="/post/6a3f5dff.html"/>
    <url>/post/6a3f5dff.html</url>
    
    <content type="html"><![CDATA[<p><a href="https://github.com/alibaba/DataX" target="_blank" rel="noopener">DataX</a> 是阿里巴巴集团内被广泛使用的离线数据同步工具/平台，实现包括 MySQL、SQL Server、Oracle、PostgreSQL、HDFS、Hive、HBase、OTS、ODPS 等各种异构数据源之间高效的数据同步功能。</p><p>DataX本身作为数据同步框架，将不同数据源的同步抽象为从源头数据源读取数据的Reader插件，以及向目标端写入数据的Writer插件，理论上DataX框架可以支持任意数据源类型的数据同步工作。同时DataX插件体系作为一套生态系统, 每接入一套新数据源该新加入的数据源即可实现和现有的数据源互通。</p><a id="more"></a><h3 id="编译"><a href="#编译" class="headerlink" title="编译"></a>编译</h3><p>官方提供的二进制包缺少必要的组件，我们需要下载源码自己编译</p><div class="hljs"><pre><code class="hljs awk">wget https:<span class="hljs-regexp">//</span>codeload.github.com<span class="hljs-regexp">/alibaba/</span>DataX<span class="hljs-regexp">/zip/m</span>aster</code></pre></div><p>解压后编辑pom.xml，只保留部分必要module和mysql，es即可</p><div class="hljs"><pre><code class="hljs xml"><span class="hljs-tag">&lt;<span class="hljs-name">modules</span>&gt;</span>    <span class="hljs-tag">&lt;<span class="hljs-name">module</span>&gt;</span>common<span class="hljs-tag">&lt;/<span class="hljs-name">module</span>&gt;</span>    <span class="hljs-tag">&lt;<span class="hljs-name">module</span>&gt;</span>core<span class="hljs-tag">&lt;/<span class="hljs-name">module</span>&gt;</span>    <span class="hljs-tag">&lt;<span class="hljs-name">module</span>&gt;</span>transformer<span class="hljs-tag">&lt;/<span class="hljs-name">module</span>&gt;</span>    <span class="hljs-comment">&lt;!-- reader --&gt;</span>    <span class="hljs-tag">&lt;<span class="hljs-name">module</span>&gt;</span>mysqlreader<span class="hljs-tag">&lt;/<span class="hljs-name">module</span>&gt;</span>    <span class="hljs-comment">&lt;!-- writer --&gt;</span>    <span class="hljs-tag">&lt;<span class="hljs-name">module</span>&gt;</span>elasticsearchwriter<span class="hljs-tag">&lt;/<span class="hljs-name">module</span>&gt;</span>    <span class="hljs-comment">&lt;!-- common support module --&gt;</span>    <span class="hljs-tag">&lt;<span class="hljs-name">module</span>&gt;</span>plugin-rdbms-util<span class="hljs-tag">&lt;/<span class="hljs-name">module</span>&gt;</span>    <span class="hljs-tag">&lt;<span class="hljs-name">module</span>&gt;</span>plugin-unstructured-storage-util<span class="hljs-tag">&lt;/<span class="hljs-name">module</span>&gt;</span>    <span class="hljs-tag">&lt;<span class="hljs-name">module</span>&gt;</span>hbase20xsqlreader<span class="hljs-tag">&lt;/<span class="hljs-name">module</span>&gt;</span>    <span class="hljs-tag">&lt;<span class="hljs-name">module</span>&gt;</span>hbase20xsqlwriter<span class="hljs-tag">&lt;/<span class="hljs-name">module</span>&gt;</span>    <span class="hljs-tag">&lt;<span class="hljs-name">module</span>&gt;</span>kuduwriter<span class="hljs-tag">&lt;/<span class="hljs-name">module</span>&gt;</span><span class="hljs-tag">&lt;/<span class="hljs-name">modules</span>&gt;</span></code></pre></div><p>更改配置后打包</p><div class="hljs"><pre><code class="hljs crystal">mvn -U clean package <span class="hljs-symbol">assembly:</span>assembly -Dmaven.test.skip=<span class="hljs-literal">true</span>ls ./target/datax/datax/binconfjob<span class="hljs-class"><span class="hljs-keyword">lib</span><span class="hljs-title">log</span><span class="hljs-title">log_perf</span><span class="hljs-title">plugin</span><span class="hljs-title">script</span><span class="hljs-title">tmp</span></span></code></pre></div><h3 id="设置动态模板"><a href="#设置动态模板" class="headerlink" title="设置动态模板"></a>设置动态模板</h3><p>开始同步数据前，在es中创建一个动态模板</p><div class="hljs"><pre><code class="hljs json">PUT _template/hkey_transferbill&#123;    <span class="hljs-attr">"index_patterns"</span>:<span class="hljs-string">"hkey_transferbill"</span>,    <span class="hljs-attr">"settings"</span>:&#123;        <span class="hljs-attr">"number_of_shards"</span>:<span class="hljs-number">2</span>,        <span class="hljs-attr">"number_of_replicas"</span>:<span class="hljs-number">1</span>    &#125;,    <span class="hljs-attr">"mappings"</span>:&#123;        <span class="hljs-attr">"data"</span>:&#123;            <span class="hljs-attr">"dynamic_templates"</span>:[                &#123;                    <span class="hljs-attr">"keyword_field"</span>:&#123;                        <span class="hljs-attr">"unmatch"</span>:<span class="hljs-string">"*Name"</span>,                        <span class="hljs-attr">"match_mapping_type"</span>:<span class="hljs-string">"string"</span>,                        <span class="hljs-attr">"mapping"</span>:&#123;                            <span class="hljs-attr">"type"</span>:<span class="hljs-string">"keyword"</span>                        &#125;                    &#125;                &#125;,                &#123;                    <span class="hljs-attr">"text_fields"</span>:&#123;                        <span class="hljs-attr">"match"</span>:<span class="hljs-string">"*Name"</span>,                        <span class="hljs-attr">"match_mapping_type"</span>:<span class="hljs-string">"string"</span>,                        <span class="hljs-attr">"mapping"</span>:&#123;                            <span class="hljs-attr">"type"</span>:<span class="hljs-string">"text"</span>,                            <span class="hljs-attr">"fields"</span>:&#123;                                <span class="hljs-attr">"raw"</span>:&#123;                                    <span class="hljs-attr">"type"</span>:<span class="hljs-string">"keyword"</span>                                &#125;                            &#125;                        &#125;                    &#125;                &#125;            ],            <span class="hljs-attr">"properties"</span>:&#123;                <span class="hljs-attr">"rowkey"</span>:&#123;                    <span class="hljs-attr">"type"</span>:<span class="hljs-string">"keyword"</span>                &#125;,                <span class="hljs-attr">"age"</span>:&#123;                    <span class="hljs-attr">"type"</span>:<span class="hljs-string">"keyword"</span>                &#125;            &#125;        &#125;    &#125;&#125;</code></pre></div><h3 id="编写同步配置文件"><a href="#编写同步配置文件" class="headerlink" title="编写同步配置文件"></a>编写同步配置文件</h3><p><a href="https://github.com/alibaba/DataX/blob/master/mysqlreader/doc/mysqlreader.md" target="_blank" rel="noopener">MysqlReader 插件文档</a></p><p><a href="https://github.com/alibaba/DataX/blob/master/elasticsearchwriter/doc/elasticsearchwriter.md" target="_blank" rel="noopener">ElasticSearchWriter 插件文档</a></p><p>同步时间类型问题，部分mysql中时间字段为空，导入到ES中时间变成了系统时间，我在导入时将ES中column的type更改为<code>text</code>解决了这个问题。</p><p><code>setting.speed.channel</code>为并发数，<code>setting.speed.byte</code>是最大同步字节数</p><div class="hljs"><pre><code class="hljs json">&#123;    <span class="hljs-attr">"job"</span>:&#123;        <span class="hljs-attr">"setting"</span>:&#123;            <span class="hljs-attr">"speed"</span>:&#123;                <span class="hljs-attr">"channel"</span>:<span class="hljs-number">4</span>,                <span class="hljs-attr">"byte"</span>: <span class="hljs-number">104857600</span>            &#125;        &#125;,        <span class="hljs-attr">"content"</span>:[            &#123;                <span class="hljs-attr">"reader"</span>:&#123;                    <span class="hljs-attr">"name"</span>:<span class="hljs-string">"mysqlreader"</span>,                    <span class="hljs-attr">"parameter"</span>:&#123;                        <span class="hljs-attr">"username"</span>:<span class="hljs-string">"root"</span>,                        <span class="hljs-attr">"password"</span>:<span class="hljs-string">"test"</span>,                        <span class="hljs-attr">"column"</span>:[                            <span class="hljs-string">"code"</span>,                            <span class="hljs-string">"internal_code"</span>,                            <span class="hljs-string">"internal_code_md5"</span>                        ],                        <span class="hljs-attr">"connection"</span>:[                            &#123;                                <span class="hljs-attr">"jdbcUrl"</span>:[                                    <span class="hljs-string">"jdbc:mysql://192.168.1.200:3306/test"</span>                                ],                                <span class="hljs-attr">"table"</span>:[                                    <span class="hljs-string">"code_t"</span>                                ]                            &#125;                        ]                    &#125;                &#125;,                <span class="hljs-attr">"writer"</span>:&#123;                    <span class="hljs-attr">"name"</span>:<span class="hljs-string">"elasticsearchwriter"</span>,                    <span class="hljs-attr">"parameter"</span>:&#123;                        <span class="hljs-attr">"endpoint"</span>:<span class="hljs-string">"http://192.168.1.200:9200"</span>,                        <span class="hljs-attr">"accessId"</span>:<span class="hljs-string">"1"</span>,                        <span class="hljs-attr">"accessKey"</span>:<span class="hljs-string">"1"</span>,                        <span class="hljs-attr">"index"</span>:<span class="hljs-string">"code_t"</span>,                        <span class="hljs-attr">"type"</span>:<span class="hljs-string">"_doc"</span>,                        <span class="hljs-attr">"cleanup"</span>:<span class="hljs-literal">true</span>,                        <span class="hljs-attr">"dynamic"</span>:<span class="hljs-literal">true</span>,                        <span class="hljs-attr">"batchSize"</span>:<span class="hljs-number">5000</span>,                        <span class="hljs-attr">"splitter"</span>:<span class="hljs-string">","</span>,                        <span class="hljs-attr">"column"</span>:[                            &#123;                                <span class="hljs-attr">"name"</span>:<span class="hljs-string">"code"</span>,                                <span class="hljs-attr">"type"</span>:<span class="hljs-string">"long"</span>                            &#125;,                            &#123;                                <span class="hljs-attr">"name"</span>:<span class="hljs-string">"internal_code"</span>,                                <span class="hljs-attr">"type"</span>:<span class="hljs-string">"text"</span>                            &#125;,                            &#123;                                <span class="hljs-attr">"name"</span>:<span class="hljs-string">"internal_code_md5"</span>,                                <span class="hljs-attr">"type"</span>:<span class="hljs-string">"text"</span>                            &#125;                        ]                    &#125;                &#125;            &#125;        ]    &#125;&#125;</code></pre></div><h3 id="运行"><a href="#运行" class="headerlink" title="运行"></a>运行</h3><p>运行需要python环境，官方推荐2.6，我本地装的是2.7，实测可以正常使用</p><div class="hljs"><pre><code class="hljs vim"><span class="hljs-keyword">python</span> datax.<span class="hljs-keyword">py</span> ./es.json</code></pre></div><h3 id="参考文档"><a href="#参考文档" class="headerlink" title="参考文档"></a>参考文档</h3><p><a href="https://blog.csdn.net/Zsigner/article/details/108362724" target="_blank" rel="noopener">【DataX 增量同步】Mysql 同步数据到 es（Elasticsearch）</a></p><p><a href="https://blog.csdn.net/ASN_forever/article/details/106340961" target="_blank" rel="noopener">datax从mysql同步数据到elasticsearch（使用es的动态模板）</a></p>]]></content>
    
    
    <categories>
      
      <category>中间件</category>
      
    </categories>
    
    
    <tags>
      
      <tag>DataX</tag>
      
      <tag>Mysql</tag>
      
      <tag>Elasticsearch</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>MacOS上使用OpenEmu玩怀旧游戏</title>
    <link href="/post/12c7a153.html"/>
    <url>/post/12c7a153.html</url>
    
    <content type="html"><![CDATA[<p><img src="https://img.yjll.art/img/OpenEmu.png" srcset="/img/loading.gif" alt="OpenEmu"></p><p>OpenEmu是MacOS上的开源项目模拟器项目，它整合了几乎全部主流的游戏机平台，你可以在mac系统上畅玩FC/NES、SFC/SNES、N64、PlayStation、GameBoy等游戏。</p><h3 id="下载模拟器"><a href="#下载模拟器" class="headerlink" title="下载模拟器"></a>下载模拟器</h3><p>官网：<a href="http://openemu.org" target="_blank" rel="noopener">http://openemu.org</a></p><p>注意一定要下载里面的Experimental版本，正式版不支持Arcade街机游戏。</p><h3 id="下载核心"><a href="#下载核心" class="headerlink" title="下载核心"></a>下载核心</h3><p><img src="https://img.yjll.art/img/openemu_setting_core.jpg" srcset="/img/loading.gif" alt="openemu_setting_core"></p><p>打开后在核心中下载对应的游戏核心，由于源在国外，访问不是很稳定，可以借助梯子解决。可以去单独下载核心core，放到指定位置<code>~/Library/Application Support/OpenEmu/Cores</code></p><p>后边的系统文件，指的是我们常说的BIOS，BIOS 就是从游戏机里 dump 出来的基本 IO 模块。绝大部分模拟器都需要这个模块来启动游戏。为了规避版权问题， BIOS 是要自己找的。因为 BIOS 的版权属于游戏机厂商（而模拟器本身只要不提供 BIOS 一般是不侵权的）。</p><p>BIOS文件放置在mac系统的位置 <code>~/Library/Application Support/OpenEmu/bios</code></p><h3 id="游戏下载"><a href="#游戏下载" class="headerlink" title="游戏下载"></a>游戏下载</h3><p>核心设置好后，我们还缺少对应的游戏，比如小时候常玩的任天堂(Nintendo)红白机，可以在下边的这个网站上下载</p><p><a href="https://www.retrostic.com/roms/nes" target="_blank" rel="noopener">https://www.retrostic.com/roms/nes</a></p><p>我们将下载后的nes文件拖拽到Nintendo(NES)对应的窗口中即可。</p><p><img src="http://openemu.org/img/intro-nes-grid.png" srcset="/img/loading.gif" alt=""></p><p>对于如合金弹头和拳皇等街机游戏直接运行的话会提示缺少文件，还需要和游戏配套的游戏BIOS，可通过<a href="https://www.mamechannel.it/pages/bios.php" target="_blank" rel="noopener">www.mamechannel.it</a>下载。放置在<code>~/Library/Application Support/OpenEmu/Game Library/roms/Arcade</code>即可</p><p>如游戏和新核心不兼容可尝试手动安装老版本核心，下载<a href="https://github.com/OpenEmu/UME-Core/releases" target="_blank" rel="noopener">UME-Core</a>覆盖<code>~/Library/Application Support/OpenEmu/Cores/MAME.oecoreplugin</code>。</p><p>附街机游戏下载</p><p><a href="https://ia803104.us.archive.org/view_archive.php?archive=/8/items/MAME217RomsOnlyMerged/MAME%200.217%20ROMs%20%28merged%29.zip" target="_blank" rel="noopener">archive.org</a></p><h3 id="参考文档"><a href="#参考文档" class="headerlink" title="参考文档"></a>参考文档</h3><p><a href="https://www.imtrq.com/archives/1629" target="_blank" rel="noopener">MacOS上的MAME街机模拟器，附三国战记+恐龙快打+拳皇97</a></p><p><a href="https://zhuanlan.zhihu.com/p/42108658" target="_blank" rel="noopener">想在Mac上玩红白机吗？OpenEmu了解一下！</a></p><p><a href="https://post.smzdm.com/p/aoo8lr79/" target="_blank" rel="noopener">OpenEmu，mac上最完整的街机和游戏机怀旧全能模拟器</a></p>]]></content>
    
    
    
    <tags>
      
      <tag>MacOS</tag>
      
      <tag>OpenEmu</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>Spring使用@Value注入List和Map</title>
    <link href="/post/a336f015.html"/>
    <url>/post/a336f015.html</url>
    
    <content type="html"><![CDATA[<h3 id="yml常规写法"><a href="#yml常规写法" class="headerlink" title="yml常规写法"></a>yml常规写法</h3><p>需要添加<code>spring-boot-configuration-processor</code>模块，并且对要注入的对象添加<code>set</code>方法</p><h4 id="List"><a href="#List" class="headerlink" title="List"></a>List</h4><div class="hljs"><pre><code class="hljs xml"><span class="hljs-tag">&lt;<span class="hljs-name">dependency</span>&gt;</span>    <span class="hljs-tag">&lt;<span class="hljs-name">groupId</span>&gt;</span>org.springframework.boot<span class="hljs-tag">&lt;/<span class="hljs-name">groupId</span>&gt;</span>    <span class="hljs-tag">&lt;<span class="hljs-name">artifactId</span>&gt;</span>spring-boot-configuration-processor<span class="hljs-tag">&lt;/<span class="hljs-name">artifactId</span>&gt;</span>    <span class="hljs-tag">&lt;<span class="hljs-name">optional</span>&gt;</span>true<span class="hljs-tag">&lt;/<span class="hljs-name">optional</span>&gt;</span><span class="hljs-tag">&lt;/<span class="hljs-name">dependency</span>&gt;</span></code></pre></div><div class="hljs"><pre><code class="hljs yaml"><span class="hljs-attr">interceptorconfig:</span>  <span class="hljs-attr">path:</span>    <span class="hljs-attr">exclude:</span>      <span class="hljs-bullet">-</span> <span class="hljs-string">/source</span>      <span class="hljs-bullet">-</span> <span class="hljs-string">/sep/user/login</span>      <span class="hljs-bullet">-</span> <span class="hljs-string">/sep/user/getCheckCode/</span><span class="hljs-string">```</span>      <span class="hljs-string">```</span> <span class="hljs-string">java</span><span class="hljs-string">@Component</span><span class="hljs-string">@Data</span><span class="hljs-string">@ConfigurationProperties(prefix</span> <span class="hljs-string">=</span> <span class="hljs-string">"interceptorconfig.path"</span><span class="hljs-string">)</span><span class="hljs-string">public</span> <span class="hljs-string">class</span> <span class="hljs-string">TokenFilter</span> <span class="hljs-string">&#123;</span>    <span class="hljs-string">private</span>  <span class="hljs-string">List&lt;String&gt;</span> <span class="hljs-string">exclude;</span><span class="hljs-string">&#125;</span></code></pre></div><h4 id="Map"><a href="#Map" class="headerlink" title="Map"></a>Map</h4><div class="hljs"><pre><code class="hljs yaml"><span class="hljs-attr">generate:</span>  <span class="hljs-attr">companyAndPlanMap:</span>    <span class="hljs-attr">101:</span> <span class="hljs-number">2018081601</span>    <span class="hljs-attr">121:</span> <span class="hljs-number">2018081602</span>    <span class="hljs-attr">111:</span> <span class="hljs-number">2018081603</span></code></pre></div><div class="hljs"><pre><code class="hljs java"><span class="hljs-meta">@RestController</span><span class="hljs-meta">@RequestMapping</span>(<span class="hljs-string">"/c"</span>)<span class="hljs-meta">@Slf</span>4j<span class="hljs-meta">@ConfigurationProperties</span>(<span class="hljs-string">"generate"</span>)<span class="hljs-keyword">public</span> <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">C35Controller</span> </span>&#123;    <span class="hljs-keyword">private</span> Map&lt;String, String&gt; companyAndPlanMap;    <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">setCompanyAndPlanMap</span><span class="hljs-params">(Map&lt;String, String&gt; companyAndPlanMap)</span> </span>&#123;        <span class="hljs-keyword">this</span>.companyAndPlanMap = companyAndPlanMap;    &#125;&#125;</code></pre></div><h3 id="EL表达式-JSON写法"><a href="#EL表达式-JSON写法" class="headerlink" title="EL表达式+JSON写法"></a>EL表达式+JSON写法</h3><div class="hljs"><pre><code class="hljs yml"><span class="hljs-attr">scio.cloud.list:</span> <span class="hljs-string">topic1,topic2,topic3</span><span class="hljs-attr">scio.cloud.maps:</span> <span class="hljs-string">"&#123;key1: 'value1', key2: 'value2'&#125;"</span></code></pre></div><div class="hljs"><pre><code class="hljs java"><span class="hljs-meta">@Value</span>(<span class="hljs-string">"#&#123;'$&#123;scio.cloud.list&#125;'.split(',')&#125;"</span>)<span class="hljs-keyword">private</span> List&lt;String&gt; list; <span class="hljs-meta">@Value</span>(<span class="hljs-string">"#&#123;$&#123;scio.cloud.maps&#125;&#125;"</span>)  <span class="hljs-keyword">private</span> Map&lt;String,String&gt; maps;</code></pre></div><h3 id="参考文档"><a href="#参考文档" class="headerlink" title="参考文档"></a>参考文档</h3><p><a href="https://www.jianshu.com/p/1aa662a5f170" target="_blank" rel="noopener">Spring @Value 注入List和Map</a></p><p><a href="https://juejin.im/post/6844903972403150861" target="_blank" rel="noopener">Spring Boot配置文件注入</a></p>]]></content>
    
    
    <categories>
      
      <category>Java</category>
      
    </categories>
    
    
    <tags>
      
      <tag>Java</tag>
      
      <tag>Spring</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>Java加密压缩Zip</title>
    <link href="/post/45e649c7.html"/>
    <url>/post/45e649c7.html</url>
    
    <content type="html"><![CDATA[<p>JDK自带的ZIP操作接口（java.util.zip包）不直接支持加密压缩，项目中需要用到加密压缩，在网上查了查，已经有人封装好了。<a href="https://github.com/srikanth-lingala/zip4j" target="_blank" rel="noopener">Zip4j</a>功能比较强大，支持加密、解密压缩，支持文件的添加、删除等。</p><h3 id="使用Zip4j进行加密压缩"><a href="#使用Zip4j进行加密压缩" class="headerlink" title="使用Zip4j进行加密压缩"></a>使用Zip4j进行加密压缩</h3><div class="hljs"><pre><code class="hljs xml"><span class="hljs-tag">&lt;<span class="hljs-name">dependency</span>&gt;</span>    <span class="hljs-tag">&lt;<span class="hljs-name">groupId</span>&gt;</span>net.lingala.zip4j<span class="hljs-tag">&lt;/<span class="hljs-name">groupId</span>&gt;</span>    <span class="hljs-tag">&lt;<span class="hljs-name">artifactId</span>&gt;</span>zip4j<span class="hljs-tag">&lt;/<span class="hljs-name">artifactId</span>&gt;</span>    <span class="hljs-tag">&lt;<span class="hljs-name">version</span>&gt;</span>2.6.4<span class="hljs-tag">&lt;/<span class="hljs-name">version</span>&gt;</span><span class="hljs-tag">&lt;/<span class="hljs-name">dependency</span>&gt;</span></code></pre></div><div class="hljs"><pre><code class="hljs java"><span class="hljs-keyword">byte</span>[] buffer = writer.toString().getBytes(StandardCharsets.US_ASCII);ZipParameters zipParameters = <span class="hljs-keyword">new</span> ZipParameters();<span class="hljs-comment">// 使用密码</span>zipParameters.setEncryptFiles(<span class="hljs-keyword">true</span>);<span class="hljs-comment">// 加密方式</span>zipParameters.setEncryptionMethod(EncryptionMethod.AES);zipParameters.setAesKeyStrength(AesKeyStrength.KEY_STRENGTH_256);<span class="hljs-comment">// zip中的文件名</span>zipParameters.setFileNameInZip(String.format(<span class="hljs-string">"%s.txt"</span>, packageName));File tempFile = File.createTempFile(packageName, <span class="hljs-string">".zip"</span>);<span class="hljs-comment">// 先删除，fileZip会自动创建</span>tempFile.delete();log.info(<span class="hljs-string">"file_path:&#123;&#125;"</span>, tempFile.getAbsolutePath());<span class="hljs-keyword">new</span> ZipFile(tempFile, <span class="hljs-keyword">this</span>.password.toCharArray()).addStream(<span class="hljs-keyword">new</span> ByteArrayInputStream(buffer), zipParameters);</code></pre></div><p>我生成压缩文件是为了上传使用，所以使用了<code>File.createTempFile</code>创建了一个临时文件，实际使用时发现生成压缩包时<code>zip4j</code>会判断我们传入的<code>file</code>是否为一个zip文件，我们这个文件是临时创建的是一个长度为0的空文件，所以直接报错了。</p><div class="hljs"><pre><code class="hljs java"><span class="hljs-keyword">if</span> (zip4jRaf.length() &lt; ENDHDR) &#123;    <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> ZipException(<span class="hljs-string">"Zip file size less than minimum expected zip file size. "</span> +        <span class="hljs-string">"Probably not a zip file or a corrupted zip file"</span>);&#125;</code></pre></div><p>我再<code>File.createTempFile</code>后又将这个文件删除了，把生成文件的任务交给<code>zip4j</code>去处理，这样就没有问题了。</p><h3 id="参考文档"><a href="#参考文档" class="headerlink" title="参考文档"></a>参考文档</h3><p><a href="https://github.com/srikanth-lingala/zip4j" target="_blank" rel="noopener">Zip4j - A Java library for zip files / streams</a></p>]]></content>
    
    
    <categories>
      
      <category>Java</category>
      
    </categories>
    
    
    <tags>
      
      <tag>Java</tag>
      
      <tag>Zip</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>Elasticsearch使用JSON字符串的原生DSL查询</title>
    <link href="/post/3b7237cd.html"/>
    <url>/post/3b7237cd.html</url>
    
    <content type="html"><![CDATA[<blockquote><p>注：本文的<code>spring-boot-starter-data-elasticsearch</code>的版本为<code>2.2.10.RELEASE</code><br>Elasticsearch版本为<code>6.8.12</code></p></blockquote><p><code>RestHighLevelClient</code>是ES官方推荐使用的客户端，新版本的<code>spring-data-elasticsearch</code>也对<code>RestHighLevelClient</code>进行了支持，使用<code>ElasticsearchRestTemplate</code>对<code>RestHighLevelClient</code>进行封装，我们直接使用<code>ElasticsearchRestTemplate</code>即可，但是对于参数较多特别是有嵌套聚合的查询，使用Spring提供的API过于繁琐，我们想使用原生的DSL进行查询，但是<code>ElasticsearchRestTemplate</code>并不支持这种写法。我们知道<code>ElasticsearchRestTemplate</code>底层一定通过JSON传输，分析内部实现，看看实现方式。</p><h3 id="ElasticsearchRestTemplate底层"><a href="#ElasticsearchRestTemplate底层" class="headerlink" title="ElasticsearchRestTemplate底层"></a>ElasticsearchRestTemplate底层</h3><p>拿ElasticsearchRestTemplate#query这个方法举例，<code>query</code>调用<code>client.search()</code>，这个client就是<code>RestHighLevelClient</code>，Spring对其进行了二次封装。</p><div class="hljs"><pre><code class="hljs java"><span class="hljs-meta">@Override</span><span class="hljs-keyword">public</span> &lt;T&gt; <span class="hljs-function">T <span class="hljs-title">query</span><span class="hljs-params">(SearchQuery query, ResultsExtractor&lt;T&gt; resultsExtractor)</span> </span>&#123;SearchResponse response = doSearch(prepareSearch(query, Optional.ofNullable(query.getQuery()), <span class="hljs-keyword">null</span>), query);<span class="hljs-keyword">return</span> resultsExtractor.extract(response);&#125;   <span class="hljs-function"><span class="hljs-keyword">private</span> SearchResponse <span class="hljs-title">doSearch</span><span class="hljs-params">(SearchRequest searchRequest, SearchQuery searchQuery)</span> </span>&#123;prepareSearch(searchRequest, searchQuery);<span class="hljs-keyword">try</span> &#123;<span class="hljs-keyword">return</span> client.search(searchRequest, RequestOptions.DEFAULT);&#125; <span class="hljs-keyword">catch</span> (IOException e) &#123;     <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> ElasticsearchException(<span class="hljs-string">"Error for search request with scroll: "</span> + searchRequest.toString(), e);&#125;&#125;</code></pre></div><p>继续看<code>RestHighLevelClient#search</code>方法，主要是对参数和返回值进行封装，层层跟进该方法会调用<code>client.performRequest</code>，此处的<code>client</code>并非指<code>RestHighLevelClient</code>，而是更底层的客户端<code>RestClient</code>。</p><div class="hljs"><pre><code class="hljs java"><span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">final</span> SearchResponse <span class="hljs-title">search</span><span class="hljs-params">(SearchRequest searchRequest, RequestOptions options)</span> <span class="hljs-keyword">throws</span> IOException </span>&#123;    <span class="hljs-keyword">return</span> performRequestAndParseEntity(            searchRequest,            r -&gt; RequestConverters.search(r, <span class="hljs-string">"_search"</span>),            options,            SearchResponse::fromXContent,            emptySet());&#125;<span class="hljs-keyword">private</span> &lt;Req, Resp&gt; <span class="hljs-function">Resp <span class="hljs-title">internalPerformRequest</span><span class="hljs-params">(Req request,</span></span><span class="hljs-function"><span class="hljs-params">                                        CheckedFunction&lt;Req, Request, IOException&gt; requestConverter,</span></span><span class="hljs-function"><span class="hljs-params">                                        RequestOptions options,</span></span><span class="hljs-function"><span class="hljs-params">                                        CheckedFunction&lt;Response, Resp, IOException&gt; responseConverter,</span></span><span class="hljs-function"><span class="hljs-params">                                        Set&lt;Integer&gt; ignores)</span> <span class="hljs-keyword">throws</span> IOException </span>&#123;    Request req = requestConverter.apply(request);    req.setOptions(options);    Response response;    <span class="hljs-keyword">try</span> &#123;        response = client.performRequest(req);    &#125; <span class="hljs-keyword">catch</span> (ResponseException e) &#123;        <span class="hljs-keyword">if</span> (ignores.contains(e.getResponse().getStatusLine().getStatusCode())) &#123;            <span class="hljs-keyword">try</span> &#123;                <span class="hljs-keyword">return</span> responseConverter.apply(e.getResponse());            &#125; <span class="hljs-keyword">catch</span> (Exception innerException) &#123;                <span class="hljs-comment">// the exception is ignored as we now try to parse the response as an error.</span>                <span class="hljs-comment">// this covers cases like get where 404 can either be a valid document not found response,</span>                <span class="hljs-comment">// or an error for which parsing is completely different. We try to consider the 404 response as a valid one</span>                <span class="hljs-comment">// first. If parsing of the response breaks, we fall back to parsing it as an error.</span>                <span class="hljs-keyword">throw</span> parseResponseException(e);            &#125;        &#125;        <span class="hljs-keyword">throw</span> parseResponseException(e);    &#125;    <span class="hljs-keyword">try</span> &#123;        <span class="hljs-keyword">return</span> responseConverter.apply(response);    &#125; <span class="hljs-keyword">catch</span>(Exception e) &#123;        <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> IOException(<span class="hljs-string">"Unable to parse response body for "</span> + response, e);    &#125;&#125;</code></pre></div><p><code>RestClient#performRequest</code>接受的参数为<code>org.elasticsearch.client.Request</code>，其中重要的参数有一下几个:<br><code>method</code>:Http的方法<code>GET</code>,<code>POST</code>等<br><code>endpoint</code>:包含<code>index</code>,<code>type</code>,查询类别等,如<code>code/data/_search</code><br><code>entity</code>:包好JSON的实体</p><div class="hljs"><pre><code class="hljs java"><span class="hljs-function"><span class="hljs-keyword">public</span> Response <span class="hljs-title">performRequest</span><span class="hljs-params">(Request request)</span> <span class="hljs-keyword">throws</span> IOException </span>&#123;    SyncResponseListener listener = <span class="hljs-keyword">new</span> SyncResponseListener(maxRetryTimeoutMillis);    performRequestAsyncNoCatch(request, listener);    <span class="hljs-keyword">return</span> listener.get();&#125;<span class="hljs-keyword">public</span> <span class="hljs-keyword">final</span> <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">Request</span> </span>&#123;    <span class="hljs-keyword">private</span> <span class="hljs-keyword">final</span> String method;    <span class="hljs-keyword">private</span> <span class="hljs-keyword">final</span> String endpoint;    <span class="hljs-keyword">private</span> <span class="hljs-keyword">final</span> Map&lt;String, String&gt; parameters = <span class="hljs-keyword">new</span> HashMap&lt;&gt;();    <span class="hljs-keyword">private</span> HttpEntity entity;    <span class="hljs-keyword">private</span> RequestOptions options = RequestOptions.DEFAULT;&#125;</code></pre></div><h3 id="使用RestClient进行请求"><a href="#使用RestClient进行请求" class="headerlink" title="使用RestClient进行请求"></a>使用RestClient进行请求</h3><p>知道底层的运行方式，我们构建一个Request再调用<code>RestClient#performRequest</code>即可。因为涉及到字符串拼接和json序列化，用Java不太方便，我这里使用Groovy语法编写。</p><div class="hljs"><pre><code class="hljs groovy">String dsl = <span class="hljs-string">"""</span><span class="hljs-string">        &#123;</span><span class="hljs-string">          "size": 0,</span><span class="hljs-string">          "query": &#123;</span><span class="hljs-string">            "terms": &#123;</span><span class="hljs-string">              "xxx": [</span><span class="hljs-string">                $&#123;xxx&#125;</span><span class="hljs-string">              ]</span><span class="hljs-string">            &#125;</span><span class="hljs-string">          &#125;,</span><span class="hljs-string">          "aggs": &#123;</span><span class="hljs-string">            "first": &#123;</span><span class="hljs-string">              "value_count": &#123;</span><span class="hljs-string">                "field": "first_id"</span><span class="hljs-string">              &#125;</span><span class="hljs-string">            &#125;,</span><span class="hljs-string">            "second": &#123;</span><span class="hljs-string">              "value_count": &#123;</span><span class="hljs-string">                "field": "second_id"</span><span class="hljs-string">              &#125;</span><span class="hljs-string">            &#125;</span><span class="hljs-string">          &#125;</span><span class="hljs-string">        &#125;</span><span class="hljs-string">        """</span>String endpoint = <span class="hljs-string">'code/data/_search'</span>Request request = <span class="hljs-keyword">new</span> Request(HttpPost.METHOD_NAME, endpoint);request.setJsonEntity(dsl)Response response = elasticsearchRestTemplate.getClient().getLowLevelClient().performRequest(request)<span class="hljs-keyword">def</span> respJson = <span class="hljs-keyword">new</span> JsonSlurper().parse(response.getEntity().getContent())CodeTerms codeTerms = <span class="hljs-keyword">new</span> CodeTerms().with &#123;    firstCount = respJson[<span class="hljs-string">'aggregations'</span>][<span class="hljs-string">'first'</span>][<span class="hljs-string">'value'</span>] <span class="hljs-keyword">as</span> Long    secondCount = respJson[<span class="hljs-string">'aggregations'</span>][<span class="hljs-string">'second'</span>][<span class="hljs-string">'value'</span>] <span class="hljs-keyword">as</span> Long    it&#125;</code></pre></div><h3 id="使用RestHighLevelClient直接进行简单查询"><a href="#使用RestHighLevelClient直接进行简单查询" class="headerlink" title="使用RestHighLevelClient直接进行简单查询"></a>使用RestHighLevelClient直接进行简单查询</h3><p>简单查询，使用QueryBuilders组装SearchRequest</p><div class="hljs"><pre><code class="hljs groovy">String dsl = <span class="hljs-string">'''</span><span class="hljs-string">&#123;</span><span class="hljs-string">  "range": &#123;</span><span class="hljs-string">      "second_upload_time": &#123;</span><span class="hljs-string">      "gte": "2020-06-01 00:00:00",</span><span class="hljs-string">      "lte": "2020-06-02 00:00:00"</span><span class="hljs-string">      &#125;</span><span class="hljs-string">  &#125;</span><span class="hljs-string">&#125;</span><span class="hljs-string">'''</span>SearchSourceBuilder ssb = <span class="hljs-keyword">new</span> SearchSourceBuilder().query(QueryBuilders.wrapperQuery(dsl))SearchRequest searchRequest = <span class="hljs-keyword">new</span> SearchRequest(<span class="hljs-string">"code"</span>)        .types(<span class="hljs-string">"data"</span>)        .source(ssb)<span class="hljs-keyword">def</span> response = elasticsearchRestTemplate.getClient().search(searchRequest, RequestOptions.DEFAULT)</code></pre></div><h3 id="使用ElasticsearchRestTemplate直接进行简单查询"><a href="#使用ElasticsearchRestTemplate直接进行简单查询" class="headerlink" title="使用ElasticsearchRestTemplate直接进行简单查询"></a>使用ElasticsearchRestTemplate直接进行简单查询</h3><p>使用StringQuery基于DSL定义的JSON的查询，底层使用<code>SearchRequest</code></p><div class="hljs"><pre><code class="hljs Java">StringQuery stringQuery = <span class="hljs-keyword">new</span> StringQuery(<span class="hljs-string">"&#123;\"query\":&#123;\"bool\":&#123;\"must\":[&#123;\"term\":&#123;\"id\":\"1\"&#125;&#125;],\"must_not\":[],</span><span class="hljs-string">\"should\":[]&#125;&#125;,\"from\":0,\"size\":10,\"sort\":[],\"aggs\":&#123;&#125;&#125;"</span>);    searchQuery.addIndices(<span class="hljs-string">"test"</span>);    searchQuery.addTypes(<span class="hljs-string">"test"</span>);List&lt;TestObject&gt; list = template.queryForList(stringQuery, TestObject<span class="hljs-class">.<span class="hljs-keyword">class</span>)</span>;</code></pre></div><h3 id="参考文档"><a href="#参考文档" class="headerlink" title="参考文档"></a>参考文档</h3><p><a href="https://my.oschina.net/xiaopei/blog/4302389" target="_blank" rel="noopener">Elasticsearch如何查询使用JSON字符串的DSL</a></p><p><a href="https://blog.csdn.net/Qensq/article/details/103081923?utm_medium=distribute.pc_relevant_t0.none-task-blog-BlogCommendFromMachineLearnPai2-1.add_param_isCf&depth_1-utm_source=distribute.pc_relevant_t0.none-task-blog-BlogCommendFromMachineLearnPai2-1.add_param_isCf" target="_blank" rel="noopener">Elasticsearch 常用聚合(group by || sum || count)分组查询</a></p><p><a href="https://tuwaner.com/2019/01/17/%E5%9F%BA%E4%BA%8ESpring-Data-Elasticsearch%E4%B8%89%E7%A7%8D%E6%9F%A5%E8%AF%A2%E7%9A%84%E4%BD%BF%E7%94%A8%E7%A4%BA%E4%BE%8B/" target="_blank" rel="noopener">基于Spring-Data-Elasticsearch三种查询的使用示例</a></p><p><a href="https://blog.csdn.net/qq_38966984/article/details/91049878" target="_blank" rel="noopener">Spring-data-Elasticsearch聚合查询</a></p>]]></content>
    
    
    
    <tags>
      
      <tag>Elasticsearch</tag>
      
      <tag>Java</tag>
      
      <tag>Groovy</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>IDEA部署应用到远程Docker</title>
    <link href="/post/bee47fed.html"/>
    <url>/post/bee47fed.html</url>
    
    <content type="html"><![CDATA[<p>Docker 是一个开源的应用容器引擎，让开发者可以打包他们的应用以及依赖包到一个可移植的镜像中，使用Docker可保证每个人的环境保持一致，减少团队开发中因项目环境不一致导致的问题，本文介绍如何使用IDEA直接部署Docker。</p><a id="more"></a><h3 id="配置服务端Docker"><a href="#配置服务端Docker" class="headerlink" title="配置服务端Docker"></a>配置服务端Docker</h3><p>首先需要修改 Docker 配置文件让其开放 <code>Docker REST API</code></p><p>vim /etc/docker/daemon.json</p><div class="hljs"><pre><code class="hljs json">&#123;  <span class="hljs-attr">"hosts"</span>:[    <span class="hljs-string">"unix:///var/run/docker.sock"</span>,    <span class="hljs-string">"tcp://0.0.0.0:2375"</span>  ]&#125;</code></pre></div><div class="hljs"><pre><code class="hljs properties"><span class="hljs-comment"># 重新加载配置文件，并重启Docker</span><span class="hljs-attr">systemctl</span> <span class="hljs-string">daemon-reload</span><span class="hljs-attr">systemctl</span> <span class="hljs-string">restart docker</span></code></pre></div><h3 id="IDEA连接Docker"><a href="#IDEA连接Docker" class="headerlink" title="IDEA连接Docker"></a>IDEA连接Docker</h3><p>Docker配置完成后，我们配置IDEA</p><p><img src="https://img.yjll.art/img/20200927194234.png" srcset="/img/loading.gif" alt="20200927194234"></p><p>配置正确会有Connection successful提示</p><h3 id="Dockerfile文件"><a href="#Dockerfile文件" class="headerlink" title="Dockerfile文件"></a>Dockerfile文件</h3><p>熟悉Docker的对Dockerfile应该不会陌生，我这里写一个简单的</p><div class="hljs"><pre><code class="hljs dockerfile"><span class="hljs-keyword">FROM</span> openjdk:<span class="hljs-number">8</span>u102-jdk<span class="hljs-keyword">ADD</span><span class="bash"> target/*.jar app.jar</span><span class="hljs-keyword">RUN</span><span class="bash"> ln -sf /usr/share/zoneinfo/Asia/Shanghai /etc/localtime</span><span class="hljs-keyword">ENTRYPOINT</span><span class="bash"> [<span class="hljs-string">"java"</span>,<span class="hljs-string">"-Djava.security.egd=file:/dev/./urandom"</span>,<span class="hljs-string">"-jar"</span>,<span class="hljs-string">"/app.jar"</span>]</span></code></pre></div><p>我用的是maven进行打包，打包后的文件放在target目录中，<code>ADD target/*.jar app.jar</code>是将target中的jar文件添加近容器并重命名为<code>app.jar</code>。</p><p>docker中的时区与我们不同，手动设置东八区。</p><p>注意openjdk的版本，我最开始使用的是<code>alpine</code>版本，占用体积较小。但实际运行时，生成验证码接口报错，同样的代码我在CentOS中能运行，Docker中报错。最后换了JDK的版本解决了，猜测是<code>alpine</code>可能阉精简一部分功能。</p><h3 id="IDEA部署Docker"><a href="#IDEA部署Docker" class="headerlink" title="IDEA部署Docker"></a>IDEA部署Docker</h3><p>在Run/Debug Configurations中添加Docker配置，支持<code>Docker Image</code>,<code>Docker-compose</code>,<code>Dockerfile</code>，我这里使用<code>Dockerfile</code>进行部署</p><p><img src="https://img.yjll.art/img/20200928090658.png" srcset="/img/loading.gif" alt="20200928090658"></p><p>这是一个Spring Cloud项目，启动容器后发现向调度中心注册的地址不是本机IP，而是Docker中的桥接网卡的地址，运行参数我添加了<code>--network=host</code>，容器直接使用宿主机的网络，不进行桥接。</p><p>以上直接配置好后，启动项目</p><div class="hljs"><pre><code class="hljs ada">Deploying <span class="hljs-symbol">'admin</span>-service Dockerfile: Dockerfile'...Building image...Preparing build context archive...[==================================================&gt;]<span class="hljs-number">1540</span>/<span class="hljs-number">1540</span> filesDoneSending build context to Docker daemon...[==================================================&gt;] <span class="hljs-number">107.5</span>MBDoneStep <span class="hljs-number">1</span>/<span class="hljs-number">4</span> : <span class="hljs-type">FROM</span> openjdk:<span class="hljs-number">8</span>u102-jdk <span class="hljs-comment">---&gt; ca5dd051db43</span>Step <span class="hljs-number">2</span>/<span class="hljs-number">4</span> : <span class="hljs-type">ADD</span> target/*.jar app.jar <span class="hljs-comment">---&gt; 8d0c14567ec9</span>Step <span class="hljs-number">3</span>/<span class="hljs-number">4</span> : <span class="hljs-type">RUN</span> ln -sf /usr/share/zoneinfo/Asia/Shanghai /etc/localtime <span class="hljs-comment">---&gt; Running in a289f48a6aa9</span>Removing intermediate container a289f48a6aa9 <span class="hljs-comment">---&gt; 12cbefb7337e</span>Step <span class="hljs-number">4</span>/<span class="hljs-number">4</span> : <span class="hljs-type">ENTRYPOINT</span> [<span class="hljs-string">"java"</span>,<span class="hljs-string">"-Djava.security.egd=file:/dev/./urandom"</span>,<span class="hljs-string">"-jar"</span>,<span class="hljs-string">"/app.jar"</span>] <span class="hljs-comment">---&gt; Running in 208933b6329b</span>Removing intermediate container <span class="hljs-number">208933</span>b6329b <span class="hljs-comment">---&gt; 7e398428e4c6</span>Successfully built <span class="hljs-number">7e398428</span>e4c6Successfully <span class="hljs-keyword">tagged</span> admin-service:latestCreating container...Container Id: b45137c747b9fd52713b191b92bf826fba7b0bdefd2996ddcbbfde98dbe872bcContainer name: <span class="hljs-symbol">'admin</span>-service'Attaching to container <span class="hljs-symbol">'admin</span>-service'...Starting container <span class="hljs-symbol">'admin</span>-service'<span class="hljs-symbol">'admin</span>-service Dockerfile: Dockerfile' has been deployed successfully.</code></pre></div><p>好了，部署完成了，可以访问项目试一试了。</p>]]></content>
    
    
    
    <tags>
      
      <tag>Docker</tag>
      
      <tag>IDEA</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>使用Logstash同步Mysql数据到Elasticsearch</title>
    <link href="/post/863f4886.html"/>
    <url>/post/863f4886.html</url>
    
    <content type="html"><![CDATA[<p><img src="https://img.yjll.art/img/Logstash.png" srcset="/img/loading.gif" alt="Logstash"><br>Logstash是一个开源数据收集引擎，具有近实时管道功能。Logstash可以动态地将来自不同数据源的数据统一起来，并将数据标准化到你所选择的目的地。</p><p>项目用到ES做统计，需要同步Mysql数据到ES中，增量数据使用阿里开源工具cannel,cannel是通过读取mysql的binlog实现同步，可对实时数据进行同步，无法同步历史数据，既有的数据我们使用Logstash进行同步，Logstash安装需要依赖的包比较多，我为了方便直接使用Docker进行操作。</p><a id="more"></a><h3 id="基本语法"><a href="#基本语法" class="headerlink" title="基本语法"></a>基本语法</h3><p>首先要编写同步数据的配置文件，从Mysql同步数据到ElasticSearch中，使用jdbc里连接数据库作为input，ouput为ES。</p><div class="hljs"><pre><code class="hljs puppet"><span class="hljs-keyword">input</span> &#123; stdin &#123; &#125;    <span class="hljs-keyword">jdbc</span> &#123;        <span class="hljs-comment">#注意mysql连接地址一定要用ip，不能使用localhost等</span>        <span class="hljs-attr">jdbc_connection_string</span> =&gt; <span class="hljs-string">"jdbc:mysql://192.168.1.200:3308/test"</span>        <span class="hljs-attr">jdbc_user</span> =&gt; <span class="hljs-string">"test"</span>        <span class="hljs-attr">jdbc_password</span> =&gt; <span class="hljs-string">"test"</span>        <span class="hljs-comment">#这个jar包的地址是容器内的地址</span>        <span class="hljs-attr">jdbc_driver_library</span> =&gt; <span class="hljs-string">"/usr/share/logstash/pipeline/mysql-connector-java-8.0.11.jar"</span>        <span class="hljs-attr">jdbc_driver_class</span> =&gt; <span class="hljs-string">"com.mysql.jdbc.Driver"</span>        <span class="hljs-attr">jdbc_paging_enabled</span> =&gt; <span class="hljs-string">"true"</span>        <span class="hljs-attr">jdbc_page_size</span> =&gt; <span class="hljs-string">"5000"</span>        <span class="hljs-attr">statement</span> =&gt; <span class="hljs-string">"SELECT * FROM base_areas where id &gt; :sql_last_value"</span>        <span class="hljs-attr">tracking_column</span> =&gt; <span class="hljs-built_in">id</span>        <span class="hljs-attr">use_column_value</span> =&gt; <span class="hljs-keyword">true</span>        <span class="hljs-attr">last_run_metadata_path</span> =&gt; <span class="hljs-string">"/usr/share/logstash/pipeline/area"</span>        <span class="hljs-attr">schedule</span> =&gt; <span class="hljs-string">"* * * * *"</span>    &#125; &#125;   <span class="hljs-keyword">output</span> &#123;     stdout &#123;        <span class="hljs-attr">codec</span> =&gt; json_lines    &#125;    <span class="hljs-keyword">elasticsearch</span> &#123;        <span class="hljs-attr">hosts</span> =&gt; <span class="hljs-string">"192.168.1.200:9200"</span>        <span class="hljs-attr">index</span> =&gt; <span class="hljs-string">"area"</span>        <span class="hljs-attr">document_type</span> =&gt; <span class="hljs-string">"_doc"</span>        <span class="hljs-attr">document_id</span> =&gt; <span class="hljs-string">"%&#123;id&#125;"</span>    &#125;&#125;</code></pre></div><p>重点在于 <code>use_column_value</code> 和 <code>tracking_column</code> 这两个参数，当use_column_value为true时，可以用 :sql_last_value 这个变量来获取tracking_column对应的字段的最新值，默认即第一次启动时为 0 。我的示例中tracking_column对应id，即Logstash都会记录每次查询结果id的最大值，供下一次查询使用。</p><p>Logstash将tracking_column的最新值记录到<code>last_run_metadata_path</code>配置的文件，启动时读取该文件</p><div class="hljs"><pre><code class="hljs awk">docker run -it -v  <span class="hljs-variable">$PWD</span><span class="hljs-regexp">/pipeline/</span>:<span class="hljs-regexp">/usr/</span>share<span class="hljs-regexp">/logstash/</span>pipeline -v <span class="hljs-variable">$PWD</span><span class="hljs-regexp">/logstash.yml:/u</span>sr<span class="hljs-regexp">/share/</span>logstash<span class="hljs-regexp">/config/</span>logstash.yml -v <span class="hljs-variable">$PWD</span><span class="hljs-regexp">/pipelines.yml:/u</span>sr<span class="hljs-regexp">/share/</span>logstash<span class="hljs-regexp">/config/</span>pipelines.yml   logstash:<span class="hljs-number">6.4</span>.<span class="hljs-number">3</span>  bash</code></pre></div><p>先安装jdbc和es的插件</p><div class="hljs"><pre><code class="hljs sql">bash-4.2$ ~/bin/logstash-plugin <span class="hljs-keyword">install</span> logstash-<span class="hljs-keyword">input</span>-jdbcValidating logstash-<span class="hljs-keyword">input</span>-jdbcInstalling logstash-<span class="hljs-keyword">input</span>-jdbcInstallation successfulbash<span class="hljs-number">-4.2</span>$ ~/<span class="hljs-keyword">bin</span>/logstash-<span class="hljs-keyword">plugin</span> <span class="hljs-keyword">install</span> logstash-<span class="hljs-keyword">output</span>-elasticsearchValidating logstash-<span class="hljs-keyword">output</span>-elasticsearchInstalling logstash-<span class="hljs-keyword">output</span>-elasticsearchInstallation successful</code></pre></div><p>由于是在容器内部连接服务，所以ES的连接不要使用localhost和127.0.0.1，更改为实际IP地址</p><div class="hljs"><pre><code class="hljs stylus">bash-<span class="hljs-number">4.2</span>$ cat ~/config/logstash<span class="hljs-selector-class">.yml</span> http<span class="hljs-selector-class">.host</span>: <span class="hljs-string">"0.0.0.0"</span>xpack<span class="hljs-selector-class">.monitoring</span><span class="hljs-selector-class">.elasticsearch</span><span class="hljs-selector-class">.url</span>: http:<span class="hljs-comment">//192.168.1.200:9200</span></code></pre></div><h3 id="执行脚本"><a href="#执行脚本" class="headerlink" title="执行脚本"></a>执行脚本</h3><p>运行同步脚本可手动执行也可以配置调度执行</p><h4 id="手动执行"><a href="#手动执行" class="headerlink" title="手动执行"></a>手动执行</h4><p><code>~/bin/logstash -f ***.config</code></p><h4 id="调度执行"><a href="#调度执行" class="headerlink" title="调度执行"></a>调度执行</h4><p>调度任务可读取配置文件<code>config/pipelines.yml</code>，默认有一个<code>main</code>，<code>/usr/share/logstash/pipeline</code>为pipeline的存放路径，该文件夹内的pipeline都会被执行。<br>可手动更改该文件，添加<code>id</code>和<code>config</code>即可。</p><div class="hljs"><pre><code class="hljs groovy">bash<span class="hljs-number">-4.2</span>$ cat <span class="hljs-regexp">~/config/</span>pipelines.yml - pipeline.<span class="hljs-string">id:</span> main  path.<span class="hljs-string">config:</span> <span class="hljs-string">"/usr/share/logstash/pipeline"</span></code></pre></div><p>好，配置已完成，运行<code>/usr/local/bin/docker-entrypoint</code>启动调度任务</p><h3 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h3><p><code>logstash</code>是ES官方提供的工具，支持扩展多，配置灵活，但是只能同步增量数据，对于源数据删除和修改的情况，logstash无能为力，还有基于定时任务同步无法保证实时，对实时性要求高的场景要考虑其他方案。</p><h3 id="参考文档"><a href="#参考文档" class="headerlink" title="参考文档"></a>参考文档</h3><p><a href="https://www.elastic.co/guide/en/logstash/current/docker-config.html#_pipeline_configuration" target="_blank" rel="noopener">Configuring Logstash for Docker</a></p><p><a href="https://www.elastic.co/guide/en/logstash/current/plugins-inputs-jdbc.html" target="_blank" rel="noopener">Jdbc input plugin</a></p>]]></content>
    
    
    <categories>
      
      <category>中间件</category>
      
    </categories>
    
    
    <tags>
      
      <tag>Mysql</tag>
      
      <tag>Elasticsearch</tag>
      
      <tag>Logstash</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>Manjaro安装与配置</title>
    <link href="/post/d9d0da7d.html"/>
    <url>/post/d9d0da7d.html</url>
    
    <content type="html"><![CDATA[<p>闲来无事折腾了一下Manjaro，本文记录安装配置Manjaro的过程，一方面备忘，另一方面希望可以帮助到有需要的人</p><p>我版本选择<a href="https://manjaro.org/download/" target="_blank" rel="noopener">官网</a>上的KDE版本，用<a href="https://www.balena.io/etcher/" target="_blank" rel="noopener">balenaEtcher</a>将镜像写入U盘，Manjaro的安装步骤有图像界面，根据自己需求安装即可。</p><a id="more"></a><h3 id="配置国内源"><a href="#配置国内源" class="headerlink" title="配置国内源"></a>配置国内源</h3><div class="hljs"><pre><code class="hljs bash">sudo pacman-mirrors -i -c China -m rank<span class="hljs-comment"># 查看是否已经更改</span>cat /etc/pacman.d/mirrorlist</code></pre></div><p>手动更改可参考<a href="https://www.archlinux.org/mirrorlist/" target="_blank" rel="noopener">镜像地址</a></p><h3 id="添加-ArchLinuxCN-源"><a href="#添加-ArchLinuxCN-源" class="headerlink" title="添加 ArchLinuxCN 源"></a>添加 ArchLinuxCN 源</h3><p>ArchLinuxCN 是 Arch 中文组维护的一个软件合集，包含了中文用户常用的 WPS Office、搜狗拼音、Google Chrome 等软件。不过，Arch 本身和它的常见衍生版（如：Manjaro）默认都不包含这个源，因此我们需要手动来配置使用这个源。</p><p>参考<a href="https://github.com/archlinuxcn/mirrorlist-repo" target="_blank" rel="noopener">镜像地址</a></p><p>我使用的是清华大学的源</p><div class="hljs"><pre><code class="hljs bash">sudo vim etc/pacman.conf[archlinuxcn]SigLevel = Optional TrustedOnlyServer = http://mirrors.tuna.tsinghua.edu.cn/archlinuxcn//<span class="hljs-variable">$arch</span></code></pre></div><p>导入 GPG key 并更新系统<br>这是 archlinuxcn 的 key，安装后作为验证，就可以安装软件了。</p><div class="hljs"><pre><code class="hljs bash"><span class="hljs-comment"># 更新数据源</span>sudo pacman -Syy<span class="hljs-comment"># 安装导入 GPG key</span>sudo pacman -S archlinuxcn-keyring<span class="hljs-comment"># 更新系统</span>sudo pacman -Syu<span class="hljs-comment"># 安装chrome</span>sudo pacman -S google-chrome</code></pre></div><h3 id="AUR"><a href="#AUR" class="headerlink" title="AUR"></a>AUR</h3><p>Arch用户软件仓库（Arch User Repository，AUR）是为用户而建、由用户主导的Arch软件仓库。AUR中的软件包以软件包生成脚本（PKGBUILD）的形式提供，用户自己通过makepkg生成包，再由pacman安装。创建AUR的初衷是方便用户维护和分享新软件包，并由官方定期从中挑选软件包进入community仓库。</p><p>许多官方仓库软件包都来自AUR。通过AUR，大家相互分享新的软件包生成脚本（PKGBUILD和其他相关文件）。用户还可以为软件包投票。如果一个软件包投票足够多、没有协议问题、打包质量好，那么<br>它就很有希望被收录进官方[community]仓库（以后就可以直接通过pacman 或 abs 安装了）。</p><p>Yay 是用 Go 编写的 Arch Linux AUR 包管理工具。具体可以查看 <a href="https://wiki.archlinux.org/index.php/AUR_helpers" target="_blank" rel="noopener">AUR_helpers</a></p><p>安装 yay：</p><div class="hljs"><pre><code class="hljs bash">sudo pacman -S yay</code></pre></div><p>配置 yay 的 aur 源为<a href="https://mirror.tuna.tsinghua.edu.cn/help/AUR/" target="_blank" rel="noopener">清华源 AUR 镜像</a>：</p><div class="hljs"><pre><code class="hljs bash">yay --aururl <span class="hljs-string">"https://aur.tuna.tsinghua.edu.cn"</span> --save</code></pre></div><p>修改的配置文件位于 <code>~/.config/yay/config.json</code> ，还可通过以下命令查看修改过的配置:</p><div class="hljs"><pre><code class="hljs bash">yay -P -g</code></pre></div><p>yay的兼容Pacman命令</p><p>yay 安装命令不需要加 <code>sudo</code>。</p><h3 id="参考文档"><a href="#参考文档" class="headerlink" title="参考文档"></a>参考文档</h3><p><a href="https://wiki.archlinux.org/index.php/Arch_User_Repository_(%E7%AE%80%E4%BD%93%E4%B8%AD%E6%96%87)" target="_blank" rel="noopener">AUR wiki</a></p><p><a href="https://zh.wikipedia.org/wiki/Manjaro_Linux" target="_blank" rel="noopener">Manjaro wiki</a></p>]]></content>
    
    
    <categories>
      
      <category>Linux</category>
      
    </categories>
    
    
    <tags>
      
      <tag>Manjaro</tag>
      
      <tag>Linux</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>Java配合Groovy实现动态公式计算</title>
    <link href="/post/7b75058.html"/>
    <url>/post/7b75058.html</url>
    
    <content type="html"><![CDATA[<p>最近项目用到了动态公式计算，也就是页面配置一个公式，服务端进行计算。我们后端使用Java，Java处理这部分公式可以使用表达式引擎Aviator，Jexl。也可以以使用JVM上的脚本语言像Jython，Groovy。表达式引擎不是一门单独的语言，局限性比较高，由于我对Python比较熟悉，最开始准备使用Jython，查了查资料发现和Java配合不是很好，最终决定使用Groovy，Groovy兼容Java的语法，不熟悉的地方可以直接使用Java语法。</p><a id="more"></a><p>可以使用<code>GroovyShell</code>根据公式求值</p><div class="hljs"><pre><code class="hljs java"><span class="hljs-comment">// 绑定公式中的变量和对应的值</span>Binding binding = <span class="hljs-keyword">new</span> Binding()binding.setVariable(<span class="hljs-string">"a"</span>,<span class="hljs-number">1</span>)binding.setVariable(<span class="hljs-string">"b"</span>,<span class="hljs-number">2</span>)GroovyShell shell = <span class="hljs-keyword">new</span> GroovyShell(binding);<span class="hljs-comment">// 进行计算 返回值是3</span>def result = shell.evaluate(<span class="hljs-string">'a + b'</span>)</code></pre></div><p>接口使用Java定义</p><div class="hljs"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-class"><span class="hljs-keyword">interface</span> <span class="hljs-title">DynamicFormulaService</span> </span>&#123;    <span class="hljs-comment">/**</span><span class="hljs-comment">     * 根据公式和source中的值计算</span><span class="hljs-comment">     * <span class="hljs-doctag">@param</span> source 源数据</span><span class="hljs-comment">     * <span class="hljs-doctag">@param</span> formulaMap 公式</span><span class="hljs-comment">     * <span class="hljs-doctag">@return</span></span><span class="hljs-comment">     */</span>    List&lt;Map&lt;String, Object&gt;&gt; compute(List&lt;Map&lt;String, Object&gt;&gt; source , Map&lt;String, String&gt; formulaMap);&#125;</code></pre></div><p><code>source</code>为要被统计的源数据列表，<code>key</code>为公式中变量，如</p><p><code>{ price : 1.5 , quantity : 400 , total : null }</code></p><p><code>formulaMap</code>中的key为结果，value为公式，如</p><p><code>{ total : price * quantity }</code></p><p>返回的结果为<code>{ price : 1.5 , quantity : 400 , total : 600 }</code></p><p>在计算数值之前，会先遍历<code>formulaMap</code>，将<code>value</code>中有计算公式的数据进行递归拆分，完成嵌套公式解析</p><p>如<code>{a : b * c , b : d * e}</code>解析的结果为<code>{a : ( d * e ) * c , b : d * e}</code></p><p>具体的实现使用Groovy，我这里在resources下新建了一个groovy文件夹存放我们的脚本</p><div class="hljs"><pre><code class="hljs groovy"><span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">DynamicFormulaServiceImpl</span> <span class="hljs-keyword">implements</span> <span class="hljs-title">DynamicFormulaService</span> &#123;</span>    <span class="hljs-meta">@Autowired</span>    <span class="hljs-keyword">private</span> IBaseTargetService baseTargetService;    <span class="hljs-comment">// 匹配加减乘除符号</span>    <span class="hljs-keyword">def</span> pattern = Pattern.compile(<span class="hljs-string">'[\\+\\-\\*\\/\\(\\)]'</span>)    <span class="hljs-comment">// 加减乘除符号</span>    <span class="hljs-keyword">def</span> formulaString = <span class="hljs-string">'''+-*/()'''</span>    <span class="hljs-meta">@Override</span>    List&lt;Map&lt;String, Object&gt;&gt; compute(List&lt;Map&lt;String, Object&gt;&gt; source, Map&lt;String, String&gt; formulaMap) &#123;        formulaMap.each &#123; k, v -&gt;            <span class="hljs-comment">// 递归格式化公式</span>            formulaMap.put(k,formatFormula(formulaMap, k))        &#125;        println formulaMap        <span class="hljs-keyword">def</span> result = <span class="hljs-keyword">this</span>.compute(source, formulaMap)        <span class="hljs-keyword">return</span> result    &#125;    <span class="hljs-keyword">def</span> formatFormula(Map&lt;String, String&gt; formulaMap, String key) &#123;        String formula = formulaMap.get(key)        <span class="hljs-comment">// 是否含有公式</span>        <span class="hljs-keyword">if</span> (!formula || formula.matches(pattern)) <span class="hljs-keyword">return</span> key        <span class="hljs-comment">// 将变量用空格分隔方便替换</span>        formula = <span class="hljs-string">" $&#123;formula&#125; "</span>        formulaString.chars.each &#123; c -&gt;            formula = formula.replace(c.toString(), <span class="hljs-string">" $&#123;c&#125; "</span>.toString())        &#125;        formula.splitEachLine(pattern, &#123; v -&gt;            v.each &#123;                k -&gt;                    k = k.trim()                    <span class="hljs-keyword">if</span> (StringUtils.isEmpty(k) || StringUtils.isNumeric(k)) <span class="hljs-keyword">return</span>                    <span class="hljs-comment">// 精确匹配替换</span>                    formula = formula.replaceFirst(<span class="hljs-string">" $&#123;k&#125; "</span>, formatFormula(formulaMap, k))            &#125;        &#125;)        <span class="hljs-keyword">return</span> <span class="hljs-string">"($&#123;formula&#125;)"</span>    &#125;    <span class="hljs-keyword">def</span> compute(List&lt;Map&lt;String, Object&gt;&gt; source, formulaMap) &#123;        source.each &#123; e -&gt;            Binding binding = <span class="hljs-keyword">new</span> Binding(e)            formulaMap.each &#123; k, v -&gt;                <span class="hljs-keyword">if</span> (e.containsKey(k)) &#123;                    GroovyShell shell = <span class="hljs-keyword">new</span> GroovyShell(binding);                    <span class="hljs-keyword">def</span> result = shell.evaluate(v)                    println <span class="hljs-string">"$&#123;k&#125; : $&#123;result&#125;"</span>                    e.put(k, result)                &#125;            &#125;        &#125;        <span class="hljs-keyword">return</span> source    &#125;&#125;</code></pre></div><p><code>DynamicFormulaServiceImpl</code>还不能直接使用，要将它作为一个bean交给Spring统一管理，Spring也提供了对Groovy的支持</p><p>appcontextContext.xml</p><div class="hljs"><pre><code class="hljs xml"><span class="hljs-meta">&lt;?xml version="1.0" encoding="UTF-8"?&gt;</span><span class="hljs-tag">&lt;<span class="hljs-name">beans</span> <span class="hljs-attr">xmlns</span>=<span class="hljs-string">"http://www.springframework.org/schema/beans"</span> <span class="hljs-attr">xmlns:xsi</span>=<span class="hljs-string">"http://www.w3.org/2001/XMLSchema-instance"</span></span><span class="hljs-tag">       <span class="hljs-attr">xmlns:lang</span>=<span class="hljs-string">"http://www.springframework.org/schema/lang"</span></span><span class="hljs-tag">       <span class="hljs-attr">xsi:schemaLocation</span>=<span class="hljs-string">"</span></span><span class="hljs-tag"><span class="hljs-string">        http://www.springframework.org/schema/beans https://www.springframework.org/schema/beans/spring-beans.xsd</span></span><span class="hljs-tag"><span class="hljs-string">        http://www.springframework.org/schema/lang https://www.springframework.org/schema/lang/spring-lang.xsd"</span>&gt;</span>    <span class="hljs-tag">&lt;<span class="hljs-name">lang:groovy</span> <span class="hljs-attr">id</span>=<span class="hljs-string">"dynamicFormulaService"</span> <span class="hljs-attr">script-source</span>=<span class="hljs-string">"classpath:groovy/DynamicFormulaServiceImpl.groovy"</span>&gt;</span>    <span class="hljs-tag">&lt;/<span class="hljs-name">lang:groovy</span>&gt;</span><span class="hljs-tag">&lt;/<span class="hljs-name">beans</span>&gt;</span></code></pre></div><p>Spring Framework 4.0 也支持<code>beans{}</code>写法</p><p>applicationContext.groovy</p><div class="hljs"><pre><code class="hljs groovy"><span class="hljs-keyword">import</span> groovy.DynamicFormulaServiceImplbeans &#123;    dynamicFormulaService(DynamicFormulaServiceImpl)&#125;</code></pre></div><p>引用以上配置</p><div class="hljs"><pre><code class="hljs java"><span class="hljs-meta">@SpringBootApplication</span><span class="hljs-meta">@ImportResource</span>(<span class="hljs-string">"classpath:applicationContext.groovy"</span>)<span class="hljs-keyword">public</span> <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">Application</span> </span>&#123;    <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">void</span> <span class="hljs-title">main</span><span class="hljs-params">(String[] args)</span> </span>&#123;        SpringApplication.run(Application<span class="hljs-class">.<span class="hljs-keyword">class</span>, <span class="hljs-title">args</span>)</span>;    &#125;&#125;</code></pre></div><p>好了，现在我们的动态公式功能就可以使用了，由于Groovy我也是第一次使用，很多东西都是现用现查，用起来还不太灵活。之后我会深入学习一下，把我感觉有趣的东西写出来。</p><h3 id="参考文档"><a href="#参考文档" class="headerlink" title="参考文档"></a>参考文档</h3><p><a href="https://docs.spring.io/spring-framework/docs/current/spring-framework-reference/languages.html#groovy" target="_blank" rel="noopener">Spring Apache Groovy</a></p><p><a href="https://docs.spring.io/spring-boot/docs/current/reference/html/spring-boot-cli.html#cli-groovy" target="_blank" rel="noopener">Developing Applications with the Groovy Beans DSL</a></p><p><a href="https://docs.spring.io/spring-framework/docs/current/spring-framework-reference/core.html#groovy-bean-definition-dsl" target="_blank" rel="noopener">The Groovy Bean Definition DSL</a></p>]]></content>
    
    
    <categories>
      
      <category>Java</category>
      
    </categories>
    
    
    <tags>
      
      <tag>Groovy</tag>
      
      <tag>Spring</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>Docker复现RUN命令</title>
    <link href="/post/89a34fac.html"/>
    <url>/post/89a34fac.html</url>
    
    <content type="html"><![CDATA[<p>如果有这样以一个场景。我使用docker run启动了了一个redis容器，而docker run的参数有很多，由于时间太久，我已经忘记了当初的启动参数，也并没有使用docker-compose这样的编排工具。现在我如何找回此前的启动命令？</p><a id="more"></a><p>我可以使用<code>inspect</code>命令查看容器的元数据，根据元数据反推出运行的参数。</p><div class="hljs"><pre><code class="hljs sh">docker inspect 1cc825a43022[    &#123;        <span class="hljs-string">"Id"</span>: <span class="hljs-string">"1cc825a430226187c957b18c3006b4e3346cf143f43513ee71647011bbd98cfe"</span>,        <span class="hljs-string">"Created"</span>: <span class="hljs-string">"2019-10-12T05:54:40.635365198Z"</span>,        <span class="hljs-string">"Path"</span>: <span class="hljs-string">"docker-entrypoint.sh"</span>,        <span class="hljs-string">"Args"</span>: [            <span class="hljs-string">"redis-server"</span>,            <span class="hljs-string">"--appendonly"</span>,            <span class="hljs-string">"yes"</span>        ],        <span class="hljs-string">"State"</span>: &#123;            <span class="hljs-string">"Status"</span>: <span class="hljs-string">"running"</span>,            <span class="hljs-string">"Running"</span>: <span class="hljs-literal">true</span>,            <span class="hljs-string">"Paused"</span>: <span class="hljs-literal">false</span>,            <span class="hljs-string">"Restarting"</span>: <span class="hljs-literal">false</span>,            <span class="hljs-string">"OOMKilled"</span>: <span class="hljs-literal">false</span>,            ...</code></pre></div><p>但实际使用不是很方便，因为inspect返回的数据实在太多，找到数据后还要手工的反推出启动时的命令，费时又容易出错。这个时候我们可以借助<code>runlike</code>这个小工具。</p><p><code>runlike</code>使用python开发，我们直接使用pip安装</p><div class="hljs"><pre><code class="hljs bash">pip install runlike<span class="hljs-comment"># 安装成功后直接运行，使用`-p`参数可将返回值美观一点</span>runlike -p 1cc825a43022docker run \        --name=jolly_mirzakhani \        --hostname=1cc825a43022 \        --env=PATH=/usr/<span class="hljs-built_in">local</span>/sbin:/usr/<span class="hljs-built_in">local</span>/bin:/usr/sbin:/usr/bin:/sbin:/bin \        --env=GOSU_VERSION=1.11 \        --env=REDIS_VERSION=4.0.14 \        --env=REDIS_DOWNLOAD_URL=http://download.redis.io/releases/redis-4.0.14.tar.gz \        --env=REDIS_DOWNLOAD_SHA=1e1e18420a86cfb285933123b04a82e1ebda20bfb0a289472745a087587e93a7 \        --volume=/usr/<span class="hljs-built_in">local</span>/docker/data:/data \        --volume=/data \        -p 6379:6379 \        --restart=always \        --detach=<span class="hljs-literal">true</span> \        redis:4 \        redis-server --appendonly yes</code></pre></div>]]></content>
    
    
    <categories>
      
      <category>Linux</category>
      
    </categories>
    
    
    <tags>
      
      <tag>Docker</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>Windows下的包管理工具</title>
    <link href="/post/82ab8e0c.html"/>
    <url>/post/82ab8e0c.html</url>
    
    <content type="html"><![CDATA[<p>Centos安装软件可使用yum，Ubuntu上可使用apt，MacOS可使用brew。Windows平台也有开发者开发对应的安装程序——Scoop，Scoop类似MacOS平台的brew，没有brew强大，但常见开发环境都可以轻松搞定。</p><p>使用scoop安装常用软件，可直接敲命令</p><div class="hljs"><pre><code class="hljs powershell">scoop install git curl <span class="hljs-number">7</span>zip</code></pre></div><a id="more"></a><h3 id="安装Scoop"><a href="#安装Scoop" class="headerlink" title="安装Scoop"></a>安装Scoop</h3><p>设置安装目录，非必须，默认安装在用户目录</p><div class="hljs"><pre><code class="hljs elixir"><span class="hljs-variable">$env</span><span class="hljs-symbol">:SCOOP=<span class="hljs-string">'D:\ProgramFile\scoop'</span></span>[environment]::setEnvironmentVariable(<span class="hljs-string">'SCOOP'</span>,<span class="hljs-variable">$env</span><span class="hljs-symbol">:SCOOP</span>,<span class="hljs-string">'User'</span>)</code></pre></div><p>在 PowerShell 中输入下面内容，保证允许本地脚本的执行</p><div class="hljs"><pre><code class="hljs dsconfig"><span class="hljs-built_in">set-executionpolicy</span> <span class="hljs-string">remotesigned </span>-<span class="hljs-string">scope </span><span class="hljs-string">currentuser</span></code></pre></div><p>安装scoop</p><div class="hljs"><pre><code class="hljs ceylon">iex (<span class="hljs-keyword">new</span>-<span class="hljs-keyword">object</span> net.webclient).downloadstring(<span class="hljs-string">'https://get.scoop.sh'</span>)</code></pre></div><p>如报以下异常，表示在dns服务器中找不到源网站，我们可以手动更改HOSTS文件</p><div class="hljs"><pre><code class="hljs angelscript">“<span class="hljs-number">1</span>”个参数调用“DownloadString”时发生异常:“未能解析此远程名称:raw.githubusercontent.com。</code></pre></div><p>编辑<code>C:\Windows\System32\drivers\etc\hosts</code>文件</p><p>添加<code>199.232.68.133 raw.githubusercontent.com</code>即可。</p><p>Scoop 可以利用<a href="https://github.com/aria2/aria2" target="_blank" rel="noopener"><code>aria2</code></a> 进行多线程下载。</p><div class="hljs"><pre><code class="hljs powershell">scoop install aria2</code></pre></div><p>可以使用<code>scoop config</code>对aria2进行配置</p><ul><li>aria2-enabled (default: true)</li><li><a href="https://aria2.github.io/manual/en/html/aria2c.html#cmdoption-retry-wait" target="_blank" rel="noopener">aria2-retry-wait</a> (default: 2)</li><li><a href="https://aria2.github.io/manual/en/html/aria2c.html#cmdoption-s" target="_blank" rel="noopener">aria2-split</a> (default: 5)</li><li><a href="https://aria2.github.io/manual/en/html/aria2c.html#cmdoption-x" target="_blank" rel="noopener">aria2-max-connection-per-server</a> (default: 5)</li><li><a href="https://aria2.github.io/manual/en/html/aria2c.html#cmdoption-k" target="_blank" rel="noopener">aria2-min-split-size</a> (default: 5M)</li></ul><p>代理设置 </p><div class="hljs"><pre><code class="hljs powershell"><span class="hljs-comment"># 设置代理</span>scoop config proxy localhost:<span class="hljs-number">1081</span><span class="hljs-comment"># 删除代理</span>scoop config rm proxy</code></pre></div><p>添加<code>buckets</code><br>buckets类似Linux中的源，默认包含<code>main</code>,可手动添加其他bucket</p><div class="hljs"><pre><code class="hljs powershell">scoop bucket add extrasscoop bucket add versionsscoop bucket add dorado https://github.com/h404bi/dorado</code></pre></div><p>bucket收录列表</p><p><a href="https://rasa.github.io/scoop-directory/by-score.html" target="_blank" rel="noopener">https://rasa.github.io/scoop-directory/by-score.html</a></p>]]></content>
    
    
    
    <tags>
      
      <tag>工具</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>当我们谈论NIO时我们在谈论什么</title>
    <link href="/post/9a133fa7.html"/>
    <url>/post/9a133fa7.html</url>
    
    <content type="html"><![CDATA[<p><img src="https://img.yjll.art/img/NIO_HEAD.png" srcset="/img/loading.gif" alt="NIO_HEAD"></p><p>NIO（Non-blocking I/O，在Java领域，也称为New I/O），是一种同步非阻塞的I/O模型，也是I/O多路复用的基础，已经被越来越多地应用到大型应用服务器，成为解决高并发与大量连接、I/O处理问题的有效方式。</p><a id="more"></a><h3 id="用户空间与内核空间"><a href="#用户空间与内核空间" class="headerlink" title="用户空间与内核空间"></a>用户空间与内核空间</h3><p>现在操作系统都是采用虚拟存储器，那么对32位操作系统而言，它的寻址空间（虚拟存储空间）为4G（2的32次方）。操作系统的核心是内核，独立于普通的应用程序，可以访问受保护的内存空间，也有访问底层硬件设备的所有权限。为了保证用户进程不能直接操作内核（kernel），保证内核的安全，操作系统将虚拟空间划分为两部分，一部分为内核空间，一部分为用户空间。针对linux操作系统而言，将最高的1G字节（从虚拟地址0xC0000000到0xFFFFFFFF），供内核使用，称为内核空间，而将较低的3G字节（从虚拟地址0x00000000到0xBFFFFFFF），供各个进程使用，称为用户空间。</p><h3 id="缓存-IO"><a href="#缓存-IO" class="headerlink" title="缓存 IO"></a>缓存 IO</h3><p>缓存 IO 又被称作标准 IO，大多数文件系统的默认 IO 操作都是缓存 IO。在 Linux 的缓存 IO 机制中，操作系统会将 IO 的数据缓存在文件系统的页缓存（ page cache ）中，也就是说，数据会先被拷贝到操作系统内核的缓冲区中，然后才会从操作系统内核的缓冲区拷贝到应用程序的地址空间。</p><h3 id="I-O-模型"><a href="#I-O-模型" class="headerlink" title="I/O 模型"></a>I/O 模型</h3><h4 id="阻塞"><a href="#阻塞" class="headerlink" title="阻塞"></a>阻塞</h4><p><img src="https://img.yjll.art/img/BIO_model.png" srcset="/img/loading.gif" alt="BIO_model"></p><p>每个连接需要独立的进程/线程单独处理</p><h4 id="轮询"><a href="#轮询" class="headerlink" title="轮询"></a>轮询</h4><p><img src="https://img.yjll.art/img/loop_model.png" srcset="/img/loading.gif" alt="loop_model"></p><p>轮询将会不断地询问内核，这将占用大量的 CPU 时间</p><h4 id="多路复用"><a href="#多路复用" class="headerlink" title="多路复用"></a>多路复用</h4><p><img src="https://img.yjll.art/img/NIO_model.png" srcset="/img/loading.gif" alt="NIO_model"></p><p>当连接数较少时效率相比多线程+阻塞 I/O 模型效率较低，可能延迟更大，因为单个连接处理需要 2 次系统调用，占用时间会有增加。</p><h3 id="应用层实现"><a href="#应用层实现" class="headerlink" title="应用层实现"></a>应用层实现</h3><h4 id="BIO"><a href="#BIO" class="headerlink" title="BIO"></a>BIO</h4><div class="hljs"><pre><code class="hljs java"> ExecutorService executor = Excutors.newFixedThreadPollExecutor(<span class="hljs-number">100</span>);<span class="hljs-comment">//线程池</span> ServerSocket serverSocket = <span class="hljs-keyword">new</span> ServerSocket(); serverSocket.bind(<span class="hljs-number">8088</span>); <span class="hljs-keyword">while</span>(<span class="hljs-keyword">true</span>)&#123;<span class="hljs-comment">//主线程死循环等待新连接到来</span> Socket socket = serverSocket.accept(); executor.submit(<span class="hljs-keyword">new</span> ConnectIOnHandler(socket));<span class="hljs-comment">//为新的连接创建新的线程</span><span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">ConnectIOnHandler</span> <span class="hljs-keyword">extends</span> <span class="hljs-title">Thread</span></span>&#123;    <span class="hljs-keyword">private</span> Socket socket;    <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-title">ConnectIOnHandler</span><span class="hljs-params">(Socket socket)</span></span>&#123;       <span class="hljs-keyword">this</span>.socket = socket;    &#125;    <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">run</span><span class="hljs-params">()</span></span>&#123;        String someThing = socket.read()....<span class="hljs-comment">//读取数据</span>        <span class="hljs-keyword">if</span>(someThing!=<span class="hljs-keyword">null</span>)&#123;            ......<span class="hljs-comment">//处理数据</span>            socket.write()....<span class="hljs-comment">//写数据</span>        &#125;        ...    &#125;&#125;</code></pre></div><h4 id="NIO"><a href="#NIO" class="headerlink" title="NIO"></a>NIO</h4><div class="hljs"><pre><code class="hljs java">Selector selector = Selector.open();ServerSocketChannel serverSocketChannel = ServerSocketChannel.open();serverSocketChannel.configureBlocking(<span class="hljs-keyword">false</span>);SelectionKey key = serverSocketChannel.register(selector, SelectionKey.OP_ACCEPT);</code></pre></div><p><code>selector.select()</code>将会阻塞线程，直到有连接,并根据订阅的事件进行分发</p><div class="hljs"><pre><code class="hljs java"><span class="hljs-keyword">while</span>(<span class="hljs-keyword">true</span>) &#123;  <span class="hljs-keyword">int</span> readyChannels = selector.select();  <span class="hljs-keyword">if</span>(readyChannels == <span class="hljs-number">0</span>) <span class="hljs-keyword">continue</span>;  Set&lt;SelectionKey&gt; selectedKeys = selector.selectedKeys();  Iterator&lt;SelectionKey&gt; keyIterator = selectedKeys.iterator();  <span class="hljs-keyword">while</span>(keyIterator.hasNext()) &#123;    SelectionKey key = keyIterator.next();    <span class="hljs-keyword">if</span>(key.isAcceptable()) &#123;        <span class="hljs-comment">// a connection was accepted by a ServerSocketChannel.</span>    &#125; <span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span> (key.isConnectable()) &#123;        <span class="hljs-comment">// a connection was established with a remote server.</span>    &#125; <span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span> (key.isReadable()) &#123;        <span class="hljs-comment">// a channel is ready for reading</span>    &#125; <span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span> (key.isWritable()) &#123;        <span class="hljs-comment">// a channel is ready for writing</span>    &#125;    keyIterator.remove();  &#125;&#125;</code></pre></div>]]></content>
    
    
    <categories>
      
      <category>Java</category>
      
    </categories>
    
    
    <tags>
      
      <tag>NIO</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>基于Java NIO实现SOCKS代理协议</title>
    <link href="/post/e99ab4dc.html"/>
    <url>/post/e99ab4dc.html</url>
    
    <content type="html"><![CDATA[<p>SOCKS是一种网络传输协议，主要用于客户端与外网服务器之间通讯的中间传递。<br>当防火墙后的客户端要访问外部的服务器时，就跟SOCKS代理服务器连接。这个代理服务器控制客户端访问外网的资格，允许的话，就将客户端的请求发往外部的服务器。</p><a id="more"></a><h3 id="SOCKS协议解析"><a href="#SOCKS协议解析" class="headerlink" title="SOCKS协议解析"></a>SOCKS协议解析</h3><p>SOCKS代理需要以下几个步骤</p><p>1 握手，客户端与代理协商SOCKS5版本及账号密码</p><p>2 请求建立连接，客户端将发送要请求的远程地址发送给代理</p><p>3 发送数据包给代理服务，代理进行转发后，将目标服务的数据返回</p><p><img src="https://img.yjll.art/img/sock5.png" srcset="/img/loading.gif" alt="sock5"></p><p>根据OSI模型，SOCKS是会话层的协议，位于表示层与传输层之间。</p><h3 id="OSI网络模型"><a href="#OSI网络模型" class="headerlink" title="OSI网络模型"></a>OSI网络模型</h3><p><img src="https://img.yjll.art/img/OSI_model.jpg" srcset="/img/loading.gif" alt="OSI_model"></p><h3 id="Shadowsocks工作原理"><a href="#Shadowsocks工作原理" class="headerlink" title="Shadowsocks工作原理"></a>Shadowsocks工作原理</h3><p>Shadowsocks是基于Python开发的科学上网软件。Shadowsocks实现了SOCKS协议，SOCKS协议本身是没有加密功能的，为了防止GFW根据报文对数据进行拦截，所以要对数据进行加密。<br>Shadowsocks分为 <code>SS Local</code> 和 <code>SS Server</code>。</p><ul><li><code>SS Local</code>负责通过SOCKS协议与客户端交互，将客户端(浏览器)请求的数据加密发送给<code>SS Server</code>和将<code>SS Server</code>响应的数据解密返回给客户端。</li><li><code>SS Server</code>负责将<code>SS Local</code>请求的数据解密，发送给目标网站(例如google.com)，并将目标网站的响应进行加密返回。</li></ul><p><img src="https://img.yjll.art/img/Shadowsocks.jpg" srcset="/img/loading.gif" alt="Shadowsocks"></p><h3 id="NIO实现"><a href="#NIO实现" class="headerlink" title="NIO实现"></a>NIO实现</h3><p>首先我们要先创建一个选择器和一个ServerSocketChannel，ServerSocketChannel订阅ACCEPT事件注册到选择器上，<code>selector.select()</code>会阻塞直到有请求到来时。</p><div class="hljs"><pre><code class="hljs java"><span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">execute</span><span class="hljs-params">()</span> <span class="hljs-keyword">throws</span> IOException </span>&#123;    log.info(<span class="hljs-string">"starting server at port &#123;&#125;"</span>, ssConfig.getLocalPort());    Selector selector = Selector.open();    ServerSocketChannel serverSocketChannel = ServerSocketChannel.open();    serverSocketChannel.socket().bind(<span class="hljs-keyword">new</span> InetSocketAddress(ssConfig.getLocalPort()));    <span class="hljs-comment">// 非阻塞</span>    serverSocketChannel.configureBlocking(<span class="hljs-keyword">false</span>);    <span class="hljs-comment">// 订阅事件，当有请求时激活</span>    serverSocketChannel.register(selector, SelectionKey.OP_ACCEPT);    <span class="hljs-keyword">while</span> (<span class="hljs-keyword">true</span>) &#123;        <span class="hljs-comment">// 阻塞线程，直到有订阅的事件</span>        <span class="hljs-keyword">int</span> count = selector.select();        <span class="hljs-keyword">if</span> (count &lt;= <span class="hljs-number">0</span>) <span class="hljs-keyword">continue</span>;        Iterator&lt;SelectionKey&gt; iterator = selector.selectedKeys().iterator();        <span class="hljs-keyword">while</span> (iterator.hasNext()) &#123;            SelectionKey selectionKey = iterator.next();            iterator.remove();                        <span class="hljs-keyword">if</span> (!selectionKey.isValid()) <span class="hljs-keyword">continue</span>;            <span class="hljs-comment">// 一个新的连接进来时，</span>            <span class="hljs-keyword">if</span> (selectionKey.isAcceptable()) &#123;                ServerSocketChannel channel = (ServerSocketChannel) selectionKey.channel();                <span class="hljs-comment">// 新进来的SocketChannel                    </span>                SocketChannel socketChannel = channel.accept();                socketChannel.configureBlocking(<span class="hljs-keyword">false</span>);                <span class="hljs-comment">// 我们希望从中获取数据，我们注册读事件，当数据可读时会通知我们</span>                <span class="hljs-comment">// 并绑定一个处理器，当数据可读时，使用这个处理器进行数据处理</span>                socketChannel.register(selector, SelectionKey.OP_READ, <span class="hljs-keyword">new</span> ChannelHandler(ssConfig, selector, socketChannel));            &#125; <span class="hljs-keyword">else</span> &#123;                <span class="hljs-comment">// 交由对应的处理器来处理</span>                ChannelHandler channelHandler = (ChannelHandler) selectionKey.attachment();                <span class="hljs-keyword">if</span> (Objects.nonNull(channelHandler)) &#123;                    channelHandler.handle(selectionKey);                &#125;            &#125;        &#125;    &#125;&#125;</code></pre></div><p>处理器将根据事件状态进行分发</p><div class="hljs"><pre><code class="hljs java"><span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">handle</span><span class="hljs-params">(SelectionKey key)</span> </span>&#123;    SocketChannel channel = (SocketChannel) key.channel();    <span class="hljs-keyword">try</span> &#123;        <span class="hljs-comment">// 与浏览器交互的Channel</span>        <span class="hljs-keyword">if</span> (localSocketChannel == channel) &#123;            <span class="hljs-comment">// 当有request时，读取数据</span>            <span class="hljs-keyword">if</span> (key.isReadable()) &#123;                onLocalRead(channel);            &#125;            <span class="hljs-comment">// 与远程服务交互的Channel</span>        &#125; <span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span> (remoteSocketChannel == channel) &#123;            <span class="hljs-comment">// 远程可连接时，发送加密数据包</span>            <span class="hljs-keyword">if</span> (key.isConnectable()) &#123;                onRemoteConnect(key, channel);            &#125;            <span class="hljs-comment">// 当远程返回数据时，进行解密，将解密数据返回给浏览器</span>            <span class="hljs-keyword">if</span> (key.isReadable()) &#123;                onRemoteRead(channel);            &#125;        &#125;    &#125; <span class="hljs-keyword">catch</span> (Exception e) &#123;        log.error(e.toString(), e);        <span class="hljs-keyword">this</span>.stage = SocksStage.STAGE_DESTROYED;    &#125;    <span class="hljs-comment">// 销毁</span>    <span class="hljs-keyword">if</span> (<span class="hljs-keyword">this</span>.stage == SocksStage.STAGE_DESTROYED) &#123;        destroy();    &#125;&#125;</code></pre></div><p>与浏览器交互的Channel可读取时表示有新的数据到来，需从Channel读取数据，并写入响应数据</p><div class="hljs"><pre><code class="hljs java"><span class="hljs-function"><span class="hljs-keyword">private</span> <span class="hljs-keyword">void</span> <span class="hljs-title">onLocalRead</span><span class="hljs-params">(SocketChannel socketChannel)</span> <span class="hljs-keyword">throws</span> IOException </span>&#123;        SocksStage socksStage = <span class="hljs-keyword">this</span>.stage;        <span class="hljs-keyword">int</span> read = socketChannel.read(byteBuffer);        <span class="hljs-keyword">byte</span>[] readData = ByteBuffers.convertByte(byteBuffer);        <span class="hljs-keyword">if</span> (readData.length == <span class="hljs-number">0</span>) &#123;            <span class="hljs-keyword">this</span>.stage = SocksStage.STAGE_DESTROYED;            <span class="hljs-keyword">return</span>;        &#125;        <span class="hljs-comment">// 通过状态控制socks5交互的步骤</span>        <span class="hljs-keyword">switch</span> (socksStage) &#123;            <span class="hljs-comment">/*</span><span class="hljs-comment">                +----+----------+----------+</span><span class="hljs-comment">                |VER | NMETHODS | METHODS  |</span><span class="hljs-comment">                +----+----------+----------+</span><span class="hljs-comment">                | 1  |    1     |  1~255   |</span><span class="hljs-comment">                +----+----------+----------+</span><span class="hljs-comment">             */</span>            <span class="hljs-comment">// 握手，告知支持的协议</span>            <span class="hljs-keyword">case</span> STAGE_INIT:                socketChannel.write(ByteBuffer.wrap(<span class="hljs-keyword">new</span> <span class="hljs-keyword">byte</span>[]&#123;<span class="hljs-number">0x05</span>, <span class="hljs-number">0x00</span>&#125;));                <span class="hljs-keyword">this</span>.stage = SocksStage.STAGE_ADDR;                <span class="hljs-keyword">break</span>;            <span class="hljs-comment">/*</span><span class="hljs-comment">                +----+-----+-------+------+----------+----------+</span><span class="hljs-comment">                |VER | CMD |  RSV  | ATYP | DST.ADDR | DST.PORT |</span><span class="hljs-comment">                +----+-----+-------+------+----------+----------+</span><span class="hljs-comment">                | 1  |  1  |   1   |  1   | Variable |    2     |</span><span class="hljs-comment">                +----+-----+-------+------+----------+----------+</span><span class="hljs-comment">             */</span>            <span class="hljs-comment">// request中有要访问的域名，此处需要与远程建立连接，订阅OP_CONNECT事件，当可连接时通知到我们。</span>            <span class="hljs-keyword">case</span> STAGE_ADDR:                remoteSocketChannel = SocketChannel.open();                remoteSocketChannel.configureBlocking(<span class="hljs-keyword">false</span>);                remoteSocketChannel.register(<span class="hljs-keyword">this</span>.selector, SelectionKey.OP_CONNECT, <span class="hljs-keyword">this</span>);                remoteSocketChannel.connect(<span class="hljs-keyword">new</span> InetSocketAddress(ssConfig.getServer(), ssConfig.getServerPort()));                socketChannel.write(ByteBuffer.wrap(<span class="hljs-keyword">new</span> <span class="hljs-keyword">byte</span>[]&#123;<span class="hljs-number">0x05</span>, <span class="hljs-number">0x00</span>, <span class="hljs-number">0x00</span>, <span class="hljs-number">0x01</span>, <span class="hljs-number">0x00</span>, <span class="hljs-number">0x00</span>, <span class="hljs-number">0x00</span>, <span class="hljs-number">0x00</span>, <span class="hljs-number">0x10</span>, <span class="hljs-number">0x10</span>&#125;));                <span class="hljs-keyword">this</span>.stage = SocksStage.STAGE_CONNECTING;                <span class="hljs-comment">// 0x05 0x01 0x00 0x03 0x0a b'google.com'  0x00 0x50</span>                <span class="hljs-comment">// 0x0a 表示域名长度</span>                Socks5Utils.parseAddr(readData);                <span class="hljs-comment">// 去掉无用数据,缓存向服务端请求的数据</span>                <span class="hljs-keyword">this</span>.byteQueue.add(Arrays.copyOfRange(readData, <span class="hljs-number">3</span>, read));                <span class="hljs-keyword">break</span>;            <span class="hljs-keyword">case</span> STAGE_CONNECTING:                <span class="hljs-comment">// 远程未连接，但是客户端数据已经到达，将数据缓存到队列中</span>                <span class="hljs-keyword">this</span>.byteQueue.add(readData);                <span class="hljs-keyword">break</span>;            <span class="hljs-keyword">case</span> STAGE_STREAM:                <span class="hljs-comment">// 将数据加密发送给远程服务器</span>                <span class="hljs-keyword">byte</span>[] encrypt = <span class="hljs-keyword">this</span>.cryptHelper.encrypt(readData);                <span class="hljs-keyword">this</span>.remoteSocketChannel.write(ByteBuffer.wrap(encrypt));                <span class="hljs-keyword">break</span>;        &#125;    &#125;</code></pre></div><p>与浏览器交互的第2步时，会与远程建立连接，订阅连接事件。此处为可连接时的处理，将数据推送给远程，并订阅读取事件准备接受远程服务器的响应</p><div class="hljs"><pre><code class="hljs java"><span class="hljs-function"><span class="hljs-keyword">private</span> <span class="hljs-keyword">void</span> <span class="hljs-title">onRemoteConnect</span><span class="hljs-params">(SelectionKey key, SocketChannel socketChannel)</span> <span class="hljs-keyword">throws</span> IOException </span>&#123;    <span class="hljs-keyword">if</span> (socketChannel.finishConnect()) &#123;        key.interestOps(SelectionKey.OP_READ);        <span class="hljs-keyword">this</span>.stage = SocksStage.STAGE_STREAM;        <span class="hljs-keyword">for</span> (<span class="hljs-keyword">byte</span>[] bytes : byteQueue) &#123;            <span class="hljs-keyword">byte</span>[] encrypt = <span class="hljs-keyword">this</span>.cryptHelper.encrypt(bytes);            socketChannel.write(ByteBuffer.wrap(encrypt));        &#125;    &#125;&#125;</code></pre></div><p>远程服务可读时表示有数据返回，读取并将数据解密，返回给浏览器</p><div class="hljs"><pre><code class="hljs java"><span class="hljs-function"><span class="hljs-keyword">private</span> <span class="hljs-keyword">void</span> <span class="hljs-title">onRemoteRead</span><span class="hljs-params">(SocketChannel socketChannel)</span> <span class="hljs-keyword">throws</span> IOException </span>&#123;    socketChannel.read(byteBuffer);    <span class="hljs-keyword">byte</span>[] readData = ByteBuffers.convertByte(byteBuffer);    <span class="hljs-keyword">if</span> (readData.length == <span class="hljs-number">0</span>) &#123;        <span class="hljs-keyword">this</span>.stage = SocksStage.STAGE_DESTROYED;        <span class="hljs-keyword">return</span>;    &#125;    <span class="hljs-keyword">byte</span>[] decrypt = <span class="hljs-keyword">this</span>.cryptHelper.decrypt(readData);    <span class="hljs-keyword">this</span>.localSocketChannel.write(ByteBuffer.wrap(decrypt));&#125;</code></pre></div><h3 id="参考文档"><a href="#参考文档" class="headerlink" title="参考文档"></a>参考文档</h3><p><a href="https://zh.wikipedia.org/wiki/SOCKS" target="_blank" rel="noopener">wiki</a></p><p><a href="https://github.com/shadowsocks/shadowsocks/tree/master" target="_blank" rel="noopener">Shadowsocks</a></p>]]></content>
    
    
    <categories>
      
      <category>Java</category>
      
    </categories>
    
    
    <tags>
      
      <tag>NIO</tag>
      
      <tag>SOCKS</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>Spring Cloud Gateway 登录拦截</title>
    <link href="/post/a9515f36.html"/>
    <url>/post/a9515f36.html</url>
    
    <content type="html"><![CDATA[<p><code>Spring Cloud Gateway</code>为Spring官方基于Spring 5.0、Spring Boot2.0和Project Reactor等技术开发的网关，鉴权是网关常用的功能，本文介绍基于<code>Spring Cloud Gateway</code>的登录拦截功能。</p><a id="more"></a><p><code>GlobalFilter</code>为全局过滤器的标识接口，我这个拦截是针对所有服务，所以使用了全局过滤器。</p><p>实现<code>Ordered</code>为了告诉<code>Gateway</code>我这个过滤器的优先级，<code>getOrder</code>方法返回值越大优先级越低，越后执行。</p><p>还有要注意的是<code>Spring Cloud Gateway</code>底层使用的是<code>webflux</code>，基于响应式编程，我们也要避免代码中出现阻塞I/O的调用，所以我从Redis中取token使用的是非阻塞的<code>ReactiveRedisTemplate</code>。</p><blockquote><p>Talk is cheap,show me the code</p></blockquote><div class="hljs"><pre><code class="hljs java"><span class="hljs-meta">@Component</span><span class="hljs-meta">@Slf</span>4j<span class="hljs-keyword">public</span> <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">TokenFilter</span> <span class="hljs-keyword">implements</span> <span class="hljs-title">GlobalFilter</span>, <span class="hljs-title">Ordered</span> </span>&#123;    <span class="hljs-keyword">private</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> String TOKEN_HEAD = <span class="hljs-string">"token"</span>;    <span class="hljs-comment">/**</span><span class="hljs-comment">     * Redis中token前缀</span><span class="hljs-comment">     */</span>    <span class="hljs-keyword">private</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> String PREFIX = <span class="hljs-string">"CASECODE:USER:TOKEN:"</span>;    <span class="hljs-keyword">private</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> <span class="hljs-keyword">long</span> TIMEOUT = <span class="hljs-number">60</span> * <span class="hljs-number">30</span>;    <span class="hljs-comment">/**</span><span class="hljs-comment">     * 需排除的接口</span><span class="hljs-comment">     */</span>    <span class="hljs-keyword">private</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> Set&lt;String&gt; exclude_path = ImmutableSet.of(            <span class="hljs-string">"/sep/user/login"</span>, <span class="hljs-string">"/sep/user/getCheckCode/"</span>    );    <span class="hljs-meta">@Autowired</span>    <span class="hljs-keyword">private</span> ReactiveRedisTemplate&lt;String, String&gt; redisTemplate;    <span class="hljs-meta">@Override</span>    <span class="hljs-function"><span class="hljs-keyword">public</span> Mono&lt;Void&gt; <span class="hljs-title">filter</span><span class="hljs-params">(ServerWebExchange exchange, GatewayFilterChain chain)</span> </span>&#123;        <span class="hljs-keyword">return</span> Mono.just(exchange)                .map(ServerWebExchange::getRequest)                .flatMap(serverHttpRequest -&gt; &#123;                    String path = serverHttpRequest.getPath().toString();                    log.info(<span class="hljs-string">"path:&#123;&#125;"</span>,path);                    <span class="hljs-comment">// 排除不需要登录的接口</span>                    <span class="hljs-keyword">if</span> (exclude_path.stream().anyMatch(path::contains)) &#123;                        log.info(<span class="hljs-string">"无需登录"</span>);                        <span class="hljs-keyword">return</span> chain.filter(exchange);                    &#125;                    <span class="hljs-comment">// 从HEAD中取token</span>                    List&lt;String&gt; tokens = serverHttpRequest.getHeaders().get(TOKEN_HEAD);                    <span class="hljs-comment">// 无TOKEN</span>                    <span class="hljs-keyword">if</span> (CollectionUtils.isEmpty(tokens)) &#123;                        <span class="hljs-keyword">return</span> <span class="hljs-keyword">this</span>.buildLoginResp(exchange);                    &#125;                    String token = Iterables.getOnlyElement(tokens);                    <span class="hljs-comment">// 拼接前缀做为RedisKey</span>                    String redisKey = PREFIX + token;                    <span class="hljs-keyword">return</span> redisTemplate.opsForValue().get(redisKey)                            .defaultIfEmpty(StringUtils.EMPTY)                            .flatMap(user -&gt; &#123;                                log.info(<span class="hljs-string">"\nTOKEN:&#123;&#125;\nVALUE:&#123;&#125;"</span>, token, user);                                <span class="hljs-comment">// redis中有该用户</span>                                <span class="hljs-keyword">if</span> (StringUtils.isNotEmpty(user)) &#123;                                    <span class="hljs-keyword">return</span> redisTemplate.expire(redisKey, Duration.of(TIMEOUT, ChronoUnit.SECONDS))                                            .then(chain.filter(exchange));                                &#125;                                <span class="hljs-keyword">return</span> <span class="hljs-keyword">this</span>.buildLoginResp(exchange);                            &#125;);                &#125;);    &#125;    <span class="hljs-meta">@Override</span>    <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">int</span> <span class="hljs-title">getOrder</span><span class="hljs-params">()</span> </span>&#123;        <span class="hljs-keyword">return</span> <span class="hljs-number">0</span>;    &#125;    <span class="hljs-function"><span class="hljs-keyword">private</span> Mono&lt;Void&gt; <span class="hljs-title">buildLoginResp</span><span class="hljs-params">(ServerWebExchange exchange)</span> </span>&#123;        ServerHttpResponse response = exchange.getResponse();        Map&lt;String, Object&gt; map = <span class="hljs-keyword">new</span> HashMap&lt;&gt;();        map.put(<span class="hljs-string">"code"</span>, <span class="hljs-number">401</span>);        map.put(<span class="hljs-string">"message"</span>, <span class="hljs-string">"无效token"</span>);        String json = <span class="hljs-keyword">new</span> Gson().toJson(map);        <span class="hljs-keyword">byte</span>[] bytes = json.getBytes(StandardCharsets.UTF_8);        DataBuffer buffer = response.bufferFactory().wrap(bytes);        response.setStatusCode(HttpStatus.OK);        <span class="hljs-keyword">return</span> response.writeWith(Flux.just(buffer));    &#125;&#125;</code></pre></div>]]></content>
    
    
    <categories>
      
      <category>Java</category>
      
    </categories>
    
    
    <tags>
      
      <tag>Spring Cloud</tag>
      
      <tag>Gateway</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>Spring Cloud OpenFeign 上传文件</title>
    <link href="/post/4c4b951a.html"/>
    <url>/post/4c4b951a.html</url>
    
    <content type="html"><![CDATA[<p><a href="https://github.com/OpenFeign/feign" target="_blank" rel="noopener">Feign</a>是一个声明式的伪Http客户端，它使得写Http客户端变得更简单。</p><p><code>Spring Cloud OpenFeign</code>是<code>Spring Could</code>服务间调用的基础组件,将Feign进行了封装，更便于使用，本文使用<code>Spring Cloud OpenFeign</code>进行文件上传。</p><a id="more"></a><blockquote><p><code>spring-cloud-openfeign</code>版本<code>2.1.1.RELEASE</code></p></blockquote><h3 id="声明使用Form表单形式提交"><a href="#声明使用Form表单形式提交" class="headerlink" title="声明使用Form表单形式提交"></a>声明使用Form表单形式提交</h3><div class="hljs"><pre><code class="hljs java"><span class="hljs-meta">@Configuration</span><span class="hljs-keyword">public</span> <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">MultipartSupportConfig</span> </span>&#123;    <span class="hljs-meta">@Bean</span>    <span class="hljs-function"><span class="hljs-keyword">public</span> Encoder <span class="hljs-title">feignFormEncoder</span><span class="hljs-params">()</span> </span>&#123;        <span class="hljs-keyword">return</span> <span class="hljs-keyword">new</span> SpringFormEncoder();    &#125;&#125;</code></pre></div><p><code>consumes</code>指定<code>MediaType</code>为<code>multipart/form-data</code></p><div class="hljs"><pre><code class="hljs java"><span class="hljs-meta">@FeignClient</span>(name = <span class="hljs-string">"test"</span>, url = <span class="hljs-string">"http://localhost:8141"</span>, path = <span class="hljs-string">"p/c"</span>, configuration = MultipartSupportConfig<span class="hljs-class">.<span class="hljs-keyword">class</span>)</span><span class="hljs-class"><span class="hljs-title">public</span> <span class="hljs-title">interface</span> <span class="hljs-title">UploadFileClient</span> </span>&#123;    <span class="hljs-meta">@RequestMapping</span>(path = <span class="hljs-string">"uploadPrintedCodesFile"</span>, consumes = MULTIPART_FORM_DATA_VALUE)    <span class="hljs-function">Object <span class="hljs-title">uploadPrintedCodesFile</span><span class="hljs-params">(UploadData uploadData)</span></span>;&#125;</code></pre></div><h3 id="将文件放在对象中作为form表单中的一部分进行提交"><a href="#将文件放在对象中作为form表单中的一部分进行提交" class="headerlink" title="将文件放在对象中作为form表单中的一部分进行提交"></a>将文件放在对象中作为form表单中的一部分进行提交</h3><div class="hljs"><pre><code class="hljs java"><span class="hljs-meta">@Autowired</span>   <span class="hljs-keyword">private</span> UploadFileClient uploadFileClient;   <span class="hljs-meta">@Test</span>   <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">test</span><span class="hljs-params">()</span></span>&#123;       MultipartFile result = <span class="hljs-keyword">new</span> MockMultipartFile(name,               originalFileName, contentType, content);       UploadData uploadData = <span class="hljs-keyword">new</span> UploadData();       uploadData.setFactoryNo(<span class="hljs-string">"test"</span>);       uploadData.setFile(result);       uploadFileClient.uploadPrintedCodesFile(uploadData);   &#125;</code></pre></div><p>如果只是想提交文件，方法可以这样写,将<code>MultipartFile</code>传入即可</p><div class="hljs"><pre><code class="hljs java"><span class="hljs-meta">@FeignClient</span>(name = <span class="hljs-string">"test"</span>, url = <span class="hljs-string">"http://localhost:8141"</span>, path = <span class="hljs-string">"p/c"</span>, configuration = MultipartSupportConfig<span class="hljs-class">.<span class="hljs-keyword">class</span>)</span><span class="hljs-class"><span class="hljs-title">public</span> <span class="hljs-title">interface</span> <span class="hljs-title">UploadFileClient</span> </span>&#123;    <span class="hljs-meta">@RequestMapping</span>(path = <span class="hljs-string">"uploadPrintedCodesFile"</span>, consumes = MULTIPART_FORM_DATA_VALUE)    <span class="hljs-function">Object <span class="hljs-title">uploadPrintedCodesFile</span><span class="hljs-params">(MultipartFile file)</span></span>;&#125;</code></pre></div><h3 id="多个client使用相同name报错"><a href="#多个client使用相同name报错" class="headerlink" title="多个client使用相同name报错"></a>多个client使用相同name报错</h3><div class="hljs"><pre><code class="hljs stylus">Caused by: org<span class="hljs-selector-class">.springframework</span><span class="hljs-selector-class">.beans</span><span class="hljs-selector-class">.factory</span><span class="hljs-selector-class">.support</span><span class="hljs-selector-class">.BeanDefinitionOverrideException</span>: Invalid bean definition with name <span class="hljs-string">'XXX.FeignClientSpecification'</span> defined <span class="hljs-keyword">in</span> null:</code></pre></div><p>Spring Bean定义冲突导致服务不能启动,可以添加如下配置</p><div class="hljs"><pre><code class="hljs ini"><span class="hljs-attr">spring.main.allow-bean-definition-overriding</span>=<span class="hljs-literal">true</span></code></pre></div><p>如果不想更改整体Spring配置的话，可以手动指定contextId,优先使用contextId作为ClientName</p><div class="hljs"><pre><code class="hljs reasonml">@<span class="hljs-constructor">FeignClient(<span class="hljs-params">value</span> = <span class="hljs-string">"admin"</span>,<span class="hljs-params">contextId</span> = <span class="hljs-string">"aTest"</span>)</span></code></pre></div>]]></content>
    
    
    <categories>
      
      <category>Java</category>
      
    </categories>
    
    
    <tags>
      
      <tag>Spring Cloud</tag>
      
      <tag>Feign</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>Docker使用宿主代理</title>
    <link href="/post/21f939f5.html"/>
    <url>/post/21f939f5.html</url>
    
    <content type="html"><![CDATA[<p><img src="https://img.yjll.art/img/20200428102310.png" srcset="/img/loading.gif" alt="20200428102310"></p><p>在docker中安装软件，下载地址在外国，下载速度奇慢，这个时候可以通过宿主的代理访问。</p><a id="more"></a><p>Docker启动容器，默认使用bridge模式分配网络，会在宿主机安装一个虚拟网卡docker0。我们通过<code>ifconfig</code>命令查看一下</p><div class="hljs"><pre><code class="hljs angelscript">docker0: flags=<span class="hljs-number">4163</span>&lt;UP,BROADCAST,RUNNING,MULTICAST&gt;  mtu <span class="hljs-number">1500</span>        inet <span class="hljs-number">172.17</span><span class="hljs-number">.0</span><span class="hljs-number">.1</span>  netmask <span class="hljs-number">255.255</span><span class="hljs-number">.0</span><span class="hljs-number">.0</span>  broadcast <span class="hljs-number">172.17</span><span class="hljs-number">.255</span><span class="hljs-number">.255</span>        inet6 fe80::<span class="hljs-number">42</span>:caff:fefe:<span class="hljs-number">2f</span>7e  prefixlen <span class="hljs-number">64</span>  scopeid <span class="hljs-number">0x20</span>&lt;link&gt;        ether <span class="hljs-number">02</span>:<span class="hljs-number">42</span>:ca:fe:<span class="hljs-number">2f</span>:<span class="hljs-number">7</span>e  txqueuelen <span class="hljs-number">0</span>  (Ethernet)        RX packets <span class="hljs-number">7941959</span>  bytes <span class="hljs-number">525663055</span> (<span class="hljs-number">525.6</span> MB)        RX errors <span class="hljs-number">0</span>  dropped <span class="hljs-number">0</span>  overruns <span class="hljs-number">0</span>  frame <span class="hljs-number">0</span>        TX packets <span class="hljs-number">8654991</span>  bytes <span class="hljs-number">11073643780</span> (<span class="hljs-number">11.0</span> GB)        TX errors <span class="hljs-number">0</span>  dropped <span class="hljs-number">0</span> overruns <span class="hljs-number">0</span>  carrier <span class="hljs-number">0</span>  collisions <span class="hljs-number">0</span></code></pre></div><p>对于容器来说，宿主的IP地址为<code>172.17.0.1</code></p><h3 id="在已有的容器中使用代理"><a href="#在已有的容器中使用代理" class="headerlink" title="在已有的容器中使用代理"></a>在已有的容器中使用代理</h3><p>我们进入容器进行代理配置</p><div class="hljs"><pre><code class="hljs angelscript">export http_proxy=http:<span class="hljs-number">172.17</span><span class="hljs-number">.0</span><span class="hljs-number">.1</span>:<span class="hljs-number">1081</span>export https_proxy=http:<span class="hljs-number">172.17</span><span class="hljs-number">.0</span><span class="hljs-number">.1</span>:<span class="hljs-number">1081</span></code></pre></div><h3 id="bulid-Dockerfile-使用代理"><a href="#bulid-Dockerfile-使用代理" class="headerlink" title="bulid Dockerfile 使用代理"></a>bulid Dockerfile 使用代理</h3><p>编辑Dokkerfile添加以下环境</p><div class="hljs"><pre><code class="hljs dockerfile"><span class="hljs-keyword">ENV</span> http_proxy <span class="hljs-string">"http://172.17.0.1:1081"</span><span class="hljs-keyword">ENV</span> https_proxy <span class="hljs-string">"http://172.17.0.1:1081"</span></code></pre></div><h3 id="下载镜像时使用代理"><a href="#下载镜像时使用代理" class="headerlink" title="下载镜像时使用代理"></a>下载镜像时使用代理</h3><p>官方的镜像源在国外，我们可以使用国内的镜像，但有时还是需要通过官方的源拉取镜像</p><div class="hljs"><pre><code class="hljs routeros">sudo mkdir -p /etc/systemd/system/docker.service.d<span class="hljs-comment"># 创建proxy文件</span>vim /etc/systemd/system/docker.service.d/http-proxy.conf<span class="hljs-comment"># 配置代理地址</span>[Service]<span class="hljs-attribute">Environment</span>=<span class="hljs-string">"HTTPS_PROXY=http://localhost:1081"</span><span class="hljs-comment"># 需要重启Docker</span>sudo<span class="hljs-built_in"> service </span>docker restart</code></pre></div><p>好了，可以愉快的乱搞了。</p>]]></content>
    
    
    <categories>
      
      <category>Linux</category>
      
    </categories>
    
    
    <tags>
      
      <tag>Docker</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>RDS MySQL 物理备份文件恢复到自建数据库</title>
    <link href="/post/236702d.html"/>
    <url>/post/236702d.html</url>
    
    <content type="html"><![CDATA[<p>阿里云提供了定期备份数据库的功能，可以在阿里云的控制台将数据库恢复到备份点，那我们想将这部分数据导入到我们自建的数据库怎么操作呢？官方推荐使用开源软件Percona Xtrabackup对数据库进行恢复，Percona Xtrabackup目前没有Windows版本，但是我们可以使用Docker进行恢复。</p><a id="more"></a><h3 id="准备工作"><a href="#准备工作" class="headerlink" title="准备工作"></a>准备工作</h3><p>在阿里云的后台下载备份文件</p><p><img src="https://img.yjll.art/img/bak.png" srcset="/img/loading.gif" alt="bak"></p><p>生产环境的数据库使用的Mysql 8.0.16，我直接通过docker运行，并直接进入容器内部。</p><div class="hljs"><pre><code class="hljs angelscript">docker run -v /root/bak.xb:/root/bak.xb  -p <span class="hljs-number">3308</span>:<span class="hljs-number">3306</span> -it mysql:<span class="hljs-number">8.0</span><span class="hljs-number">.16</span> bash</code></pre></div><p>先更换<a href="https://mirrors.163.com/.help/debian.html" target="_blank" rel="noopener">163源</a>方便安装软件</p><div class="hljs"><pre><code class="hljs vim"><span class="hljs-keyword">cat</span> &gt;&gt;/etc/apt/sources.<span class="hljs-keyword">list</span>&lt;&lt;EOF<span class="hljs-keyword">deb</span> http://mirrors.<span class="hljs-number">163</span>.<span class="hljs-keyword">com</span>/debian/ stretch main non-free contrib<span class="hljs-keyword">deb</span> http://mirrors.<span class="hljs-number">163</span>.<span class="hljs-keyword">com</span>/debian/ stretch-updates main non-free contrib<span class="hljs-keyword">deb</span> http://mirrors.<span class="hljs-number">163</span>.<span class="hljs-keyword">com</span>/debian/ stretch-backports main non-free contrib<span class="hljs-keyword">deb</span>-src http://mirrors.<span class="hljs-number">163</span>.<span class="hljs-keyword">com</span>/debian/ stretch main non-free contrib<span class="hljs-keyword">deb</span>-src http://mirrors.<span class="hljs-number">163</span>.<span class="hljs-keyword">com</span>/debian/ stretch-updates main non-free contrib<span class="hljs-keyword">deb</span>-src http://mirrors.<span class="hljs-number">163</span>.<span class="hljs-keyword">com</span>/debian/ stretch-backports main non-free contrib<span class="hljs-keyword">deb</span> http://mirrors.<span class="hljs-number">163</span>.<span class="hljs-keyword">com</span>/debian-security/ stretch/updates main non-free contrib<span class="hljs-keyword">deb</span>-src http://mirrors.<span class="hljs-number">163</span>.<span class="hljs-keyword">com</span>/debian-security/ stretch/updates main non-free contribEOF</code></pre></div><p>安装我们的主角percona-xtrabackup用户恢复文件，直接下载deb包进行安装</p><div class="hljs"><pre><code class="hljs vim">apt -<span class="hljs-keyword">y</span> install lsb-release wget aria2 <span class="hljs-keyword">vim</span>wget http<span class="hljs-variable">s:</span>//repo.percona.<span class="hljs-keyword">com</span>/apt/percona-release_latest.$(lsb_release -sc)_all.<span class="hljs-keyword">deb</span>dpkg -i percona-release_latest.$(lsb_release -sc)_all.<span class="hljs-keyword">deb</span>apt-<span class="hljs-built_in">get</span> <span class="hljs-keyword">update</span>apt-<span class="hljs-built_in">get</span> -<span class="hljs-keyword">y</span> install percona-xtrabackup-<span class="hljs-number">80</span></code></pre></div><p>创建解压目录，官方文档中使用的这个路径，我懒得改了，就创建了一个</p><div class="hljs"><pre><code class="hljs arduino"><span class="hljs-built_in">mkdir</span> -p /<span class="hljs-built_in">home</span>/mysql/data</code></pre></div><p>开始解压</p><div class="hljs"><pre><code class="hljs jboss-cli">先安装qpress解压软件aria2c http:<span class="hljs-string">//www.quicklz.com/qpress-11-linux-x64.tar</span>tar xvf qpress-11-linux-x64.tarchmod 775 qpresscp qpress <span class="hljs-string">/usr/bin</span>cat bak.xb | xbstream -x -v -C <span class="hljs-string">/home/mysql/data</span>xtrabackup <span class="hljs-params">--decompress</span> <span class="hljs-params">--remove-original</span> <span class="hljs-params">--target-dir=/home/mysql/data</span></code></pre></div><p>解压后进行数据恢复</p><div class="hljs"><pre><code class="hljs crystal"><span class="hljs-comment"># 删除原数据库中数据</span>rm -rf /var/<span class="hljs-class"><span class="hljs-keyword">lib</span>/<span class="hljs-title">mysql</span>/*</span>xtrabackup --prepare --target-dir=<span class="hljs-regexp">/home/mysql</span><span class="hljs-regexp">/data</span><span class="hljs-regexp">xtrabackup --datadir=/var</span><span class="hljs-regexp">/lib/mysql</span> --copy-back --target-dir=<span class="hljs-regexp">/home/mysql</span><span class="hljs-regexp">/data</span></code></pre></div><p>自建数据库不支持如下参数，需要注释掉。</p><p>vi /home/mysql/data/backup-my.cnf</p><div class="hljs"><pre><code class="hljs vala"><span class="hljs-meta">#innodb_log_checksum_algorithm</span><span class="hljs-meta">#innodb_fast_checksum</span><span class="hljs-meta">#innodb_log_block_size</span><span class="hljs-meta">#innodb_doublewrite_file</span><span class="hljs-meta">#rds_encrypt_data</span><span class="hljs-meta">#innodb_encrypt_algorithm</span><span class="hljs-meta">#redo_log_version</span><span class="hljs-meta">#master_key_id</span><span class="hljs-meta">#server_uuid</span></code></pre></div><p>启动</p><div class="hljs"><pre><code class="hljs routeros">mysqld_safe <span class="hljs-attribute">--defaults-file</span>=/home/mysql/data/backup-my.cnf <span class="hljs-attribute">--user</span>=root <span class="hljs-attribute">--datadir</span>=/home/mysql/data &amp;</code></pre></div><div class="hljs"><pre><code class="hljs routeros">2020-04-28T09:43:49.658819Z mysqld_safe<span class="hljs-built_in"> Logging </span><span class="hljs-keyword">to</span> <span class="hljs-string">'/home/mysql/data/14864923d3cb.err'</span>.2020-04-28T09:43:49.703474Z mysqld_safe Starting mysqld daemon with databases <span class="hljs-keyword">from</span> /home/mysql/data</code></pre></div><p>如果mysql服务没起来，可以查看/home/mysql/data/14864923d3cb.err这个日志文件</p><p>实际使用遇到过的问题</p><div class="hljs"><pre><code class="hljs angelscript"><span class="hljs-number">2020</span><span class="hljs-number">-08</span><span class="hljs-number">-18</span>T06:<span class="hljs-number">18</span>:<span class="hljs-number">32.304152</span>Z <span class="hljs-number">0</span> [ERROR] [MY<span class="hljs-number">-010095</span>] [Server] Failed to access directory <span class="hljs-keyword">for</span> --secure-file-priv. Please make sure that directory exists <span class="hljs-keyword">and</span> <span class="hljs-keyword">is</span> accessible by MySQL Server. Supplied value : /var/lib/mysql-files</code></pre></div><p><code>--secure-file-priv</code> 默认使用 <code>/var/lib/mysql-files</code>这个目录，我们更改一下</p><div class="hljs"><pre><code class="hljs gradle">vim <span class="hljs-regexp">/home/my</span>sql<span class="hljs-regexp">/data/</span>backup-my.cnfsecure-<span class="hljs-keyword">file</span>-priv=<span class="hljs-regexp">/home/my</span>sql<span class="hljs-regexp">/data</span></code></pre></div><div class="hljs"><pre><code class="hljs routeros">2020-08-18T06:20:49.765666Z 1 [<span class="hljs-builtin-name">ERROR</span>] [MY-011087] [Server] Different lower_case_table_names<span class="hljs-built_in"> settings </span><span class="hljs-keyword">for</span><span class="hljs-built_in"> server </span>(<span class="hljs-string">'0'</span>) <span class="hljs-keyword">and</span> data dictionary (<span class="hljs-string">'1'</span>).</code></pre></div><p>大小写配置问题</p><div class="hljs"><pre><code class="hljs ini"><span class="hljs-attr">lower_case_table_names</span>=<span class="hljs-number">1</span></code></pre></div><p>再次启动后我们登录一下</p><div class="hljs"><pre><code class="hljs routeros">root@a8a32828e279:/home/mysql/data# mysql -upro -p            Enter password: Welcome <span class="hljs-keyword">to</span> the MySQL monitor.  Commands end with ; <span class="hljs-keyword">or</span> \g.Your MySQL<span class="hljs-built_in"> connection </span>id is 17Server version: 8.0.16 MySQL<span class="hljs-built_in"> Community Server </span>- GPLCopyright (c) 2000, 2019, Oracle <span class="hljs-keyword">and</span>/<span class="hljs-keyword">or</span> its affiliates. All rights reserved.Oracle is a registered trademark of Oracle Corporation <span class="hljs-keyword">and</span>/<span class="hljs-keyword">or</span> itsaffiliates. Other names may be trademarks of their respectiveowners.Type <span class="hljs-string">'help;'</span> <span class="hljs-keyword">or</span> <span class="hljs-string">'\h'</span> <span class="hljs-keyword">for</span> help.<span class="hljs-built_in"> Type </span><span class="hljs-string">'\c'</span> <span class="hljs-keyword">to</span> clear the current input statement.mysql&gt; show databases    -&gt; ;+--------------------+| Database           |+--------------------+| pro                || information_schema || mysql              |+--------------------+3 rows <span class="hljs-keyword">in</span> <span class="hljs-builtin-name">set</span> (0.01 sec)mysql&gt;</code></pre></div><p>OK，大功告成</p><h3 id="参考资料"><a href="#参考资料" class="headerlink" title="参考资料"></a>参考资料</h3><p><a href="https://help.aliyun.com/knowledge_detail/41817.html" target="_blank" rel="noopener">https://help.aliyun.com/knowledge_detail/41817.html</a></p>]]></content>
    
    
    <categories>
      
      <category>Linux</category>
      
    </categories>
    
    
    <tags>
      
      <tag>Docker</tag>
      
      <tag>Mysql</tag>
      
      <tag>Aliyun</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>Nginx使用Certbot配置HTTPS证书</title>
    <link href="/post/77a89a46.html"/>
    <url>/post/77a89a46.html</url>
    
    <content type="html"><![CDATA[<p><img src="https://img.yjll.art//img/20200426131416.jpg" srcset="/img/loading.gif" alt="20200426131416"></p><p>随着互联网的发展，数据安全问题越来越收到重视，Chrome 将没有配置 SSL 加密的 HTTP 网站标记为不安全，HTTPS是未来互联网发展的趋势，但是想要网站支持HTTPS网站必须有Certificate Authority颁发的证书，一般CA签发的证书都是收费的。本文将介绍如何使用免费的授权证书并自动续签。</p><p>Let’s Encrypt 是 一个叫 ISRG （ Internet Security Research Group ，互联网安全研究小组）的组织推出的免费安全证书计划。ISRG 的发起者 EFF （电子前哨基金会）为 Let’s Encrypt 项目发布了一个官方的客户端 Certbot ，利用它可以完全自动化的获取、部署和更新安全证书。</p><a id="more"></a><h3 id="准备"><a href="#准备" class="headerlink" title="准备"></a>准备</h3><p>我们已经安装了Nginx，并且使用默认的80端口，例如我的Hexo博客配置，Certbot会更改nginx的配置文件，操作前最好备份一下nginx.conf。</p><div class="hljs"><pre><code class="hljs pgsql"><span class="hljs-keyword">server</span> &#123;    <span class="hljs-keyword">listen</span> <span class="hljs-number">80</span>;    server_name blog.yjll.art;    <span class="hljs-keyword">location</span> / &#123;        <span class="hljs-keyword">include</span> /etc/nginx/mime.<span class="hljs-keyword">types</span>;        root /var/www/blog/<span class="hljs-built_in">public</span>;        <span class="hljs-keyword">index</span> <span class="hljs-keyword">index</span>.html;    &#125;&#125;</code></pre></div><p>我们先安装Certbot,我是用的是CentOS7，就直接使用yum安装啦。</p><div class="hljs"><pre><code class="hljs cmake">yum <span class="hljs-keyword">install</span> python2-certbot-nginx</code></pre></div><h3 id="通过Certbot获取配置证书"><a href="#通过Certbot获取配置证书" class="headerlink" title="通过Certbot获取配置证书"></a>通过Certbot获取配置证书</h3><p>安装完成后，输入命令</p><div class="hljs"><pre><code class="hljs ada">certbot <span class="hljs-comment">--nginx</span></code></pre></div><p>期间会要求输入邮箱信息，确认协议和域名等，根据提示输入就好。我们看一下生成好的nginx.conf，原来的80变成了443端口，增加了证书配置。</p><div class="hljs"><pre><code class="hljs nginx"><span class="hljs-section">server</span> &#123;    <span class="hljs-attribute">server_name</span> blog.yjll.art;    <span class="hljs-attribute">location</span> / &#123;        <span class="hljs-attribute">include</span> /etc/nginx/mime.types;        <span class="hljs-attribute">root</span> /var/www/blog/public;        <span class="hljs-attribute">index</span> index.html;    &#125;    <span class="hljs-attribute">listen</span> <span class="hljs-number">443</span> ssl; <span class="hljs-comment"># managed by Certbot</span>    <span class="hljs-attribute">ssl_certificate</span> /etc/letsencrypt/live/yjll.art/fullchain.pem; <span class="hljs-comment"># managed by Certbot</span>    <span class="hljs-attribute">ssl_certificate_key</span> /etc/letsencrypt/live/yjll.art/privkey.pem; <span class="hljs-comment"># managed by Certbot</span>    <span class="hljs-attribute">include</span> /etc/letsencrypt/options-ssl-nginx.conf; <span class="hljs-comment"># managed by Certbot</span>    <span class="hljs-attribute">ssl_dhparam</span> /etc/letsencrypt/ssl-dhparams.pem; <span class="hljs-comment"># managed by Certbot</span>&#125;</code></pre></div><p>为了原来的http连接也可以正常使用，我们可以通过Nginx将http请求重定向到现在的端口上</p><div class="hljs"><pre><code class="hljs nginx"><span class="hljs-section">server</span> &#123;<span class="hljs-attribute">listen</span>       <span class="hljs-number">80</span>;<span class="hljs-attribute">rewrite</span><span class="hljs-regexp"> ^(.*)</span> https://<span class="hljs-variable">$host</span><span class="hljs-variable">$1</span> <span class="hljs-literal">permanent</span>;&#125;</code></pre></div><p>好了，可以在浏览器访问HTTP和HTTPS链接试一试了。</p><h3 id="自动续期"><a href="#自动续期" class="headerlink" title="自动续期"></a>自动续期</h3><p>使用起来确实挺方便的，但是Let’s Encrypt发布的证书期限只有三个月，在证书到期前我们要重新申请证书，<code>certbot</code>也提供了续订的功能</p><div class="hljs"><pre><code class="hljs ebnf"><span class="hljs-attribute">certbot renew</span></code></pre></div><p>也就是说在证书过期前要运行一下上边的命令，并且重新加载nginx的配置文件，这部分我们可以配置在定时任务中。</p><div class="hljs"><pre><code class="hljs haxe">certbot renew <span class="hljs-type"></span>--force-renew <span class="hljs-type"></span>--renew<span class="hljs-type"></span>-hook <span class="hljs-string">"nginx -s reload"</span></code></pre></div><h3 id="参考资料"><a href="#参考资料" class="headerlink" title="参考资料"></a>参考资料</h3><p><a href="https://certbot.eff.org/" target="_blank" rel="noopener">https://certbot.eff.org/</a></p><p><a href="https://learnku.com/articles/19999" target="_blank" rel="noopener">https://learnku.com/articles/19999</a></p>]]></content>
    
    
    <categories>
      
      <category>Linux</category>
      
    </categories>
    
    
    <tags>
      
      <tag>Nginx</tag>
      
      <tag>Https</tag>
      
      <tag>Certbot</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>漫谈Guava之Cache（二）</title>
    <link href="/post/a6e28e21.html"/>
    <url>/post/a6e28e21.html</url>
    
    <content type="html"><![CDATA[<p>之前的文章介绍了Guava Cache的使用，本文将剖析Cache的源码，对Guava Cache的不太了解的可以移步这里：</p><p><a href="https://blog.yjll.art/post/96f462f9.html">漫谈Guava之Cache（一）</a></p><a id="more"></a><h3 id="创建"><a href="#创建" class="headerlink" title="创建"></a>创建</h3><p>创建Cache需通过CacheBuilder，该处使用了建造者模式，我们来看CacheBuilder的build方法。</p><div class="hljs"><pre><code class="hljs java"><span class="hljs-keyword">public</span> &lt;K1 extends K, V1 extends V&gt; <span class="hljs-function">Cache&lt;K1, V1&gt; <span class="hljs-title">build</span><span class="hljs-params">()</span> </span>&#123;  checkWeightWithWeigher();  checkNonLoadingCache();  <span class="hljs-keyword">return</span> <span class="hljs-keyword">new</span> LocalCache.LocalManualCache&lt;&gt;(<span class="hljs-keyword">this</span>);&#125;</code></pre></div><div class="hljs"><pre><code class="hljs java">LocalManualCache(CacheBuilder&lt;? <span class="hljs-keyword">super</span> K, ? <span class="hljs-keyword">super</span> V&gt; builder) &#123;  <span class="hljs-keyword">this</span>(<span class="hljs-keyword">new</span> LocalCache&lt;K, V&gt;(builder, <span class="hljs-keyword">null</span>));&#125;</code></pre></div><p>LocalManualCache 相当于LocalCache的包装类，大部分方法直接使用LocalCache的实现，部分方法自己实现了一下。我们直接看LocalCache的逻辑，LocalCache的实现参考了<code>ConcurrentHashMap</code>，采用分段锁技术，当一个线程访问一个 <code>Segment</code> 时，不会影响其他的 <code>Segment</code>。</p><p><img src="https://img.yjll.art/img/Segment.png" srcset="/img/loading.gif" alt="Segment"></p><div class="hljs"><pre><code class="hljs java"><span class="hljs-function">Segment&lt;K, V&gt; <span class="hljs-title">segmentFor</span><span class="hljs-params">(<span class="hljs-keyword">int</span> hash)</span> </span>&#123;  <span class="hljs-keyword">return</span> segments[(hash &gt;&gt;&gt; segmentShift) &amp; segmentMask];&#125;</code></pre></div><p>Segment中维护了5个队列</p><ul><li>keyReferenceQueue key引用队列</li><li>valueReferenceQueue value引用队列 </li><li>recencyQueue 临时队列，数据刷新清空该队列，队列中数据同步到accessQueue中</li><li>writeQueue 写队列</li><li>accessQueue 访问队列</li></ul><h3 id="存放元素"><a href="#存放元素" class="headerlink" title="存放元素"></a>存放元素</h3><p>segmentFor(hash)根据hash值取到Segment，优化并发操作。<br>加锁，<code>preWriteCleanup</code>删除被GC的数据，判断加入的元素在缓存中是否存在，若存在替换<code>enqueueNotification</code>或者不做更改，不存在新增放入缓存中，若缓存元素超限删除不活跃元素<code>evictEntries</code>(LRU)，最后释放锁并推送删除的元素给监听者<code>postWriteCleanup</code>。</p><div class="hljs"><pre><code class="hljs java"><span class="hljs-meta">@Override</span><span class="hljs-function"><span class="hljs-keyword">public</span> V <span class="hljs-title">put</span><span class="hljs-params">(K key, V value)</span> </span>&#123;  checkNotNull(key);  checkNotNull(value);  <span class="hljs-keyword">int</span> hash = hash(key);  <span class="hljs-keyword">return</span> segmentFor(hash).put(key, hash, value, <span class="hljs-keyword">false</span>);&#125;<span class="hljs-function">V <span class="hljs-title">put</span><span class="hljs-params">(K key, <span class="hljs-keyword">int</span> hash, V value, <span class="hljs-keyword">boolean</span> onlyIfAbsent)</span> </span>&#123;  lock();  <span class="hljs-keyword">try</span> &#123;    <span class="hljs-keyword">long</span> now = map.ticker.read();    <span class="hljs-comment">// 清理过期数据和被GC回收的数据</span>    preWriteCleanup(now);    <span class="hljs-keyword">int</span> newCount = <span class="hljs-keyword">this</span>.count + <span class="hljs-number">1</span>;    <span class="hljs-comment">// 扩容</span>    <span class="hljs-keyword">if</span> (newCount &gt; <span class="hljs-keyword">this</span>.threshold) &#123; <span class="hljs-comment">// ensure capacity</span>      expand();      newCount = <span class="hljs-keyword">this</span>.count + <span class="hljs-number">1</span>;    &#125;    AtomicReferenceArray&lt;ReferenceEntry&lt;K, V&gt;&gt; table = <span class="hljs-keyword">this</span>.table;    <span class="hljs-keyword">int</span> index = hash &amp; (table.length() - <span class="hljs-number">1</span>);    ReferenceEntry&lt;K, V&gt; first = table.get(index);    <span class="hljs-comment">// Look for an existing entry.</span>    <span class="hljs-keyword">for</span> (ReferenceEntry&lt;K, V&gt; e = first; e != <span class="hljs-keyword">null</span>; e = e.getNext()) &#123;      K entryKey = e.getKey();      <span class="hljs-keyword">if</span> (e.getHash() == hash          &amp;&amp; entryKey != <span class="hljs-keyword">null</span>          &amp;&amp; map.keyEquivalence.equivalent(key, entryKey)) &#123;        <span class="hljs-comment">// We found an existing entry.</span>        ValueReference&lt;K, V&gt; valueReference = e.getValueReference();        V entryValue = valueReference.get();        <span class="hljs-comment">// 表示触发GC，值被回收了</span>        <span class="hljs-keyword">if</span> (entryValue == <span class="hljs-keyword">null</span>) &#123;          ++modCount;          <span class="hljs-comment">// 排除CacheLoad加载的数据</span>          <span class="hljs-keyword">if</span> (valueReference.isActive()) &#123;            <span class="hljs-comment">// 移入删除队列中，由删除队列触发监听</span>            enqueueNotification(                key, hash, entryValue, valueReference.getWeight(), RemovalCause.COLLECTED);            setValue(e, key, value, now);            newCount = <span class="hljs-keyword">this</span>.count; <span class="hljs-comment">// count remains unchanged</span>          &#125; <span class="hljs-keyword">else</span> &#123;            setValue(e, key, value, now);            newCount = <span class="hljs-keyword">this</span>.count + <span class="hljs-number">1</span>;          &#125;          <span class="hljs-keyword">this</span>.count = newCount; <span class="hljs-comment">// write-volatile</span>          <span class="hljs-comment">// 移除原来元素</span>          evictEntries(e);          <span class="hljs-keyword">return</span> <span class="hljs-keyword">null</span>;        &#125; <span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span> (onlyIfAbsent) &#123;           <span class="hljs-comment">// Mimic</span>          <span class="hljs-comment">// "if (!map.containsKey(key)) ...</span>          <span class="hljs-comment">// else return map.get(key);</span>          <span class="hljs-comment">// 只更新最后访问时间</span>          recordLockedRead(e, now);          <span class="hljs-keyword">return</span> entryValue;        &#125; <span class="hljs-keyword">else</span> &#123;          <span class="hljs-comment">// clobber existing entry, count remains unchanged</span>          ++modCount;          <span class="hljs-comment">// 与第一个条件分支逻辑类似</span>          enqueueNotification(              key, hash, entryValue, valueReference.getWeight(), RemovalCause.REPLACED);          setValue(e, key, value, now);          evictEntries(e);          <span class="hljs-keyword">return</span> entryValue;        &#125;      &#125;    &#125;    <span class="hljs-comment">// Create a new entry.</span>    <span class="hljs-comment">// 缓存中没有，创建一个</span>    ++modCount;    ReferenceEntry&lt;K, V&gt; newEntry = newEntry(key, hash, first);    <span class="hljs-comment">// 包装key，value放到newEntry中，并调用recordWrite()方法，更新权重,将entry存入accessQueue，writeQueue队列中</span>    setValue(newEntry, key, value, now);    table.set(index, newEntry);    newCount = <span class="hljs-keyword">this</span>.count + <span class="hljs-number">1</span>;    <span class="hljs-comment">// count用volatile修饰，保证内存可见性</span>    <span class="hljs-keyword">this</span>.count = newCount; <span class="hljs-comment">// write-volatile</span>    <span class="hljs-comment">// 新增元素后有可能导致缓存数据过多，需进行清理，优先删除较少访问的</span>    evictEntries(newEntry);    <span class="hljs-keyword">return</span> <span class="hljs-keyword">null</span>;  &#125; <span class="hljs-keyword">finally</span> &#123;    unlock();    <span class="hljs-comment">// 移除的元素会放到单独队列中，此时触发监听</span>    postWriteCleanup();  &#125;&#125;</code></pre></div><p>添加或替换元素时会触发<code>recordWrite</code>,更新写队列和读队列</p><div class="hljs"><pre><code class="hljs java"><span class="hljs-function"><span class="hljs-keyword">void</span> <span class="hljs-title">setValue</span><span class="hljs-params">(ReferenceEntry&lt;K, V&gt; entry, K key, V value, <span class="hljs-keyword">long</span> now)</span> </span>&#123;  ValueReference&lt;K, V&gt; previous = entry.getValueReference();  <span class="hljs-keyword">int</span> weight = map.weigher.weigh(key, value);  checkState(weight &gt;= <span class="hljs-number">0</span>, <span class="hljs-string">"Weights must be non-negative"</span>);  ValueReference&lt;K, V&gt; valueReference =      map.valueStrength.referenceValue(<span class="hljs-keyword">this</span>, entry, value, weight);  entry.setValueReference(valueReference);  recordWrite(entry, weight, now);  previous.notifyNewValue(value);&#125;<span class="hljs-function"><span class="hljs-keyword">void</span> <span class="hljs-title">recordWrite</span><span class="hljs-params">(ReferenceEntry&lt;K, V&gt; entry, <span class="hljs-keyword">int</span> weight, <span class="hljs-keyword">long</span> now)</span> </span>&#123;  <span class="hljs-comment">// we are already under lock, so drain the recency queue immediately</span>  drainRecencyQueue();  totalWeight += weight;  <span class="hljs-keyword">if</span> (map.recordsAccess()) &#123;    entry.setAccessTime(now);  &#125;  <span class="hljs-keyword">if</span> (map.recordsWrite()) &#123;    entry.setWriteTime(now);  &#125;  accessQueue.add(entry);  writeQueue.add(entry);&#125;</code></pre></div><p>缓存元素有变动时会调用<code>drainRecencyQueue</code>方法，将<code>recencyQueue</code>队列数据清空，并按照队列顺序放入访问队列中以备LRU策略删除</p><div class="hljs"><pre><code class="hljs java"><span class="hljs-function"><span class="hljs-keyword">void</span> <span class="hljs-title">drainRecencyQueue</span><span class="hljs-params">()</span> </span>&#123;    ReferenceEntry&lt;K, V&gt; e;    <span class="hljs-keyword">while</span> ((e = recencyQueue.poll()) != <span class="hljs-keyword">null</span>) &#123;      <span class="hljs-comment">// An entry may be in the recency queue despite it being removed from</span>      <span class="hljs-comment">// the map . This can occur when the entry was concurrently read while a</span>      <span class="hljs-comment">// writer is removing it from the segment or after a clear has removed</span>      <span class="hljs-comment">// all of the segment's entries.</span>      <span class="hljs-keyword">if</span> (accessQueue.contains(e)) &#123;        accessQueue.add(e);      &#125;    &#125;  &#125;</code></pre></div><h3 id="取元素"><a href="#取元素" class="headerlink" title="取元素"></a>取元素</h3><p>和存放逻辑一样，也要先取对应的Segment。从Segment中取出value，value不为空继续判断是否超过缓存过期时间，若元素可用，更新读队列，最后清除GC和过期数据。</p><div class="hljs"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-meta">@Nullable</span> <span class="hljs-function">V <span class="hljs-title">getIfPresent</span><span class="hljs-params">(Object key)</span> </span>&#123;  <span class="hljs-keyword">int</span> hash = hash(checkNotNull(key));  V value = segmentFor(hash).get(key, hash);  <span class="hljs-keyword">if</span> (value == <span class="hljs-keyword">null</span>) &#123;    globalStatsCounter.recordMisses(<span class="hljs-number">1</span>);  &#125; <span class="hljs-keyword">else</span> &#123;    globalStatsCounter.recordHits(<span class="hljs-number">1</span>);  &#125;  <span class="hljs-keyword">return</span> value;&#125;<span class="hljs-comment">// `getLiveEntry`方法传入当前时间，过期返回空</span><span class="hljs-function">V <span class="hljs-title">get</span><span class="hljs-params">(Object key, <span class="hljs-keyword">int</span> hash)</span> </span>&#123;  <span class="hljs-keyword">try</span> &#123;    <span class="hljs-keyword">if</span> (count != <span class="hljs-number">0</span>) &#123; <span class="hljs-comment">// read-volatile</span>      <span class="hljs-keyword">long</span> now = map.ticker.read();      ReferenceEntry&lt;K, V&gt; e = getLiveEntry(key, hash, now);      <span class="hljs-keyword">if</span> (e == <span class="hljs-keyword">null</span>) &#123;        <span class="hljs-keyword">return</span> <span class="hljs-keyword">null</span>;      &#125;      V value = e.getValueReference().get();      <span class="hljs-keyword">if</span> (value != <span class="hljs-keyword">null</span>) &#123; <span class="hljs-comment">// 未被GC回收</span>        <span class="hljs-comment">// 放入recencyQueue队列中</span>        recordRead(e, now);        <span class="hljs-comment">// 当前时间和写入的时间超过刷新间隔，重新加载value</span>        <span class="hljs-keyword">return</span> scheduleRefresh(e, e.getKey(), hash, value, now, map.defaultLoader);      &#125;      <span class="hljs-comment">// 垃圾回收</span>      tryDrainReferenceQueues();    &#125;    <span class="hljs-keyword">return</span> <span class="hljs-keyword">null</span>;  &#125; <span class="hljs-keyword">finally</span> &#123;    <span class="hljs-comment">// 清超时和GC数据</span>    postReadCleanup();  &#125;&#125;<span class="hljs-function">ReferenceEntry&lt;K, V&gt; <span class="hljs-title">getLiveEntry</span><span class="hljs-params">(Object key, <span class="hljs-keyword">int</span> hash, <span class="hljs-keyword">long</span> now)</span> </span>&#123;  ReferenceEntry&lt;K, V&gt; e = getEntry(key, hash);  <span class="hljs-keyword">if</span> (e == <span class="hljs-keyword">null</span>) &#123;    <span class="hljs-keyword">return</span> <span class="hljs-keyword">null</span>;  &#125; <span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span> (map.isExpired(e, now)) &#123; <span class="hljs-comment">// 缓存已过期</span>    tryExpireEntries(now);    <span class="hljs-keyword">return</span> <span class="hljs-keyword">null</span>;  &#125;  <span class="hljs-keyword">return</span> e;&#125;</code></pre></div><h3 id="参考文档"><a href="#参考文档" class="headerlink" title="参考文档"></a>参考文档</h3><p><a href="https://www.cnblogs.com/aspirant/p/11734918.html" target="_blank" rel="noopener">Guava cacha 机制及源码分析</a></p><p><a href="https://www.jianshu.com/p/2502e8301ec7" target="_blank" rel="noopener">guava Cache源码分析（二）</a></p>]]></content>
    
    
    <categories>
      
      <category>Java</category>
      
    </categories>
    
    
    <tags>
      
      <tag>Guava</tag>
      
      <tag>Cache</tag>
      
      <tag>缓存</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>漫谈Guava之Cache（一）</title>
    <link href="/post/96f462f9.html"/>
    <url>/post/96f462f9.html</url>
    
    <content type="html"><![CDATA[<p>计算机的世界中，缓存无处不在，最常见的如CPU高速缓存，<code>CPU Cache</code>是用于减少<a href="https://zh.wikipedia.org/wiki/中央处理器" target="_blank" rel="noopener">处理器</a>访问内存所需平均时间的部件。在金字塔式<a href="https://zh.wikipedia.org/w/index.php?title=存储体系&action=edit&redlink=1" target="_blank" rel="noopener">存储体系</a>中它位于自顶向下的第二层，仅次于<a href="https://zh.wikipedia.org/wiki/寄存器" target="_blank" rel="noopener">CPU寄存器</a>。其容量远小于<a href="https://zh.wikipedia.org/wiki/内存" target="_blank" rel="noopener">内存</a>，但速度却可以接近处理器的频率。</p><p>当处理器发出内存访问请求时，会先查看缓存内是否有请求数据。如果存在（命中），则不经访问内存直接返回该数据；如果不存在（失效），则要先把内存中的相应数据载入缓存，再将其返回处理器。</p><p>这和我们日常开发中的很多场景相似，例如调用第三方接口取数据，微服务间取数据，甚至从数据库中取数据。这些IO操作所消耗的时间对于CPU来说是很久的。如果这些数据是读多写少，也就是说数据不会轻易改变，那我们可以把这部分数据进行缓存，取数据前先访问缓存，在缓存中查不到时再走原来的逻辑。</p><a id="more"></a><p>日常开发中我们可能会使用<code>ConcurrentHashMap</code>存数据，替代缓存使用，简单场景这样使用没什么问题，只是<code>ConcurrentHashMap</code>设计的目的不是做缓存，所以很多Cache应该有的很多功能，<code>ConcurrentHashMap</code>并不直接支持，类如设置key的过期时间，内存不足时释放缓存等。</p><p>本文介绍Guava的Cache使用，为该系列的第一部分。</p><p>我先写一个简单的例子，创建一个缓存，该缓存最多存100个元素，超时时间为10分钟</p><div class="hljs"><pre><code class="hljs java">Cache&lt;String, String&gt; cache = CacheBuilder.newBuilder()            .maximumSize(<span class="hljs-number">100</span>)            .expireAfterWrite(<span class="hljs-number">10</span>, TimeUnit.MINUTES)            .build();cache.getIfPresent(key)cache.get(key, () -&gt; defaultValue)</code></pre></div><h3 id="CacheLoader"><a href="#CacheLoader" class="headerlink" title="CacheLoader"></a>CacheLoader</h3><p>上文中的<code>cache.get(key, () -&gt; defaultValue)</code>会在<code>cache</code>中没这条数据的情况下，根据我们写的策略重新计算，并将数据保存在缓存中一份。对于这种情况可以使用<code>CacheLoader</code>来统一处理。</p><div class="hljs"><pre><code class="hljs java">LoadingCache&lt;Key, Graph&gt; graphs = CacheBuilder.newBuilder()      .expireAfterAccess(<span class="hljs-number">10</span>, TimeUnit.MINUTES)      .maximumSize(<span class="hljs-number">1000</span>)      .build(          <span class="hljs-keyword">new</span> CacheLoader&lt;Key, Graph&gt;() &#123;            <span class="hljs-function"><span class="hljs-keyword">public</span> Graph <span class="hljs-title">load</span><span class="hljs-params">(Key key)</span> </span>&#123; <span class="hljs-comment">// no checked exception</span>              <span class="hljs-keyword">return</span> createExpensiveGraph(key);            &#125;          &#125;);...<span class="hljs-keyword">return</span> graphs.getUnchecked(key);</code></pre></div><h2 id="三种淘汰策略"><a href="#三种淘汰策略" class="headerlink" title="三种淘汰策略"></a>三种淘汰策略</h2><blockquote><p>Guava provides three basic types of eviction: size-based eviction, time-based eviction, and reference-based eviction.</p></blockquote><h3 id="容量大小"><a href="#容量大小" class="headerlink" title="容量大小"></a>容量大小</h3><p>前文使用的<code>CacheBuilder.maximumSize(long)</code> 便可限制最大缓存数量。<br>如缓冲的每条数据有不同的权重的话可自己写<code>Weigher</code>策略，并设置最大<code>重量</code>。</p><div class="hljs"><pre><code class="hljs java">LoadingCache&lt;Key, Graph&gt; graphs = CacheBuilder.newBuilder()       .maximumWeight(<span class="hljs-number">100000</span>)       .weigher(<span class="hljs-keyword">new</span> Weigher&lt;Key, Graph&gt;() &#123;          <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">int</span> <span class="hljs-title">weigh</span><span class="hljs-params">(Key k, Graph g)</span> </span>&#123;            <span class="hljs-keyword">return</span> g.vertices().size();          &#125;        &#125;)       .build(           <span class="hljs-keyword">new</span> CacheLoader&lt;Key, Graph&gt;() &#123;             <span class="hljs-function"><span class="hljs-keyword">public</span> Graph <span class="hljs-title">load</span><span class="hljs-params">(Key key)</span> </span>&#123; <span class="hljs-comment">// no checked exception</span>               <span class="hljs-keyword">return</span> createExpensiveGraph(key);             &#125;           &#125;);</code></pre></div><h3 id="时间"><a href="#时间" class="headerlink" title="时间"></a>时间</h3><p><code>CacheBuilder</code> provides two approaches to timed eviction:</p><ul><li><code>expireAfterAccess(long, TimeUnit)</code> 读或写多长时间后，缓存失效</li><li><code>expireAfterWrite(long, TimeUnit)</code> 值更改后多长时间后，缓存失效</li></ul><h3 id="引用"><a href="#引用" class="headerlink" title="引用"></a>引用</h3><p>使用JVM的垃圾回收，可被动的缓存失效。</p><ul><li><code>CacheBuilder.weakKeys()</code> 对key使用弱引用包装</li><li><code>CacheBuilder.weakValues()</code> 对value使用弱引用包装</li><li><code>CacheBuilder.softValues()</code> 对soft使用软引用包装</li></ul><h2 id="Removal-Listeners"><a href="#Removal-Listeners" class="headerlink" title="Removal Listeners"></a>Removal Listeners</h2><p>如果我们想对淘汰的元素进行处理，我们可以写一个<code>RemovalListener</code>对Cache进行监听。例如我们将DB连接存入cache中，当缓存失效时，我们需要关闭数据库连接，这部分逻辑就可以通过<code>RemovalListener</code>来完成</p><div class="hljs"><pre><code class="hljs java">CacheLoader&lt;Key, DatabaseConnection&gt; loader = <span class="hljs-keyword">new</span> CacheLoader&lt;Key, DatabaseConnection&gt; () &#123;  <span class="hljs-function"><span class="hljs-keyword">public</span> DatabaseConnection <span class="hljs-title">load</span><span class="hljs-params">(Key key)</span> <span class="hljs-keyword">throws</span> Exception </span>&#123;    <span class="hljs-keyword">return</span> openConnection(key);  &#125;&#125;;RemovalListener&lt;Key, DatabaseConnection&gt; removalListener = <span class="hljs-keyword">new</span> RemovalListener&lt;Key, DatabaseConnection&gt;() &#123;  <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">onRemoval</span><span class="hljs-params">(RemovalNotification&lt;Key, DatabaseConnection&gt; removal)</span> </span>&#123;    DatabaseConnection conn = removal.getValue();    conn.close(); <span class="hljs-comment">// tear down properly</span>  &#125;&#125;;CacheBuilder.newBuilder()  .expireAfterWrite(<span class="hljs-number">2</span>, TimeUnit.MINUTES)  .removalListener(removalListener)  .build(loader);</code></pre></div><h2 id="Refresh"><a href="#Refresh" class="headerlink" title="Refresh"></a>Refresh</h2><p>对于失效性比较强的元素，我们可以配置刷新的策略。刷新和淘汰不同，刷新元素是可以是异步操作，刷新未完成时返回还是原数据，刷新成功则返回新数据。</p><div class="hljs"><pre><code class="hljs java"><span class="hljs-comment">// Some keys don't need refreshing, and we want refreshes to be done asynchronously.</span>LoadingCache&lt;Key, Graph&gt; graphs = CacheBuilder.newBuilder()       .maximumSize(<span class="hljs-number">1000</span>)       .refreshAfterWrite(<span class="hljs-number">1</span>, TimeUnit.MINUTES)       .build(           <span class="hljs-keyword">new</span> CacheLoader&lt;Key, Graph&gt;() &#123;             <span class="hljs-function"><span class="hljs-keyword">public</span> Graph <span class="hljs-title">load</span><span class="hljs-params">(Key key)</span> </span>&#123; <span class="hljs-comment">// no checked exception</span>               <span class="hljs-keyword">return</span> getGraphFromDatabase(key);             &#125;             <span class="hljs-function"><span class="hljs-keyword">public</span> ListenableFuture&lt;Graph&gt; <span class="hljs-title">reload</span><span class="hljs-params">(<span class="hljs-keyword">final</span> Key key, Graph prevGraph)</span> </span>&#123;               <span class="hljs-keyword">if</span> (neverNeedsRefresh(key)) &#123;                 <span class="hljs-keyword">return</span> Futures.immediateFuture(prevGraph);               &#125; <span class="hljs-keyword">else</span> &#123;                 <span class="hljs-comment">// asynchronous!</span>                 ListenableFutureTask&lt;Graph&gt; task = ListenableFutureTask.create(<span class="hljs-keyword">new</span> Callable&lt;Graph&gt;() &#123;                   <span class="hljs-function"><span class="hljs-keyword">public</span> Graph <span class="hljs-title">call</span><span class="hljs-params">()</span> </span>&#123;                     <span class="hljs-keyword">return</span> getGraphFromDatabase(key);                   &#125;                 &#125;);                 executor.execute(task);                 <span class="hljs-keyword">return</span> task;               &#125;             &#125;           &#125;);</code></pre></div>]]></content>
    
    
    <categories>
      
      <category>Java</category>
      
    </categories>
    
    
    <tags>
      
      <tag>Guava</tag>
      
      <tag>Cache</tag>
      
      <tag>缓存</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>Spring Cloud Gateway 分布式限流</title>
    <link href="/post/c7e397c2.html"/>
    <url>/post/c7e397c2.html</url>
    
    <content type="html"><![CDATA[<p>前文我们介绍过通过Guava中的RateLimiter进行限流，但Guava中的工具只适用同一JVM中的操作，想要在分布式环境中进行接口限流我们还需要引入中间件，本文介绍基于Redis的接口限流。</p><a id="more"></a><h3 id="Spring-Cloud-Gateway"><a href="#Spring-Cloud-Gateway" class="headerlink" title="Spring Cloud Gateway"></a>Spring Cloud Gateway</h3><p><code>Spring Cloud Gateway</code>为Spring官方基于Spring 5.0、Spring Boot2.0和Project Reactor等技术开发的网关，限流作为网关应用中常用功能，<code>Gateway</code>也提供了一个基于Redis的分布式过滤器，简单配置，即可在网关层进行流量限制。</p><blockquote><p>注：本文的<code>Spring Cloud</code> 版本为<code>Greenwich.SR1</code></p></blockquote><p><code>Spring Cloud Gateway</code>的路由配置支持配置文件和Java代码配置，为了直观展示源码，本文使用Java代码编写路由。</p><ul><li>RedisRateLimiter 基于Redis的过滤器</li><li>setBurstCapacity() 设置令牌桶的容量</li><li>setReplenishRate() 设置每秒可取的令牌数量</li><li>setKeyResolver(pathKeyResolver) 配置过滤key的策略，本文使用路径作为过滤的key</li></ul><div class="hljs"><pre><code class="hljs java"><span class="hljs-meta">@Component</span><span class="hljs-keyword">public</span> <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">PathKeyResolver</span> <span class="hljs-keyword">implements</span> <span class="hljs-title">KeyResolver</span> </span>&#123;    <span class="hljs-meta">@Override</span>    <span class="hljs-function"><span class="hljs-keyword">public</span> Mono&lt;String&gt; <span class="hljs-title">resolve</span><span class="hljs-params">(ServerWebExchange exchange)</span> </span>&#123;        <span class="hljs-keyword">return</span> Mono.just(exchange.getRequest().getPath().toString());    &#125;&#125;<span class="hljs-meta">@Configuration</span><span class="hljs-keyword">public</span> <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">RouteLocatorConfig</span> </span>&#123;    <span class="hljs-meta">@Autowired</span>    <span class="hljs-keyword">private</span> PathKeyResolver pathKeyResolver;    <span class="hljs-meta">@Bean</span>    <span class="hljs-function"><span class="hljs-keyword">public</span> RouteLocator <span class="hljs-title">customerRouteLocator</span><span class="hljs-params">(RouteLocatorBuilder builder)</span> </span>&#123;        <span class="hljs-keyword">return</span> builder.routes()                .route(<span class="hljs-string">"admin"</span>, r -&gt; r.path(<span class="hljs-string">"/admin/**"</span>)                        .filters(f -&gt; f.requestRateLimiter()                                .rateLimiter(RedisRateLimiter<span class="hljs-class">.<span class="hljs-keyword">class</span>, <span class="hljs-title">c</span> -&gt; <span class="hljs-title">c</span>.<span class="hljs-title">setBurstCapacity</span>(1).<span class="hljs-title">setReplenishRate</span>(1))</span><span class="hljs-class">                                .<span class="hljs-title">configure</span>(<span class="hljs-title">c</span> -&gt; <span class="hljs-title">c</span>.<span class="hljs-title">setKeyResolver</span>(<span class="hljs-title">pathKeyResolver</span>))</span><span class="hljs-class">                        )</span>                        .uri("lb://ADMIN")                )                .build();    &#125;&#125;</code></pre></div><p>RouteLocator配置完成后限流器就可以正常使用了，每个路径符合<code>/admin/**</code>的请求都会经过<code>RequestRateLimiterGatewayFilterFactory</code>的<code>apply()</code>方法。</p><div class="hljs"><pre><code class="hljs java"><span class="hljs-meta">@Override</span><span class="hljs-function"><span class="hljs-keyword">public</span> GatewayFilter <span class="hljs-title">apply</span><span class="hljs-params">(Config config)</span> </span>&#123;   <span class="hljs-comment">// 取过滤key的策略，我们这里使用上文自定义的 PathKeyResolver</span>   KeyResolver resolver = getOrDefault(config.keyResolver, defaultKeyResolver);   <span class="hljs-comment">// 限流器，我们这里使用 RedisRateLimiter</span>   RateLimiter&lt;Object&gt; limiter = getOrDefault(config.rateLimiter,         defaultRateLimiter);   <span class="hljs-comment">// 解析的key为空时是否拦截 默认拦截</span>   <span class="hljs-keyword">boolean</span> denyEmpty = getOrDefault(config.denyEmptyKey, <span class="hljs-keyword">this</span>.denyEmptyKey);   HttpStatusHolder emptyKeyStatus = HttpStatusHolder         .parse(getOrDefault(config.emptyKeyStatus, <span class="hljs-keyword">this</span>.emptyKeyStatusCode));   <span class="hljs-keyword">return</span> (exchange, chain) -&gt; &#123;      Route route = exchange            .getAttribute(ServerWebExchangeUtils.GATEWAY_ROUTE_ATTR); <span class="hljs-comment">// 根据我们写的过滤策略计算出key</span>      <span class="hljs-keyword">return</span> resolver.resolve(exchange).defaultIfEmpty(EMPTY_KEY).flatMap(key -&gt; &#123;         <span class="hljs-keyword">if</span> (EMPTY_KEY.equals(key)) &#123;            <span class="hljs-keyword">if</span> (denyEmpty) &#123;               setResponseStatus(exchange, emptyKeyStatus);               <span class="hljs-keyword">return</span> exchange.getResponse().setComplete();            &#125;            <span class="hljs-keyword">return</span> chain.filter(exchange);         &#125;         <span class="hljs-comment">// key 不为空时走限流逻辑，这里使用的RateLimiter的isAllowed()方法</span>         <span class="hljs-keyword">return</span> limiter.isAllowed(route.getId(), key).flatMap(response -&gt; &#123;            <span class="hljs-keyword">for</span> (Map.Entry&lt;String, String&gt; header : response.getHeaders()                  .entrySet()) &#123;               exchange.getResponse().getHeaders().add(header.getKey(),                     header.getValue());            &#125;             <span class="hljs-keyword">if</span> (response.isAllowed()) &#123;               <span class="hljs-keyword">return</span> chain.filter(exchange);            &#125;            setResponseStatus(exchange, config.getStatusCode());            <span class="hljs-keyword">return</span> exchange.getResponse().setComplete();         &#125;);      &#125;);   &#125;;&#125;</code></pre></div><p><code>resolver.resolve(exchange)</code>实际调用<code>RedisRateLimiter</code>限流器的<code>isAllowed</code>方法</p><div class="hljs"><pre><code class="hljs java"><span class="hljs-meta">@Override</span><span class="hljs-meta">@SuppressWarnings</span>(<span class="hljs-string">"unchecked"</span>)<span class="hljs-function"><span class="hljs-keyword">public</span> Mono&lt;Response&gt; <span class="hljs-title">isAllowed</span><span class="hljs-params">(String routeId, String id)</span> </span>&#123;   <span class="hljs-keyword">if</span> (!<span class="hljs-keyword">this</span>.initialized.get()) &#123;      <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> IllegalStateException(<span class="hljs-string">"RedisRateLimiter is not initialized"</span>);   &#125;   Config routeConfig = loadConfiguration(routeId);   <span class="hljs-comment">// How many requests per second do you want a user to be allowed to do?</span>    <span class="hljs-comment">// 每秒可取多少令牌</span>   <span class="hljs-keyword">int</span> replenishRate = routeConfig.getReplenishRate();   <span class="hljs-comment">// How much bursting do you want to allow?</span>    <span class="hljs-comment">// 令牌桶的容量</span>   <span class="hljs-keyword">int</span> burstCapacity = routeConfig.getBurstCapacity();   <span class="hljs-keyword">try</span> &#123;       <span class="hljs-comment">// 生成redis的key,request_rate_limiter.&#123;id&#125;.tokens和request_rate_limiter.&#123;id&#125;.timestamp</span>      List&lt;String&gt; keys = getKeys(id);      <span class="hljs-comment">// The arguments to the LUA script. time() returns unixtime in seconds.</span>      List&lt;String&gt; scriptArgs = Arrays.asList(replenishRate + <span class="hljs-string">""</span>,            burstCapacity + <span class="hljs-string">""</span>, Instant.now().getEpochSecond() + <span class="hljs-string">""</span>, <span class="hljs-string">"1"</span>);      <span class="hljs-comment">// allowed, tokens_left = redis.eval(SCRIPT, keys, args)</span>       <span class="hljs-comment">// redis中支持lua脚本原子性执行,该脚本在Bean初始化时装配进来</span>      Flux&lt;List&lt;Long&gt;&gt; flux = <span class="hljs-keyword">this</span>.redisTemplate.execute(<span class="hljs-keyword">this</span>.script, keys,            scriptArgs);      <span class="hljs-comment">// .log("redisratelimiter", Level.FINER);</span>      <span class="hljs-keyword">return</span> flux.onErrorResume(throwable -&gt; Flux.just(Arrays.asList(<span class="hljs-number">1L</span>, -<span class="hljs-number">1L</span>)))            .reduce(<span class="hljs-keyword">new</span> ArrayList&lt;Long&gt;(), (longs, l) -&gt; &#123;               longs.addAll(l);               <span class="hljs-keyword">return</span> longs;            &#125;).map(results -&gt; &#123;          <span class="hljs-comment">// 根据lua脚本返回值判断是否允许通过</span>               <span class="hljs-keyword">boolean</span> allowed = results.get(<span class="hljs-number">0</span>) == <span class="hljs-number">1L</span>;               Long tokensLeft = results.get(<span class="hljs-number">1</span>);               Response response = <span class="hljs-keyword">new</span> Response(allowed,                     getHeaders(routeConfig, tokensLeft));               <span class="hljs-keyword">if</span> (log.isDebugEnabled()) &#123;                  log.debug(<span class="hljs-string">"response: "</span> + response);               &#125;               <span class="hljs-keyword">return</span> response;            &#125;);   &#125;   <span class="hljs-keyword">catch</span> (Exception e) &#123;      <span class="hljs-comment">/*</span><span class="hljs-comment">       * We don't want a hard dependency on Redis to allow traffic. Make sure to set</span><span class="hljs-comment">       * an alert so you know if this is happening too much. Stripe's observed</span><span class="hljs-comment">       * failure rate is 0.01%.</span><span class="hljs-comment">       */</span>      log.error(<span class="hljs-string">"Error determining if user allowed from redis"</span>, e);   &#125;   <span class="hljs-keyword">return</span> Mono.just(<span class="hljs-keyword">new</span> Response(<span class="hljs-keyword">true</span>, getHeaders(routeConfig, -<span class="hljs-number">1L</span>)));&#125;</code></pre></div><p><code>this.script</code>为Bean初始化时装配进来，<code>GatewayRedisAutoConfiguration#redisRequestRateLimiterScript</code>将lua脚本从文件中读取并注册成Bean</p><div class="hljs"><pre><code class="hljs java"><span class="hljs-meta">@Bean</span><span class="hljs-meta">@SuppressWarnings</span>(<span class="hljs-string">"unchecked"</span>)<span class="hljs-function"><span class="hljs-keyword">public</span> RedisScript <span class="hljs-title">redisRequestRateLimiterScript</span><span class="hljs-params">()</span> </span>&#123;   DefaultRedisScript redisScript = <span class="hljs-keyword">new</span> DefaultRedisScript&lt;&gt;();   redisScript.setScriptSource(<span class="hljs-keyword">new</span> ResourceScriptSource(         <span class="hljs-keyword">new</span> ClassPathResource(<span class="hljs-string">"META-INF/scripts/request_rate_limiter.lua"</span>)));   redisScript.setResultType(List<span class="hljs-class">.<span class="hljs-keyword">class</span>)</span>;   <span class="hljs-keyword">return</span> redisScript;&#125;</code></pre></div><p><code>request_rate_limiter.lua</code>中的代码，与前文所讲的Guava中的RateLimiter实现大同小异</p><div class="hljs"><pre><code class="hljs lua"><span class="hljs-keyword">local</span> tokens_key = KEYS[<span class="hljs-number">1</span>]<span class="hljs-keyword">local</span> timestamp_key = KEYS[<span class="hljs-number">2</span>]<span class="hljs-comment">--redis.log(redis.LOG_WARNING, "tokens_key " .. tokens_key)</span><span class="hljs-keyword">local</span> rate = <span class="hljs-built_in">tonumber</span>(ARGV[<span class="hljs-number">1</span>])<span class="hljs-keyword">local</span> capacity = <span class="hljs-built_in">tonumber</span>(ARGV[<span class="hljs-number">2</span>])<span class="hljs-keyword">local</span> now = <span class="hljs-built_in">tonumber</span>(ARGV[<span class="hljs-number">3</span>])<span class="hljs-keyword">local</span> requested = <span class="hljs-built_in">tonumber</span>(ARGV[<span class="hljs-number">4</span>])<span class="hljs-keyword">local</span> fill_time = capacity/rate<span class="hljs-comment">-- 计算超时时间</span><span class="hljs-keyword">local</span> ttl = <span class="hljs-built_in">math</span>.<span class="hljs-built_in">floor</span>(fill_time*<span class="hljs-number">2</span>)<span class="hljs-comment">--redis.log(redis.LOG_WARNING, "rate " .. ARGV[1])</span><span class="hljs-comment">--redis.log(redis.LOG_WARNING, "capacity " .. ARGV[2])</span><span class="hljs-comment">--redis.log(redis.LOG_WARNING, "now " .. ARGV[3])</span><span class="hljs-comment">--redis.log(redis.LOG_WARNING, "requested " .. ARGV[4])</span><span class="hljs-comment">--redis.log(redis.LOG_WARNING, "filltime " .. fill_time)</span><span class="hljs-comment">--redis.log(redis.LOG_WARNING, "ttl " .. ttl)</span><span class="hljs-keyword">local</span> last_tokens = <span class="hljs-built_in">tonumber</span>(redis.call(<span class="hljs-string">"get"</span>, tokens_key))<span class="hljs-keyword">if</span> last_tokens == <span class="hljs-literal">nil</span> <span class="hljs-keyword">then</span>  last_tokens = capacity<span class="hljs-keyword">end</span><span class="hljs-comment">--redis.log(redis.LOG_WARNING, "last_tokens " .. last_tokens)</span><span class="hljs-comment">-- 最后刷新时间</span><span class="hljs-keyword">local</span> last_refreshed = <span class="hljs-built_in">tonumber</span>(redis.call(<span class="hljs-string">"get"</span>, timestamp_key))<span class="hljs-keyword">if</span> last_refreshed == <span class="hljs-literal">nil</span> <span class="hljs-keyword">then</span>  last_refreshed = <span class="hljs-number">0</span><span class="hljs-keyword">end</span><span class="hljs-comment">--redis.log(redis.LOG_WARNING, "last_refreshed " .. last_refreshed)</span><span class="hljs-comment">-- 和上次请求的时间差</span><span class="hljs-keyword">local</span> delta = <span class="hljs-built_in">math</span>.<span class="hljs-built_in">max</span>(<span class="hljs-number">0</span>, now-last_refreshed)<span class="hljs-comment">-- 计算令牌桶中token数量</span><span class="hljs-keyword">local</span> filled_tokens = <span class="hljs-built_in">math</span>.<span class="hljs-built_in">min</span>(capacity, last_tokens+(delta*rate))<span class="hljs-comment">-- 桶中令牌数量是否足够</span><span class="hljs-keyword">local</span> allowed = filled_tokens &gt;= requested<span class="hljs-keyword">local</span> new_tokens = filled_tokens<span class="hljs-keyword">local</span> allowed_num = <span class="hljs-number">0</span><span class="hljs-keyword">if</span> allowed <span class="hljs-keyword">then</span>  <span class="hljs-comment">-- 桶中令牌被取走</span>  new_tokens = filled_tokens - requested  allowed_num = <span class="hljs-number">1</span><span class="hljs-keyword">end</span><span class="hljs-comment">--redis.log(redis.LOG_WARNING, "delta " .. delta)</span><span class="hljs-comment">--redis.log(redis.LOG_WARNING, "filled_tokens " .. filled_tokens)</span><span class="hljs-comment">--redis.log(redis.LOG_WARNING, "allowed_num " .. allowed_num)</span><span class="hljs-comment">--redis.log(redis.LOG_WARNING, "new_tokens " .. new_tokens)</span><span class="hljs-comment">-- 剩余令牌数量</span>redis.call(<span class="hljs-string">"setex"</span>, tokens_key, ttl, new_tokens)<span class="hljs-comment">-- 刷新时间设为当前</span>redis.call(<span class="hljs-string">"setex"</span>, timestamp_key, ttl, now)<span class="hljs-keyword">return</span> &#123; allowed_num, new_tokens &#125;</code></pre></div><h3 id="Redisson"><a href="#Redisson" class="headerlink" title="Redisson"></a>Redisson</h3><p>Redisson是一个在Redis的基础上实现的Java驻内存数据网格（In-Memory Data Grid）。它不仅提供了一系列的分布式的Java常用对象，还提供了许多分布式服务。本文使用Redisson提供的RRateLimiter实现分布式限流。</p><div class="hljs"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">RRateLimiterDemo</span> </span>&#123;    <span class="hljs-keyword">private</span> <span class="hljs-keyword">static</span> RRateLimiter rateLimiter = RedisClient.INSTANCE.getRateLimiter(<span class="hljs-string">"rate_limiter"</span>);    <span class="hljs-keyword">static</span> &#123;        rateLimiter.trySetRate(RateType.PER_CLIENT, <span class="hljs-number">2</span>, <span class="hljs-number">1</span>, RateIntervalUnit.SECONDS);    &#125;    <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">void</span> <span class="hljs-title">main</span><span class="hljs-params">(String[] args)</span> <span class="hljs-keyword">throws</span> InterruptedException </span>&#123;        ExecutorService executorService = Executors.newFixedThreadPool(<span class="hljs-number">100</span>);        <span class="hljs-comment">// 5个线程同时调用，模拟多个请求同时访问</span>        IntStream.rangeClosed(<span class="hljs-number">1</span>, <span class="hljs-number">5</span>).forEach(e -&gt; &#123;            executorService.submit(() -&gt; acquire(e));        &#125;);    &#125;    <span class="hljs-function"><span class="hljs-keyword">private</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">void</span> <span class="hljs-title">acquire</span><span class="hljs-params">(<span class="hljs-keyword">int</span> i)</span> </span>&#123;        <span class="hljs-comment">// acquire方法默认取一个令牌，桶中令牌不够的话会阻塞线程</span>        rateLimiter.acquire();        System.out.printf(<span class="hljs-string">"当前系统时间:%s\n"</span>, LocalDateTime.now());    &#125;&#125;</code></pre></div><p>静态代码块中为设置限流器的速率，每秒生成两个令牌。下面是控制台打印的结果，可以看出来时间间隔和预想的一致。</p><p>该限流器也是通过lua脚本实现限流，实现逻辑与之前介绍的大同小异，我就不做过多展示了。</p><div class="hljs"><pre><code class="hljs angelscript">当前系统时间:<span class="hljs-number">2020</span><span class="hljs-number">-04</span><span class="hljs-number">-08</span>T16:<span class="hljs-number">49</span>:<span class="hljs-number">40.425</span>当前系统时间:<span class="hljs-number">2020</span><span class="hljs-number">-04</span><span class="hljs-number">-08</span>T16:<span class="hljs-number">49</span>:<span class="hljs-number">40.425</span>当前系统时间:<span class="hljs-number">2020</span><span class="hljs-number">-04</span><span class="hljs-number">-08</span>T16:<span class="hljs-number">49</span>:<span class="hljs-number">41.420</span>当前系统时间:<span class="hljs-number">2020</span><span class="hljs-number">-04</span><span class="hljs-number">-08</span>T16:<span class="hljs-number">49</span>:<span class="hljs-number">41.421</span>当前系统时间:<span class="hljs-number">2020</span><span class="hljs-number">-04</span><span class="hljs-number">-08</span>T16:<span class="hljs-number">49</span>:<span class="hljs-number">42.422</span></code></pre></div><h3 id="结语"><a href="#结语" class="headerlink" title="结语"></a>结语</h3><p>Redis限流器不仅仅今天介绍的这两种，分布式限流器也不限于Redis，但通过这两篇介绍限流器的文章可以发现他们的原理都是相通的，我们了解其中的思想即可。</p>]]></content>
    
    
    <categories>
      
      <category>Java</category>
      
    </categories>
    
    
    <tags>
      
      <tag>limiter</tag>
      
      <tag>Spring Cloud</tag>
      
      <tag>Gateway</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>漫谈限流器</title>
    <link href="/post/38378326.html"/>
    <url>/post/38378326.html</url>
    
    <content type="html"><![CDATA[<p>​    日常开发中经常会出现一瞬间接口被超频次访问的这种情况，例如电商秒杀活动，定点抢红包等。短时间高并发可能会拖慢系统的响应速度，引起网络超时，一个服务不可用进而影响其他服务也不可用，甚至导致雪崩。</p><p>​    限流是最直接有效的控制手段，将系统处理不过来的请求拦截在核心逻辑之外。限流器实现方法有<code>漏桶算法(Leaky Bucket),</code>令牌桶算法(Token Bucket)`等。</p><a id="more"></a><ul><li>漏斗算法 理解起来比较简单，水(即请求)流入固定大小的漏斗内，漏斗也已一定的速度流出水，当水满时停止流入,进行限流。</li><li>令牌桶算法 同样是固定大小的桶，定期往桶中投令牌，有新请求时需从桶中取令牌，当桶中无令牌可取时，则拒绝。</li></ul><p>本文使用<code>Guava</code>中的<code>RateLimiter</code>进行限流，我使用的Guava的版本为<code>28.2-jre</code>，不同版本api可能略有偏差。</p><div class="hljs"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">GuavaLimiterDemo</span> </span>&#123;    <span class="hljs-comment">// 每秒生成2个令牌</span>    <span class="hljs-keyword">private</span> <span class="hljs-keyword">static</span> RateLimiter rateLimiter = RateLimiter.create(<span class="hljs-number">2</span>);    <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">void</span> <span class="hljs-title">main</span><span class="hljs-params">(String[] args)</span><span class="hljs-keyword">throws</span> InterruptedException </span>&#123;        ExecutorService executorService = Executors.newFixedThreadPool(<span class="hljs-number">100</span>);        <span class="hljs-comment">// 5个线程同时调用，模拟多个请求同时访问</span>        IntStream.rangeClosed(<span class="hljs-number">1</span>, <span class="hljs-number">5</span>).forEach(e -&gt; &#123;            executorService.submit(() -&gt; acquire(e));        &#125;);    &#125;    <span class="hljs-function"><span class="hljs-keyword">private</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">double</span> <span class="hljs-title">acquire</span><span class="hljs-params">(<span class="hljs-keyword">int</span> i)</span></span>&#123;        <span class="hljs-comment">// acquire方法默认取一个令牌，桶中令牌不够的话会阻塞线程,返回值为线程阻塞时间</span>        <span class="hljs-keyword">double</span> acquire = rateLimiter.acquire();        System.out.printf(<span class="hljs-string">"当前系统时间:%s,等待秒数:%s\n"</span>,LocalDateTime.now(),acquire);        <span class="hljs-keyword">return</span> acquire;    &#125;&#125;</code></pre></div><p>​    调用静态方法<code>create()</code>创建限流器,参数为每秒生产令牌的数量</p><p>​    <code>acquire()</code>方法默认取一个令牌，可用调用重载的方法取多个令牌，如果桶中无令牌该方法会阻塞，开发过程中要注意大量线程挂起的问题，慎用。</p><p>​    <code>tryAcquire()</code>方法可解决上述问题，取一个令牌，如桶中无令牌直接返回false。<br><code>tryAcquire(int permits, long timeout, TimeUnit unit)</code>可传入超时时间，超过这段时间桶中还是无令牌可取的话返回false。</p><div class="hljs"><pre><code class="hljs angelscript">当前系统时间:<span class="hljs-number">2020</span><span class="hljs-number">-03</span><span class="hljs-number">-25</span>T13:<span class="hljs-number">30</span>:<span class="hljs-number">37.757</span>,等待秒数:<span class="hljs-number">0.0</span>当前系统时间:<span class="hljs-number">2020</span><span class="hljs-number">-03</span><span class="hljs-number">-25</span>T13:<span class="hljs-number">30</span>:<span class="hljs-number">38.195</span>,等待秒数:<span class="hljs-number">0.458741</span>当前系统时间:<span class="hljs-number">2020</span><span class="hljs-number">-03</span><span class="hljs-number">-25</span>T13:<span class="hljs-number">30</span>:<span class="hljs-number">38.694</span>,等待秒数:<span class="hljs-number">0.958729</span>当前系统时间:<span class="hljs-number">2020</span><span class="hljs-number">-03</span><span class="hljs-number">-25</span>T13:<span class="hljs-number">30</span>:<span class="hljs-number">39.194</span>,等待秒数:<span class="hljs-number">1.458717</span>当前系统时间:<span class="hljs-number">2020</span><span class="hljs-number">-03</span><span class="hljs-number">-25</span>T13:<span class="hljs-number">30</span>:<span class="hljs-number">39.695</span>,等待秒数:<span class="hljs-number">1.958704</span></code></pre></div><p>​    这是控制台打印的结果，每秒放入两个令牌的桶，同时有5个线程访问。基本每0.5秒放行一个请求。</p><h3 id="Guava-RateLimiter-实现原理"><a href="#Guava-RateLimiter-实现原理" class="headerlink" title="Guava RateLimiter 实现原理"></a>Guava RateLimiter 实现原理</h3><p><img src="https://img.yjll.art/img/20200327141557.png" srcset="/img/loading.gif" alt=""></p><p>​    RateLimiter限流器实现了两种算法<code>SmoothWarmingUp(漏斗算法)</code>和<code>SmoothBursty(令牌桶算法)</code>，默认使用<code>SmoothBursty</code>。</p><p>​    静态方法<code>RateLimiter.create()</code>默认使用子类<code>SmoothBursty</code>创建限流器</p><div class="hljs"><pre><code class="hljs java"><span class="hljs-comment">// permitsPerSecond 创建时传入的单位时间内发放令牌数量</span><span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> RateLimiter <span class="hljs-title">create</span><span class="hljs-params">(<span class="hljs-keyword">double</span> permitsPerSecond)</span> </span>&#123;    <span class="hljs-keyword">return</span> create(permitsPerSecond, SleepingStopwatch.createFromSystemTimer());  &#125;<span class="hljs-comment">// stopwatch guava实现的时间监视器 默认SleepingStopwatch.createFromSystemTimer()</span><span class="hljs-function"><span class="hljs-keyword">static</span> RateLimiter <span class="hljs-title">create</span><span class="hljs-params">(<span class="hljs-keyword">double</span> permitsPerSecond, SleepingStopwatch stopwatch)</span> </span>&#123;    <span class="hljs-comment">// maxBurstSeconds 最大缓存时间，默认1.0</span>    RateLimiter rateLimiter = <span class="hljs-keyword">new</span> SmoothBursty(stopwatch, <span class="hljs-number">1.0</span> <span class="hljs-comment">/* maxBurstSeconds */</span>);    <span class="hljs-comment">// 初始化时设置投放令牌的速率</span>    rateLimiter.setRate(permitsPerSecond);    <span class="hljs-keyword">return</span> rateLimiter;  &#125;</code></pre></div><p>​    <code>setRate</code>底层调用<code>doSetRate</code>设置速率</p><p>​    <code>setRate</code>为设置投放令牌的速率，调用时对以下值进行更改：</p><ul><li><p>storedPermits 当前令牌数量，速率更改时会根据新旧速率的比例对原桶中令牌的数量进行更改。初始化时为0</p></li><li><p>stableIntervalMicros 每生产一个令牌需要的毫秒数</p></li><li><p>maxPermits 桶中最多可存储的令牌数量，可根据maxBurstSeconds * permitsPerSecond 计算</p></li><li><p>nextFreeTicketMicros 下一次可取令牌的时间，当前时间早于该时间表示可取元素，与之的差表示放入令牌且不取出令牌的时间，进而再跟据stableIntervalMicros可算出该时间段内桶中增加了多少令牌(受限于桶的大小)</p></li></ul><p>​    在初始化时或手动更改速率时调用。</p><div class="hljs"><pre><code class="hljs java"><span class="hljs-comment">// permitsPerSecond 创建时传入的单位时间内发放令牌数量</span><span class="hljs-comment">// nowMicros 从创建到现在经过的时间</span><span class="hljs-function"><span class="hljs-keyword">final</span> <span class="hljs-keyword">void</span> <span class="hljs-title">doSetRate</span><span class="hljs-params">(<span class="hljs-keyword">double</span> permitsPerSecond, <span class="hljs-keyword">long</span> nowMicros)</span> </span>&#123;    resync(nowMicros);    <span class="hljs-keyword">double</span> stableIntervalMicros = SECONDS.toMicros(<span class="hljs-number">1L</span>) / permitsPerSecond;    <span class="hljs-keyword">this</span>.stableIntervalMicros = stableIntervalMicros;    doSetRate(permitsPerSecond, stableIntervalMicros);  &#125;<span class="hljs-comment">// stableIntervalMicros 每生产一个令牌，需要经过多少毫秒</span><span class="hljs-function"><span class="hljs-keyword">void</span> <span class="hljs-title">doSetRate</span><span class="hljs-params">(<span class="hljs-keyword">double</span> permitsPerSecond, <span class="hljs-keyword">double</span> stableIntervalMicros)</span> </span>&#123;      <span class="hljs-keyword">double</span> oldMaxPermits = <span class="hljs-keyword">this</span>.maxPermits;      maxPermits = maxBurstSeconds * permitsPerSecond;      <span class="hljs-keyword">if</span> (oldMaxPermits == Double.POSITIVE_INFINITY) &#123;        <span class="hljs-comment">// if we don't special-case this, we would get storedPermits == NaN, below</span>        storedPermits = maxPermits;      &#125; <span class="hljs-keyword">else</span> &#123;        storedPermits =            (oldMaxPermits == <span class="hljs-number">0.0</span>)                ? <span class="hljs-number">0.0</span> <span class="hljs-comment">// initial state</span>                : storedPermits * maxPermits / oldMaxPermits;      &#125;    &#125;</code></pre></div><p>​    速率更改或者从桶中取令牌时会调用<code>resync</code>方法</p><p>​    <code>resync(nowMicros)</code>将当前时间与<code>nextFreeTicketMicros</code>作比较，如当前时间在<code>nextFreeTicketMicros</code>之后，则重新计算桶中令牌的数量，并重置<code>nextFreeTicketMicros</code>的值。从这里可以看出Guava Limiter设计的绝妙之处，不需要单独开线程向桶中添加令牌，而是惰性的在取令牌时计算一下这时间内桶中添加了几个令牌，在根据桶中令牌数量进行限流。</p><div class="hljs"><pre><code class="hljs java"><span class="hljs-function"><span class="hljs-keyword">void</span> <span class="hljs-title">resync</span><span class="hljs-params">(<span class="hljs-keyword">long</span> nowMicros)</span> </span>&#123;  <span class="hljs-comment">// if nextFreeTicket is in the past, resync to now</span>  <span class="hljs-keyword">if</span> (nowMicros &gt; nextFreeTicketMicros) &#123;    <span class="hljs-keyword">double</span> newPermits = (nowMicros - nextFreeTicketMicros) / coolDownIntervalMicros();    <span class="hljs-comment">// 防止添加后的令牌数量超过桶的极限</span>    storedPermits = min(maxPermits, storedPermits + newPermits);    nextFreeTicketMicros = nowMicros;  &#125;&#125;</code></pre></div><p>​    取数据调用<code>acquire()</code>方法，桶中无令牌的话会阻塞</p><div class="hljs"><pre><code class="hljs java"><span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">double</span> <span class="hljs-title">acquire</span><span class="hljs-params">(<span class="hljs-keyword">int</span> permits)</span> </span>&#123;  <span class="hljs-comment">// 需等待的时间</span>  <span class="hljs-keyword">long</span> microsToWait = reserve(permits);  <span class="hljs-comment">// 线程挂起</span>  stopwatch.sleepMicrosUninterruptibly(microsToWait);  <span class="hljs-keyword">return</span> <span class="hljs-number">1.0</span> * microsToWait / SECONDS.toMicros(<span class="hljs-number">1L</span>);&#125;</code></pre></div><p>​    <code>acquire()</code>最终会调用<code>reserveEarliestAvailable()</code></p><p>​    令牌桶限流器的一个优势是可以减缓突发情况带来的问题，允许单次请求的令牌数量超出剩余令牌数量，但是多拿的令牌数要从后边的补，正所谓“前人挖坑，后人填坑”。</p><div class="hljs"><pre><code class="hljs java"><span class="hljs-function"><span class="hljs-keyword">final</span> <span class="hljs-keyword">long</span> <span class="hljs-title">reserveEarliestAvailable</span><span class="hljs-params">(<span class="hljs-keyword">int</span> requiredPermits, <span class="hljs-keyword">long</span> nowMicros)</span> </span>&#123;    <span class="hljs-comment">// 前文提到过，在取数据之前计算桶中令牌数量</span>    resync(nowMicros);    <span class="hljs-keyword">long</span> returnValue = nextFreeTicketMicros;    <span class="hljs-keyword">double</span> storedPermitsToSpend = min(requiredPermits, <span class="hljs-keyword">this</span>.storedPermits);    <span class="hljs-comment">// 计算突发情况多取的令牌数量</span>    <span class="hljs-keyword">double</span> freshPermits = requiredPermits - storedPermitsToSpend;    <span class="hljs-keyword">long</span> waitMicros =        storedPermitsToWaitTime(<span class="hljs-keyword">this</span>.storedPermits, storedPermitsToSpend)            + (<span class="hljs-keyword">long</span>) (freshPermits * stableIntervalMicros);<span class="hljs-comment">// 超发的情况，下一次可取令牌的时间往后延</span>    <span class="hljs-keyword">this</span>.nextFreeTicketMicros = LongMath.saturatedAdd(nextFreeTicketMicros, waitMicros);    <span class="hljs-keyword">this</span>.storedPermits -= storedPermitsToSpend;    <span class="hljs-keyword">return</span> returnValue;  &#125;</code></pre></div>]]></content>
    
    
    <categories>
      
      <category>Java</category>
      
    </categories>
    
    
    <tags>
      
      <tag>limiter</tag>
      
      <tag>Guava</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>Mysql主从配置</title>
    <link href="/post/776d488.html"/>
    <url>/post/776d488.html</url>
    
    <content type="html"><![CDATA[<p>主从复制是指一台服务器充当主数据库服务器，另一台或多台服务器充当从数据库服务器，主服务器中的数据自动复制到从服务器之中。对于多级复制，数据库服务器即可充当主机，也可充当从机。MySQL主从复制的基础是主服务器对数据库修改记录二进制日志，从服务器通过主服务器的二进制日志自动执行更新。</p><a id="more"></a><h3 id="主库配置"><a href="#主库配置" class="headerlink" title="主库配置"></a>主库配置</h3><div class="hljs"><pre><code class="hljs routeros">$ cat ./mysql-master/etc/my.cnf [mysqld]<span class="hljs-comment">## 设置server_id，一般设置为IP，注意要唯一</span><span class="hljs-attribute">server_id</span>=1  <span class="hljs-comment">## 复制过滤：也就是指定哪个数据库不用同步（mysql库一般不同步）</span><span class="hljs-attribute">binlog-ignore-db</span>=mysql  <span class="hljs-comment">## 开启二进制日志功能，可以随便取，最好有含义（关键就是这里了）</span><span class="hljs-attribute">log-bin</span>=replicas-mysql-bin  <span class="hljs-comment">## 为每个session分配的内存，在事务过程中用来存储二进制日志的缓存</span><span class="hljs-attribute">binlog_cache_size</span>=1M  <span class="hljs-comment">## 主从复制的格式（mixed,statement,row，默认格式是statement）</span><span class="hljs-attribute">binlog_format</span>=mixed  <span class="hljs-comment">## 二进制日志自动删除/过期的天数。默认值为0，表示不自动删除。</span><span class="hljs-attribute">expire_logs_days</span>=7  <span class="hljs-comment">## 跳过主从复制中遇到的所有错误或指定类型的错误，避免slave端复制中断。</span><span class="hljs-comment">## 如：1062错误是指一些主键重复，1032错误是因为主从数据库数据不一致</span><span class="hljs-attribute">slave_skip_errors</span>=1062<span class="hljs-comment"># 忽略大小写</span><span class="hljs-attribute">lower_case_table_names</span>=1</code></pre></div><p>添加同步账户</p><p><code>grant replication slave on *.* to &#39;slave&#39;@&#39;%&#39; identified by &#39;slave@123456&#39;;</code></p><p>查看主库状态</p><div class="hljs"><pre><code class="hljs asciidoc">mysql&gt; show master status;<span class="hljs-code">+---------------------------+</span>----------<span class="hljs-code">+--------------+</span>------------------<span class="hljs-code">+-------------------+</span>| File                      | Position | Binlog<span class="hljs-emphasis">_Do_</span>DB | Binlog<span class="hljs-emphasis">_Ignore_</span>DB | Executed<span class="hljs-emphasis">_Gtid_</span>Set |<span class="hljs-code">+---------------------------+</span>----------<span class="hljs-code">+--------------+</span>------------------<span class="hljs-code">+-------------------+</span>| replicas-mysql-bin.000003 |  2841451 |              | mysql            |                   |<span class="hljs-code">+---------------------------+</span>----------<span class="hljs-code">+--------------+</span>------------------<span class="hljs-code">+-------------------+</span>1 row in set (0.00 sec)</code></pre></div><blockquote><p>File &amp;&amp; Position 需记住配置从库时使用</p></blockquote><h3 id="从库配置"><a href="#从库配置" class="headerlink" title="从库配置"></a>从库配置</h3><div class="hljs"><pre><code class="hljs routeros">$ cat mysql-slave/etc/my.cnf [mysqld]<span class="hljs-comment">## 设置server_id，一般设置为IP，注意要唯一</span><span class="hljs-attribute">server_id</span>=101  <span class="hljs-comment">## 复制过滤：也就是指定哪个数据库不用同步（mysql库一般不同步）</span><span class="hljs-attribute">binlog-ignore-db</span>=mysql  <span class="hljs-comment">## 开启二进制日志功能，以备Slave作为其它Slave的Master时使用</span><span class="hljs-attribute">log-bin</span>=replicas-mysql-slave1-bin  <span class="hljs-comment">## 为每个session 分配的内存，在事务过程中用来存储二进制日志的缓存</span><span class="hljs-attribute">binlog_cache_size</span>=1M  <span class="hljs-comment">## 主从复制的格式（mixed,statement,row，默认格式是statement）</span><span class="hljs-attribute">binlog_format</span>=mixed  <span class="hljs-comment">## 二进制日志自动删除/过期的天数。默认值为0，表示不自动删除。</span><span class="hljs-attribute">expire_logs_days</span>=7  <span class="hljs-comment">## 跳过主从复制中遇到的所有错误或指定类型的错误，避免slave端复制中断。</span><span class="hljs-comment">## 如：1062错误是指一些主键重复，1032错误是因为主从数据库数据不一致</span><span class="hljs-attribute">slave_skip_errors</span>=1062  <span class="hljs-comment">## relay_log配置中继日志</span><span class="hljs-attribute">relay_log</span>=replicas-mysql-relay-bin  <span class="hljs-comment">## log_slave_updates表示slave将复制事件写进自己的二进制日志</span><span class="hljs-attribute">log_slave_updates</span>=1  <span class="hljs-comment">## 防止改变数据(除了特殊的线程)</span><span class="hljs-attribute">read_only</span>=1 <span class="hljs-attribute">lower_case_table_names</span>=1</code></pre></div><p>链接到主库</p><div class="hljs"><pre><code class="hljs routeros">CHANGE MASTER <span class="hljs-keyword">TO</span>    <span class="hljs-attribute">MASTER_HOST</span>=<span class="hljs-string">'172.17.0.2'</span>,    <span class="hljs-attribute">MASTER_USER</span>=<span class="hljs-string">'slave'</span>,    <span class="hljs-attribute">MASTER_PASSWORD</span>=<span class="hljs-string">'slave@123456'</span>,    <span class="hljs-attribute">MASTER_LOG_FILE</span>=<span class="hljs-string">'replicas-mysql-bin.000003'</span>,    <span class="hljs-attribute">MASTER_LOG_POS</span>=2841451;</code></pre></div><p>启动同步</p><div class="hljs"><pre><code class="hljs angelscript">mysql&gt; start slave;Query OK, <span class="hljs-number">0</span> rows affected, <span class="hljs-number">1</span> warning (<span class="hljs-number">0.00</span> sec)</code></pre></div><p>查询同步状态</p><div class="hljs"><pre><code class="hljs yaml"><span class="hljs-string">mysql&gt;</span> <span class="hljs-string">show</span> <span class="hljs-string">slave</span> <span class="hljs-string">status</span> <span class="hljs-string">\G;</span><span class="hljs-string">***************************</span> <span class="hljs-number">1</span><span class="hljs-string">.</span> <span class="hljs-string">row</span> <span class="hljs-string">***************************</span>               <span class="hljs-attr">Slave_IO_State:</span> <span class="hljs-string">Waiting</span> <span class="hljs-string">for</span> <span class="hljs-string">master</span> <span class="hljs-string">to</span> <span class="hljs-string">send</span> <span class="hljs-string">event</span>                  <span class="hljs-attr">Master_Host:</span> <span class="hljs-number">172.17</span><span class="hljs-number">.0</span><span class="hljs-number">.2</span>                  <span class="hljs-attr">Master_User:</span> <span class="hljs-string">slave</span>                  <span class="hljs-attr">Master_Port:</span> <span class="hljs-number">3306</span>                <span class="hljs-attr">Connect_Retry:</span> <span class="hljs-number">60</span>              <span class="hljs-attr">Master_Log_File:</span> <span class="hljs-string">replicas-mysql-bin.000003</span>          <span class="hljs-attr">Read_Master_Log_Pos:</span> <span class="hljs-number">2854957</span>               <span class="hljs-attr">Relay_Log_File:</span> <span class="hljs-string">replicas-mysql-relay-bin.000002</span>                <span class="hljs-attr">Relay_Log_Pos:</span> <span class="hljs-number">2854848</span>        <span class="hljs-attr">Relay_Master_Log_File:</span> <span class="hljs-string">replicas-mysql-bin.000003</span>             <span class="hljs-attr">Slave_IO_Running:</span> <span class="hljs-literal">Yes</span>            <span class="hljs-attr">Slave_SQL_Running:</span> <span class="hljs-literal">Yes</span>              <span class="hljs-attr">Replicate_Do_DB:</span>           <span class="hljs-attr">Replicate_Ignore_DB:</span>            <span class="hljs-attr">Replicate_Do_Table:</span>        <span class="hljs-attr">Replicate_Ignore_Table:</span>       <span class="hljs-attr">Replicate_Wild_Do_Table:</span>   <span class="hljs-attr">Replicate_Wild_Ignore_Table:</span>                    <span class="hljs-attr">Last_Errno:</span> <span class="hljs-number">0</span>                   <span class="hljs-attr">Last_Error:</span>                  <span class="hljs-attr">Skip_Counter:</span> <span class="hljs-number">0</span>          <span class="hljs-attr">Exec_Master_Log_Pos:</span> <span class="hljs-number">2854957</span>              <span class="hljs-attr">Relay_Log_Space:</span> <span class="hljs-number">2855064</span>              <span class="hljs-attr">Until_Condition:</span> <span class="hljs-string">None</span>               <span class="hljs-attr">Until_Log_File:</span>                 <span class="hljs-attr">Until_Log_Pos:</span> <span class="hljs-number">0</span>           <span class="hljs-attr">Master_SSL_Allowed:</span> <span class="hljs-literal">No</span>           <span class="hljs-attr">Master_SSL_CA_File:</span>            <span class="hljs-attr">Master_SSL_CA_Path:</span>               <span class="hljs-attr">Master_SSL_Cert:</span>             <span class="hljs-attr">Master_SSL_Cipher:</span>                <span class="hljs-attr">Master_SSL_Key:</span>         <span class="hljs-attr">Seconds_Behind_Master:</span> <span class="hljs-number">0</span><span class="hljs-attr">Master_SSL_Verify_Server_Cert:</span> <span class="hljs-literal">No</span>                <span class="hljs-attr">Last_IO_Errno:</span> <span class="hljs-number">0</span>                <span class="hljs-attr">Last_IO_Error:</span>                <span class="hljs-attr">Last_SQL_Errno:</span> <span class="hljs-number">0</span>               <span class="hljs-attr">Last_SQL_Error:</span>   <span class="hljs-attr">Replicate_Ignore_Server_Ids:</span>              <span class="hljs-attr">Master_Server_Id:</span> <span class="hljs-number">1</span>                  <span class="hljs-attr">Master_UUID:</span> <span class="hljs-string">306755a5-d46e-11e9-9f46-0242ac110002</span>             <span class="hljs-attr">Master_Info_File:</span> <span class="hljs-string">/var/lib/mysql/master.info</span>                    <span class="hljs-attr">SQL_Delay:</span> <span class="hljs-number">0</span>          <span class="hljs-attr">SQL_Remaining_Delay:</span> <span class="hljs-literal">NULL</span>      <span class="hljs-attr">Slave_SQL_Running_State:</span> <span class="hljs-string">Slave</span> <span class="hljs-string">has</span> <span class="hljs-string">read</span> <span class="hljs-string">all</span> <span class="hljs-string">relay</span> <span class="hljs-string">log;</span> <span class="hljs-string">waiting</span> <span class="hljs-string">for</span> <span class="hljs-string">more</span> <span class="hljs-string">updates</span>           <span class="hljs-attr">Master_Retry_Count:</span> <span class="hljs-number">86400</span>                  <span class="hljs-attr">Master_Bind:</span>       <span class="hljs-attr">Last_IO_Error_Timestamp:</span>      <span class="hljs-attr">Last_SQL_Error_Timestamp:</span>                <span class="hljs-attr">Master_SSL_Crl:</span>            <span class="hljs-attr">Master_SSL_Crlpath:</span>            <span class="hljs-attr">Retrieved_Gtid_Set:</span>             <span class="hljs-attr">Executed_Gtid_Set:</span>                 <span class="hljs-attr">Auto_Position:</span> <span class="hljs-number">0</span>         <span class="hljs-attr">Replicate_Rewrite_DB:</span>                  <span class="hljs-attr">Channel_Name:</span>            <span class="hljs-attr">Master_TLS_Version:</span> <span class="hljs-number">1</span> <span class="hljs-string">row</span> <span class="hljs-string">in</span> <span class="hljs-string">set</span> <span class="hljs-string">(0.00</span> <span class="hljs-string">sec)</span></code></pre></div><h3 id="参考文档"><a href="#参考文档" class="headerlink" title="参考文档"></a>参考文档</h3><p><a href="https://juejin.im/post/6844903921677238285" target="_blank" rel="noopener">Mysql主从复制原理及搭建</a></p>]]></content>
    
    
    <categories>
      
      <category>中间件</category>
      
    </categories>
    
    
    <tags>
      
      <tag>Mqsql</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>使用Mycat实现读写分离</title>
    <link href="/post/6ea54e66.html"/>
    <url>/post/6ea54e66.html</url>
    
    <content type="html"><![CDATA[<p>Mycat是常用的数据库代理层中间件，本文通过Mycat实现读写分离，应用程序先访问Mycat服务，Mycat根据SQL语句进行分发，读操作并且无事务走从库，写操作走主库。</p><a id="more"></a><h3 id="Mycat服务配置"><a href="#Mycat服务配置" class="headerlink" title="Mycat服务配置"></a>Mycat服务配置</h3><p>$ cat mycat/conf/server.xml</p><div class="hljs"><pre><code class="hljs xml"><span class="hljs-meta">&lt;?xml version="1.0" encoding="UTF-8"?&gt;</span><span class="hljs-comment">&lt;!-- - - Licensed under the Apache License, Version 2.0 (the "License"); </span><span class="hljs-comment">- you may not use this file except in compliance with the License. - You </span><span class="hljs-comment">may obtain a copy of the License at - - http://www.apache.org/licenses/LICENSE-2.0 </span><span class="hljs-comment">- - Unless required by applicable law or agreed to in writing, software - </span><span class="hljs-comment">distributed under the License is distributed on an "AS IS" BASIS, - WITHOUT </span><span class="hljs-comment">WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied. - See the </span><span class="hljs-comment">License for the specific language governing permissions and - limitations </span><span class="hljs-comment">under the License. --&gt;</span><span class="hljs-meta">&lt;!DOCTYPE <span class="hljs-meta-keyword">mycat</span>:server <span class="hljs-meta-keyword">SYSTEM</span> <span class="hljs-meta-string">"server.dtd"</span>&gt;</span><span class="hljs-tag">&lt;<span class="hljs-name">mycat:server</span> <span class="hljs-attr">xmlns:mycat</span>=<span class="hljs-string">"http://io.mycat/"</span>&gt;</span><span class="hljs-tag">&lt;<span class="hljs-name">system</span>&gt;</span><span class="hljs-comment">&lt;!-- 这里配置的都是一些系统属性，可以自己查看mycat文档 --&gt;</span><span class="hljs-tag">&lt;<span class="hljs-name">property</span> <span class="hljs-attr">name</span>=<span class="hljs-string">"defaultSqlParser"</span>&gt;</span>druidparser<span class="hljs-tag">&lt;/<span class="hljs-name">property</span>&gt;</span><span class="hljs-tag">&lt;<span class="hljs-name">property</span> <span class="hljs-attr">name</span>=<span class="hljs-string">"charset"</span>&gt;</span>utf8mb4<span class="hljs-tag">&lt;/<span class="hljs-name">property</span>&gt;</span><span class="hljs-tag">&lt;/<span class="hljs-name">system</span>&gt;</span><span class="hljs-tag">&lt;<span class="hljs-name">user</span> <span class="hljs-attr">name</span>=<span class="hljs-string">"root"</span> <span class="hljs-attr">defaultAccount</span>=<span class="hljs-string">"true"</span>&gt;</span><span class="hljs-tag">&lt;<span class="hljs-name">property</span> <span class="hljs-attr">name</span>=<span class="hljs-string">"password"</span>&gt;</span>123456<span class="hljs-tag">&lt;/<span class="hljs-name">property</span>&gt;</span><span class="hljs-tag">&lt;<span class="hljs-name">property</span> <span class="hljs-attr">name</span>=<span class="hljs-string">"schemas"</span>&gt;</span>prod<span class="hljs-tag">&lt;/<span class="hljs-name">property</span>&gt;</span><span class="hljs-tag">&lt;/<span class="hljs-name">user</span>&gt;</span><span class="hljs-tag">&lt;/<span class="hljs-name">mycat:server</span>&gt;</span></code></pre></div><blockquote><p><code>schemas</code>表示<code>schema.xml</code>中的<code>schema</code>节点，多个库用逗号分隔</p></blockquote><h3 id="表配置"><a href="#表配置" class="headerlink" title="表配置"></a>表配置</h3><p>$ cat mycat/conf/schema.xml </p><div class="hljs"><pre><code class="hljs xml"><span class="hljs-meta">&lt;?xml version="1.0"?&gt;</span><span class="hljs-meta">&lt;!DOCTYPE <span class="hljs-meta-keyword">mycat</span>:schema <span class="hljs-meta-keyword">SYSTEM</span> <span class="hljs-meta-string">"schema.dtd"</span>&gt;</span><span class="hljs-tag">&lt;<span class="hljs-name">mycat:schema</span> <span class="hljs-attr">xmlns:mycat</span>=<span class="hljs-string">"http://io.mycat/"</span>&gt;</span><span class="hljs-tag">&lt;<span class="hljs-name">schema</span> <span class="hljs-attr">name</span>=<span class="hljs-string">"prod"</span> <span class="hljs-attr">checkSQLschema</span>=<span class="hljs-string">"false"</span> <span class="hljs-attr">sqlMaxLimit</span>=<span class="hljs-string">"100"</span> <span class="hljs-attr">dataNode</span>=<span class="hljs-string">"pcxNode"</span>&gt;</span><span class="hljs-tag">&lt;/<span class="hljs-name">schema</span>&gt;</span><span class="hljs-tag">&lt;<span class="hljs-name">dataNode</span> <span class="hljs-attr">name</span>=<span class="hljs-string">"pcxNode"</span> <span class="hljs-attr">dataHost</span>=<span class="hljs-string">"dtHost"</span> <span class="hljs-attr">database</span>=<span class="hljs-string">"test"</span> /&gt;</span><span class="hljs-tag">&lt;<span class="hljs-name">dataHost</span> <span class="hljs-attr">name</span>=<span class="hljs-string">"dtHost"</span> <span class="hljs-attr">maxCon</span>=<span class="hljs-string">"1000"</span> <span class="hljs-attr">minCon</span>=<span class="hljs-string">"10"</span> <span class="hljs-attr">balance</span>=<span class="hljs-string">"3"</span></span><span class="hljs-tag">  <span class="hljs-attr">writeType</span>=<span class="hljs-string">"0"</span> <span class="hljs-attr">dbType</span>=<span class="hljs-string">"mysql"</span> <span class="hljs-attr">dbDriver</span>=<span class="hljs-string">"native"</span> <span class="hljs-attr">switchType</span>=<span class="hljs-string">"1"</span>  <span class="hljs-attr">slaveThreshold</span>=<span class="hljs-string">"100"</span>&gt;</span><span class="hljs-tag">&lt;<span class="hljs-name">heartbeat</span>&gt;</span>select user()<span class="hljs-tag">&lt;/<span class="hljs-name">heartbeat</span>&gt;</span><span class="hljs-comment">&lt;!-- can have multi write hosts --&gt;</span><span class="hljs-tag">&lt;<span class="hljs-name">writeHost</span> <span class="hljs-attr">host</span>=<span class="hljs-string">"hostM1"</span> <span class="hljs-attr">url</span>=<span class="hljs-string">"localhost:3308"</span> <span class="hljs-attr">user</span>=<span class="hljs-string">"root"</span></span><span class="hljs-tag">   <span class="hljs-attr">password</span>=<span class="hljs-string">"123456"</span>&gt;</span><span class="hljs-comment">&lt;!-- can have multi read hosts --&gt;</span><span class="hljs-tag">&lt;<span class="hljs-name">readHost</span> <span class="hljs-attr">host</span>=<span class="hljs-string">"hostS1"</span> <span class="hljs-attr">url</span>=<span class="hljs-string">"localhost:3307"</span> <span class="hljs-attr">user</span>=<span class="hljs-string">"root"</span> <span class="hljs-attr">password</span>=<span class="hljs-string">"123456"</span> /&gt;</span><span class="hljs-tag">&lt;/<span class="hljs-name">writeHost</span>&gt;</span><span class="hljs-tag">&lt;/<span class="hljs-name">dataHost</span>&gt;</span><span class="hljs-tag">&lt;/<span class="hljs-name">mycat:schema</span>&gt;</span></code></pre></div><blockquote><p>增加数据库时要添加<code>dataNode</code>及<code>schema</code></p></blockquote><h3 id="Mycat-启动"><a href="#Mycat-启动" class="headerlink" title="Mycat 启动"></a>Mycat 启动</h3><p><code>./mycat start</code></p>]]></content>
    
    
    <categories>
      
      <category>中间件</category>
      
    </categories>
    
    
    <tags>
      
      <tag>Mqsql</tag>
      
      <tag>Mycat</tag>
      
      <tag>读写分离</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>Redis进阶</title>
    <link href="/post/3a12d43e.html"/>
    <url>/post/3a12d43e.html</url>
    
    <content type="html"><![CDATA[<p>日常开发中Redis是常用的缓存工具，Redis其实不仅仅能做缓存，Redis支持string(字符串)、list(列表)、hash(字典)、set(集合) 和zset(有序集合)这五种数据结构，本文介绍更多Redis的使用场景。</p><a id="more"></a><h3 id="分布式锁"><a href="#分布式锁" class="headerlink" title="分布式锁"></a>分布式锁</h3><p><code>SET key value [EX seconds] [PX milliseconds] [NX|XX]</code></p><p>将字符串值 value 关联到 key 。</p><p>如果 key 已经持有其他值， SET 就覆写旧值，无视类型。</p><p>对于某个原本带有生存时间（TTL）的键来说， 当 SET 命令成功在这个键上执行时， 这个键原有的 TTL 将被清除。</p><p>可选参数</p><p>从 Redis 2.6.12 版本开始， SET 命令的行为可以通过一系列参数来修改：</p><p>EX second ：设置键的过期时间为 second 秒。<br>PX millisecond ：设置键的过期时间为 millisecond 毫秒。<br>NX ：只在键不存在时，才对键进行设置操作。 SET key value NX 效果等同于 SETNX key value 。<br>XX ：只在键已经存在时，才对键进行设置操作。</p><blockquote><p>因为 SET 命令可以通过参数来实现和 SETNX 、 SETEX 和 PSETEX 三个命令的效果，所以将来的 Redis 版本可能会废弃并最终移除 SETNX 、 SETEX 和 PSETEX 这三个命令。</p></blockquote><p>使用RedisTemplate加锁</p><div class="hljs"><pre><code class="hljs java"><span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">boolean</span> <span class="hljs-title">lock</span><span class="hljs-params">(String lockKey, String lockValue, <span class="hljs-keyword">int</span> expire)</span> </span>&#123;    <span class="hljs-comment">// 加锁</span>    String lock = stringRedisTemplate.execute((RedisCallback&lt;String&gt;) connection -&gt; &#123;        JedisCommands commands = (JedisCommands) connection.getNativeConnection();        <span class="hljs-keyword">return</span> commands.set(lockKey, lockValue, <span class="hljs-string">"NX"</span>, <span class="hljs-string">"EX"</span>, expire);    &#125;);    <span class="hljs-keyword">return</span> Objects.equals(LOCK_RESULT, lock);&#125;</code></pre></div><p>使用RedisTemplate解锁</p><div class="hljs"><pre><code class="hljs java"><span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">unlock</span><span class="hljs-params">(String lockKey)</span></span>&#123;    <span class="hljs-comment">// 解锁</span>    stringRedisTemplate.delete(lockKey);&#125;</code></pre></div><blockquote><p>锁不具备拥有者标识，即任何客户端都可以解锁。</p></blockquote><div class="hljs"><pre><code class="hljs lua"><span class="hljs-keyword">if</span> redis.call(<span class="hljs-string">"get"</span>, KEYS[<span class="hljs-number">1</span>]) == ARGV[<span class="hljs-number">1</span>]<span class="hljs-keyword">then</span>    <span class="hljs-keyword">return</span> redis.call(<span class="hljs-string">"del"</span>, KEYS)<span class="hljs-keyword">else</span>    <span class="hljs-keyword">return</span> <span class="hljs-number">0</span><span class="hljs-keyword">end</span></code></pre></div><div class="hljs"><pre><code class="hljs java"><span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">unlock</span><span class="hljs-params">(String lockKey,String requestId)</span></span>&#123;    List&lt;String&gt; keys = Collections.singletonList(lockKey);    <span class="hljs-comment">// 采用lua脚本方式保证在多线程环境下也是原子操作</span>    DefaultRedisScript&lt;Long&gt; script = <span class="hljs-keyword">new</span> DefaultRedisScript&lt;&gt;();    script.setResultType(Long<span class="hljs-class">.<span class="hljs-keyword">class</span>)</span>;    script.setScriptText(luaScriptText);    stringRedisTemplate.execute(script,keys,requestId);&#125;</code></pre></div><h3 id="消息队列"><a href="#消息队列" class="headerlink" title="消息队列"></a>消息队列</h3><h4 id="可使用list实现一个简单的消息队列"><a href="#可使用list实现一个简单的消息队列" class="headerlink" title="可使用list实现一个简单的消息队列"></a>可使用list实现一个简单的消息队列</h4><p><code>RPUSH key value [value ...]</code></p><p>将一个或多个值 value 插入到列表 key 的表尾(最右边)。</p><div class="hljs"><pre><code class="hljs python">redis_conn.lpush(key,<span class="hljs-number">3</span>)</code></pre></div><p><code>LRANGE key start stop</code></p><p>返回列表 key 中指定区间内的元素，区间以偏移量 start 和 stop 指定。</p><div class="hljs"><pre><code class="hljs python">redis_conn.lrange(key,<span class="hljs-number">0</span>,<span class="hljs-number">-1</span>)</code></pre></div><p><code>BLPOP key [key ...] timeout</code></p><p>BLPOP 是列表的阻塞式(blocking)弹出原语。</p><div class="hljs"><pre><code class="hljs python"><span class="hljs-keyword">while</span> <span class="hljs-literal">True</span>:    <span class="hljs-comment"># 阻塞</span>    data = redis_conn.blpop(key)    print(data)</code></pre></div><h3 id="有序集合"><a href="#有序集合" class="headerlink" title="有序集合"></a>有序集合</h3><h4 id="有序集合实现排行榜功能"><a href="#有序集合实现排行榜功能" class="headerlink" title="有序集合实现排行榜功能"></a>有序集合实现排行榜功能</h4><p><code>ZINCRBY key increment member</code></p><p>为有序集 key 的成员 member 的 score 值加上增量 increment 。</p><div class="hljs"><pre><code class="hljs java">BoundZSetOperations&lt;String, String&gt; operations = stringRedisTemplate.boundZSetOps(INVITE_ACTIVITY_KEY);operations.incrementScore(userId.toString(), <span class="hljs-number">1</span>);</code></pre></div><p><code>ZREVRANGE key start stop [WITHSCORES]</code></p><p>返回有序集 key 中，指定区间内的成员。<br><strong>bscore相同时会按照member字典顺序排序</strong></p><div class="hljs"><pre><code class="hljs java"><span class="hljs-comment">// 取前十名</span>Set&lt;ZSetOperations.TypedTuple&lt;String&gt;&gt; typedTuples = operations.reverseRangeWithScores(<span class="hljs-number">0</span>, <span class="hljs-number">9</span>);<span class="hljs-comment">// 遍历取用户Id和积分</span><span class="hljs-keyword">for</span> (ZSetOperations.TypedTuple&lt;String&gt; typedTuple : typedTuples) &#123;    Integer inviterId = Integer.parseInt(typedTuple.getValue().trim());    <span class="hljs-comment">// 邀请用户数量</span>    Integer inviteCount = typedTuple.getScore().intValue();&#125;</code></pre></div><p><code>ZSCORE key member</code></p><p>返回有序集 key 中，成员 member 的 score 值。</p><div class="hljs"><pre><code class="hljs java">Double score = operations.score(userId.toString());</code></pre></div><h3 id="发布-订阅"><a href="#发布-订阅" class="headerlink" title="发布/订阅"></a>发布/订阅</h3><h3 id="事务"><a href="#事务" class="headerlink" title="事务"></a>事务</h3><p><code>MULTI</code></p><p>标记一个事务块的开始。</p><p>事务块内的多条命令会按照先后顺序被放进一个队列当中，最后由 <code>EXEC</code> 命令原子性(atomic)地执行。</p><p><code>EXEC</code></p><p>执行所有事务块内的命令。</p><p><code>WATCH key [key ...]</code></p><p>监视一个(或多个) key ，如果在事务执行之前这个(或这些) key 被其他命令所改动，那么事务将被打断。</p><p>减库存操作</p><div class="hljs"><pre><code class="hljs python"><span class="hljs-comment"># 监视一个key</span>pipe.watch(<span class="hljs-string">'stock_count'</span>)count = int(pipe.get(<span class="hljs-string">'stock_count'</span>))<span class="hljs-comment"># 有库存</span><span class="hljs-keyword">if</span> count &gt; <span class="hljs-number">0</span>:     <span class="hljs-comment"># 事务开始</span>    pipe.multi()    <span class="hljs-comment"># pipe.decr('stock_count')</span>    pipe.set(<span class="hljs-string">'stock_count'</span>, count - <span class="hljs-number">1</span>)    <span class="hljs-comment"># 事务结束(可能会抛异常)</span>    pipe.execute()</code></pre></div>]]></content>
    
    
    <categories>
      
      <category>中间件</category>
      
    </categories>
    
    
    <tags>
      
      <tag>Redis</tag>
      
    </tags>
    
  </entry>
  
  
  
  
</search>
