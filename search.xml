<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>当我们谈论NIO时我们在谈论什么</title>
    <url>/post/9a133fa7.html</url>
    <content><![CDATA[<p><img src="https://img.yjll.art/img/NIO_HEAD.png" alt="NIO_HEAD"></p>
<p>NIO（Non-blocking I/O，在Java领域，也称为New I/O），是一种同步非阻塞的I/O模型，也是I/O多路复用的基础，已经被越来越多地应用到大型应用服务器，成为解决高并发与大量连接、I/O处理问题的有效方式。</p>
<a id="more"></a>

<h3 id="用户空间与内核空间"><a href="#用户空间与内核空间" class="headerlink" title="用户空间与内核空间"></a>用户空间与内核空间</h3><p>现在操作系统都是采用虚拟存储器，那么对32位操作系统而言，它的寻址空间（虚拟存储空间）为4G（2的32次方）。操作系统的核心是内核，独立于普通的应用程序，可以访问受保护的内存空间，也有访问底层硬件设备的所有权限。为了保证用户进程不能直接操作内核（kernel），保证内核的安全，操作系统将虚拟空间划分为两部分，一部分为内核空间，一部分为用户空间。针对linux操作系统而言，将最高的1G字节（从虚拟地址0xC0000000到0xFFFFFFFF），供内核使用，称为内核空间，而将较低的3G字节（从虚拟地址0x00000000到0xBFFFFFFF），供各个进程使用，称为用户空间。</p>
<h3 id="缓存-IO"><a href="#缓存-IO" class="headerlink" title="缓存 IO"></a>缓存 IO</h3><p>缓存 IO 又被称作标准 IO，大多数文件系统的默认 IO 操作都是缓存 IO。在 Linux 的缓存 IO 机制中，操作系统会将 IO 的数据缓存在文件系统的页缓存（ page cache ）中，也就是说，数据会先被拷贝到操作系统内核的缓冲区中，然后才会从操作系统内核的缓冲区拷贝到应用程序的地址空间。</p>
<h3 id="I-O-模型"><a href="#I-O-模型" class="headerlink" title="I/O 模型"></a>I/O 模型</h3><h4 id="阻塞"><a href="#阻塞" class="headerlink" title="阻塞"></a>阻塞</h4><p><img src="https://img.yjll.art/img/BIO_model.png" alt="BIO_model"></p>
<p>每个连接需要独立的进程/线程单独处理</p>
<h4 id="轮询"><a href="#轮询" class="headerlink" title="轮询"></a>轮询</h4><p><img src="https://img.yjll.art/img/loop_model.png" alt="loop_model"></p>
<p>轮询将会不断地询问内核，这将占用大量的 CPU 时间</p>
<h4 id="多路复用"><a href="#多路复用" class="headerlink" title="多路复用"></a>多路复用</h4><p><img src="https://img.yjll.art/img/NIO_model.png" alt="NIO_model"></p>
<p>当连接数较少时效率相比多线程+阻塞 I/O 模型效率较低，可能延迟更大，因为单个连接处理需要 2 次系统调用，占用时间会有增加。</p>
<h3 id="应用层实现"><a href="#应用层实现" class="headerlink" title="应用层实现"></a>应用层实现</h3><h4 id="BIO"><a href="#BIO" class="headerlink" title="BIO"></a>BIO</h4><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"> ExecutorService executor = Excutors.newFixedThreadPollExecutor(<span class="number">100</span>);<span class="comment">//线程池</span></span><br><span class="line"></span><br><span class="line"> ServerSocket serverSocket = <span class="keyword">new</span> ServerSocket();</span><br><span class="line"> serverSocket.bind(<span class="number">8088</span>);</span><br><span class="line"> <span class="keyword">while</span>(<span class="keyword">true</span>)&#123;<span class="comment">//主线程死循环等待新连接到来</span></span><br><span class="line"> Socket socket = serverSocket.accept();</span><br><span class="line"> executor.submit(<span class="keyword">new</span> ConnectIOnHandler(socket));<span class="comment">//为新的连接创建新的线程</span></span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">ConnectIOnHandler</span> <span class="keyword">extends</span> <span class="title">Thread</span></span>&#123;</span><br><span class="line">    <span class="keyword">private</span> Socket socket;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="title">ConnectIOnHandler</span><span class="params">(Socket socket)</span></span>&#123;</span><br><span class="line">       <span class="keyword">this</span>.socket = socket;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">run</span><span class="params">()</span></span>&#123;</span><br><span class="line">        String someThing = socket.read()....<span class="comment">//读取数据</span></span><br><span class="line">        <span class="keyword">if</span>(someThing!=<span class="keyword">null</span>)&#123;</span><br><span class="line">            ......<span class="comment">//处理数据</span></span><br><span class="line">            socket.write()....<span class="comment">//写数据</span></span><br><span class="line">        &#125;</span><br><span class="line">        ...</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h4 id="NIO"><a href="#NIO" class="headerlink" title="NIO"></a>NIO</h4><figure class="highlight java"><table><tr><td class="code"><pre><span class="line">Selector selector = Selector.open();</span><br><span class="line">ServerSocketChannel serverSocketChannel = ServerSocketChannel.open();</span><br><span class="line">serverSocketChannel.configureBlocking(<span class="keyword">false</span>);</span><br><span class="line">SelectionKey key = serverSocketChannel.register(selector, SelectionKey.OP_ACCEPT);</span><br></pre></td></tr></table></figure>

<p><code>selector.select()</code>将会阻塞线程，直到有连接,并根据订阅的事件进行分发</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">while</span>(<span class="keyword">true</span>) &#123;</span><br><span class="line"></span><br><span class="line">  <span class="keyword">int</span> readyChannels = selector.select();</span><br><span class="line"></span><br><span class="line">  <span class="keyword">if</span>(readyChannels == <span class="number">0</span>) <span class="keyword">continue</span>;</span><br><span class="line"></span><br><span class="line">  Set&lt;SelectionKey&gt; selectedKeys = selector.selectedKeys();</span><br><span class="line"></span><br><span class="line">  Iterator&lt;SelectionKey&gt; keyIterator = selectedKeys.iterator();</span><br><span class="line"></span><br><span class="line">  <span class="keyword">while</span>(keyIterator.hasNext()) &#123;</span><br><span class="line"></span><br><span class="line">    SelectionKey key = keyIterator.next();</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span>(key.isAcceptable()) &#123;</span><br><span class="line">        <span class="comment">// a connection was accepted by a ServerSocketChannel.</span></span><br><span class="line"></span><br><span class="line">    &#125; <span class="keyword">else</span> <span class="keyword">if</span> (key.isConnectable()) &#123;</span><br><span class="line">        <span class="comment">// a connection was established with a remote server.</span></span><br><span class="line"></span><br><span class="line">    &#125; <span class="keyword">else</span> <span class="keyword">if</span> (key.isReadable()) &#123;</span><br><span class="line">        <span class="comment">// a channel is ready for reading</span></span><br><span class="line"></span><br><span class="line">    &#125; <span class="keyword">else</span> <span class="keyword">if</span> (key.isWritable()) &#123;</span><br><span class="line">        <span class="comment">// a channel is ready for writing</span></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    keyIterator.remove();</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



]]></content>
      <categories>
        <category>Java</category>
      </categories>
      <tags>
        <tag>NIO</tag>
      </tags>
  </entry>
  <entry>
    <title>Nginx使用Certbot配置HTTPS证书</title>
    <url>/post/77a89a46.html</url>
    <content><![CDATA[<p><img src="https://img.yjll.art//img/20200426131416.jpg" alt="20200426131416"></p>
<p>随着互联网的发展，数据安全问题越来越收到重视，Chrome 将没有配置 SSL 加密的 HTTP 网站标记为不安全，HTTPS是未来互联网发展的趋势，但是想要网站支持HTTPS网站必须有Certificate Authority颁发的证书，一般CA签发的证书都是收费的。本文将介绍如何使用免费的授权证书并自动续签。</p>
<p>Let’s Encrypt 是 一个叫 ISRG （ Internet Security Research Group ，互联网安全研究小组）的组织推出的免费安全证书计划。ISRG 的发起者 EFF （电子前哨基金会）为 Let’s Encrypt 项目发布了一个官方的客户端 Certbot ，利用它可以完全自动化的获取、部署和更新安全证书。</p>
<a id="more"></a>

<h3 id="准备"><a href="#准备" class="headerlink" title="准备"></a>准备</h3><p>我们已经安装了Nginx，并且使用默认的80端口，例如我的Hexo博客配置，Certbot会更改nginx的配置文件，操作前最好备份一下nginx.conf。</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">server &#123;</span><br><span class="line">    listen 80;</span><br><span class="line">    server_name blog.yjll.art;</span><br><span class="line">    location &#x2F; &#123;</span><br><span class="line">        include &#x2F;etc&#x2F;nginx&#x2F;mime.types;</span><br><span class="line">        root &#x2F;var&#x2F;www&#x2F;blog&#x2F;public;</span><br><span class="line">        index index.html;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>我们先安装Certbot,我是用的是CentOS7，就直接使用yum安装啦。</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">yum install python2-certbot-nginx</span><br></pre></td></tr></table></figure>

<h3 id="通过Certbot获取配置证书"><a href="#通过Certbot获取配置证书" class="headerlink" title="通过Certbot获取配置证书"></a>通过Certbot获取配置证书</h3><p>安装完成后，输入命令</p>
<pre><code>certbot --nginx</code></pre><p>期间会要求输入邮箱信息，确认协议和域名等，根据提示输入就好。我们看一下生成好的nginx.conf，原来的80变成了443端口，增加了证书配置。</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">server &#123;</span><br><span class="line">    server_name blog.yjll.art;</span><br><span class="line">    location &#x2F; &#123;</span><br><span class="line">        include &#x2F;etc&#x2F;nginx&#x2F;mime.types;</span><br><span class="line">        root &#x2F;var&#x2F;www&#x2F;blog&#x2F;public;</span><br><span class="line">        index index.html;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    listen 443 ssl; # managed by Certbot</span><br><span class="line">    ssl_certificate &#x2F;etc&#x2F;letsencrypt&#x2F;live&#x2F;yjll.art&#x2F;fullchain.pem; # managed by Certbot</span><br><span class="line">    ssl_certificate_key &#x2F;etc&#x2F;letsencrypt&#x2F;live&#x2F;yjll.art&#x2F;privkey.pem; # managed by Certbot</span><br><span class="line">    include &#x2F;etc&#x2F;letsencrypt&#x2F;options-ssl-nginx.conf; # managed by Certbot</span><br><span class="line">    ssl_dhparam &#x2F;etc&#x2F;letsencrypt&#x2F;ssl-dhparams.pem; # managed by Certbot</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>为了原来的http连接也可以正常使用，我们可以通过Nginx将http请求重定向到现在的端口上</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">server &#123;</span><br><span class="line">listen       80;</span><br><span class="line">rewrite ^(.*) https:&#x2F;&#x2F;$host$1 permanent;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>好了，可以在浏览器访问HTTP和HTTPS链接试一试了。</p>
<h3 id="自动续期"><a href="#自动续期" class="headerlink" title="自动续期"></a>自动续期</h3><p>使用起来确实挺方便的，但是Let’s Encrypt发布的证书期限只有三个月，在证书到期前我们要重新申请证书，<code>certbot</code>也提供了续订的功能</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">certbot renew</span><br></pre></td></tr></table></figure>

<p>也就是说在证书过期前要运行一下上边的命令，并且重新加载nginx的配置文件，这部分我们可以配置在定时任务中。</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">certbot renew --force-renew --renew-hook &quot;nginx -s reload&quot;</span><br></pre></td></tr></table></figure>

<h3 id="参考资料"><a href="#参考资料" class="headerlink" title="参考资料"></a>参考资料</h3><p><a href="https://certbot.eff.org/" target="_blank" rel="noopener">https://certbot.eff.org/</a></p>
<p><a href="https://learnku.com/articles/19999" target="_blank" rel="noopener">https://learnku.com/articles/19999</a></p>
]]></content>
      <categories>
        <category>Linux</category>
      </categories>
      <tags>
        <tag>Nginx</tag>
        <tag>Https</tag>
        <tag>Certbot</tag>
      </tags>
  </entry>
  <entry>
    <title>Logstash</title>
    <url>/post/undefined.html</url>
    <content><![CDATA[<p><a href="https://img.yjll.art/img/Logstash.png" target="_blank" rel="noopener">Logstash</a><br>Logstash是一个开源数据收集引擎，具有实时管道功能。Logstash可以动态地将来自不同数据源的数据统一起来，并将数据标准化到你所选择的目的地。</p>
<p>项目用到ES做统计，需要同步Mysql数据到ES中，增量数据使用阿里开源工具cannel,cannel是通过读取mysql的binlog实现同步，可对实时数据进行同步，无法同步历史数据，既有的数据我们使用Logstash进行同步，Logstash安装需要依赖的包比较多，我为了方便直接使用Docker进行操作。</p>
<p>首先要编写同步数据的配置文件，使用jdbc里连接数据库作为input，ouput为ES。</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">input &#123;</span><br><span class="line"> stdin &#123; &#125;</span><br><span class="line">    jdbc &#123;</span><br><span class="line">        #注意mysql连接地址一定要用ip，不能使用localhost等</span><br><span class="line">        jdbc_connection_string &#x3D;&gt; &quot;jdbc:mysql:&#x2F;&#x2F;192.168.1.200:3308&#x2F;test&quot;</span><br><span class="line">        jdbc_user &#x3D;&gt; &quot;test&quot;</span><br><span class="line">        jdbc_password &#x3D;&gt; &quot;test&quot;</span><br><span class="line">        #这个jar包的地址是容器内的地址</span><br><span class="line">        jdbc_driver_library &#x3D;&gt; &quot;&#x2F;usr&#x2F;share&#x2F;logstash&#x2F;pipeline&#x2F;mysql-connector-java-8.0.11.jar&quot;</span><br><span class="line">        jdbc_driver_class &#x3D;&gt; &quot;com.mysql.jdbc.Driver&quot;</span><br><span class="line">        jdbc_paging_enabled &#x3D;&gt; &quot;true&quot;</span><br><span class="line">        jdbc_page_size &#x3D;&gt; &quot;5000&quot;</span><br><span class="line">        statement &#x3D;&gt; &quot;SELECT * FROM base_areas where id &gt; :sql_last_value&quot;</span><br><span class="line">        tracking_column &#x3D;&gt; id</span><br><span class="line">        use_column_value &#x3D;&gt; true</span><br><span class="line">        last_run_metadata_path &#x3D;&gt; &quot;&#x2F;usr&#x2F;share&#x2F;logstash&#x2F;pipeline&#x2F;area&quot;</span><br><span class="line">        schedule &#x3D;&gt; &quot;* * * * *&quot;</span><br><span class="line">    &#125;</span><br><span class="line"> &#125;</span><br><span class="line">  </span><br><span class="line"> output &#123;</span><br><span class="line">     stdout &#123;</span><br><span class="line">        codec &#x3D;&gt; json_lines</span><br><span class="line">    &#125;</span><br><span class="line">    elasticsearch &#123;</span><br><span class="line">        hosts &#x3D;&gt; &quot;192.168.1.200:9200&quot;</span><br><span class="line">        index &#x3D;&gt; &quot;area&quot;</span><br><span class="line">        document_type &#x3D;&gt; &quot;_doc&quot;</span><br><span class="line">        document_id &#x3D;&gt; &quot;%&#123;id&#125;&quot;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>


<p>重点在于 <code>use_column_value</code> 和 <code>tracking_column</code> 这两个参数，当use_column_value为true时，可以用 :sql_last_value 这个变量来获取tracking_column对应的字段的最新值，默认即第一次启动时为 0 。我的示例中tracking_column对应id，即Logstash都会记录每次查询结果id的最大值，供下一次查询使用。</p>
<p>Logstash将tracking_column的最新值记录到<code>last_run_metadata_path</code>配置的文件，启动时读取该文件</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">docker run -it -v  $PWD&#x2F;pipeline&#x2F;:&#x2F;usr&#x2F;share&#x2F;logstash&#x2F;pipeline -v $PWD&#x2F;logstash.yml:&#x2F;usr&#x2F;share&#x2F;logstash&#x2F;config&#x2F;logstash.yml -v $PWD&#x2F;pipelines.yml:&#x2F;usr&#x2F;share&#x2F;logstash&#x2F;config&#x2F;pipelines.yml   logstash:6.4.3  bash</span><br></pre></td></tr></table></figure>

<p>先安装jdbc和es的插件</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">bash-4.2$ ~&#x2F;bin&#x2F;logstash-plugin install logstash-input-jdbc</span><br><span class="line">Validating logstash-input-jdbc</span><br><span class="line">Installing logstash-input-jdbc</span><br><span class="line">Installation successful</span><br><span class="line"></span><br><span class="line">bash-4.2$ ~&#x2F;bin&#x2F;logstash-plugin install logstash-output-elasticsearch</span><br><span class="line">Validating logstash-output-elasticsearch</span><br><span class="line">Installing logstash-output-elasticsearch</span><br><span class="line">Installation successful</span><br></pre></td></tr></table></figure>

<p>由于是在容器内部连接服务，所以ES的连接不要使用localhost和127.0.0.1，更改为实际IP地址</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">bash-4.2$ cat ~&#x2F;config&#x2F;logstash.yml </span><br><span class="line">http.host: &quot;0.0.0.0&quot;</span><br><span class="line">xpack.monitoring.elasticsearch.url: http:&#x2F;&#x2F;192.168.1.200:9200</span><br></pre></td></tr></table></figure>

<h3 id="执行脚本"><a href="#执行脚本" class="headerlink" title="执行脚本"></a>执行脚本</h3><p>运行同步脚本可手动执行也可以配置调度执行</p>
<h4 id="手动执行"><a href="#手动执行" class="headerlink" title="手动执行"></a>手动执行</h4><p><code>~/bin/logstash -f ***.config</code></p>
<h4 id="调度执行"><a href="#调度执行" class="headerlink" title="调度执行"></a>调度执行</h4><p>调度任务可读取配置文件<code>config/pipelines.yml</code>，默认有一个<code>main</code>，<code>/usr/share/logstash/pipeline</code>为pipeline的存放路径，该文件夹内的pipeline都会被执行。<br>可手动更改该文件，添加<code>id</code>和<code>config</code>即可。</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">bash-4.2$ cat ~&#x2F;config&#x2F;pipelines.yml </span><br><span class="line"></span><br><span class="line">- pipeline.id: main</span><br><span class="line">  path.config: &quot;&#x2F;usr&#x2F;share&#x2F;logstash&#x2F;pipeline&quot;</span><br></pre></td></tr></table></figure>

<p>退出容器并重启，<code>docker logs -f CONTAINER</code> 查看运行日志</p>
<h3 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h3><p><code>logstash</code>是ES官方提供的工具，支持扩展多，配置灵活，但是只能同步增量数据，对于源数据删除和修改的情况，logstash无能为力，还有基于定时任务同步无法保证实时，对实时性要求高的场景要考虑其他方案。</p>
<h3 id="参考文档"><a href="#参考文档" class="headerlink" title="参考文档"></a>参考文档</h3><p><a href="https://www.elastic.co/guide/en/logstash/current/docker-config.html#_pipeline_configuration" target="_blank" rel="noopener">Configuring Logstash for Docker</a></p>
<p><a href="https://www.elastic.co/guide/en/logstash/current/plugins-inputs-jdbc.html" target="_blank" rel="noopener">Jdbc input plugin</a></p>
]]></content>
  </entry>
  <entry>
    <title>Manjaro安装与配置</title>
    <url>/post/d9d0da7d.html</url>
    <content><![CDATA[<p>闲来无事折腾了一下Manjaro，本文记录安装配置Manjaro的过程，一方面备忘，另一方面希望可以帮助到有需要的人</p>
<p>我版本选择<a href="https://manjaro.org/download/" target="_blank" rel="noopener">官网</a>上的KDE版本，用<a href="https://www.balena.io/etcher/" target="_blank" rel="noopener">balenaEtcher</a>将镜像写入U盘，Manjaro的安装步骤有图像界面，根据自己需求安装即可。</p>
<a id="more"></a>

<h3 id="配置国内源"><a href="#配置国内源" class="headerlink" title="配置国内源"></a>配置国内源</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">sudo pacman-mirrors -i -c China -m rank</span><br><span class="line"></span><br><span class="line"><span class="comment"># 查看是否已经更改</span></span><br><span class="line">cat /etc/pacman.d/mirrorlist</span><br></pre></td></tr></table></figure>
<p>手动更改可参考<a href="https://www.archlinux.org/mirrorlist/" target="_blank" rel="noopener">镜像地址</a></p>
<h3 id="添加-ArchLinuxCN-源"><a href="#添加-ArchLinuxCN-源" class="headerlink" title="添加 ArchLinuxCN 源"></a>添加 ArchLinuxCN 源</h3><p>ArchLinuxCN 是 Arch 中文组维护的一个软件合集，包含了中文用户常用的 WPS Office、搜狗拼音、Google Chrome 等软件。不过，Arch 本身和它的常见衍生版（如：Manjaro）默认都不包含这个源，因此我们需要手动来配置使用这个源。</p>
<p>参考<a href="https://github.com/archlinuxcn/mirrorlist-repo" target="_blank" rel="noopener">镜像地址</a></p>
<p>我使用的是清华大学的源</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">sudo vim etc/pacman.conf</span><br><span class="line"></span><br><span class="line">[archlinuxcn]</span><br><span class="line">SigLevel = Optional TrustedOnly</span><br><span class="line">Server = http://mirrors.tuna.tsinghua.edu.cn/archlinuxcn//<span class="variable">$arch</span></span><br></pre></td></tr></table></figure>

<p>导入 GPG key 并更新系统<br>这是 archlinuxcn 的 key，安装后作为验证，就可以安装软件了。</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 更新数据源</span></span><br><span class="line">sudo pacman -Syy</span><br><span class="line"><span class="comment"># 安装导入 GPG key</span></span><br><span class="line">sudo pacman -S archlinuxcn-keyring</span><br><span class="line"><span class="comment"># 更新系统</span></span><br><span class="line">sudo pacman -Syu</span><br><span class="line"></span><br><span class="line"><span class="comment"># 安装chrome</span></span><br><span class="line">sudo pacman -S google-chrome</span><br></pre></td></tr></table></figure>

<h3 id="AUR"><a href="#AUR" class="headerlink" title="AUR"></a>AUR</h3><p>Arch用户软件仓库（Arch User Repository，AUR）是为用户而建、由用户主导的Arch软件仓库。AUR中的软件包以软件包生成脚本（PKGBUILD）的形式提供，用户自己通过makepkg生成包，再由pacman安装。创建AUR的初衷是方便用户维护和分享新软件包，并由官方定期从中挑选软件包进入community仓库。</p>
<p>许多官方仓库软件包都来自AUR。通过AUR，大家相互分享新的软件包生成脚本（PKGBUILD和其他相关文件）。用户还可以为软件包投票。如果一个软件包投票足够多、没有协议问题、打包质量好，那么<br>它就很有希望被收录进官方[community]仓库（以后就可以直接通过pacman 或 abs 安装了）。</p>
<p>Yay 是用 Go 编写的 Arch Linux AUR 包管理工具。具体可以查看 <a href="https://wiki.archlinux.org/index.php/AUR_helpers" target="_blank" rel="noopener">AUR_helpers</a></p>
<p>安装 yay：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">sudo pacman -S yay</span><br></pre></td></tr></table></figure>

<p>配置 yay 的 aur 源为<a href="https://mirror.tuna.tsinghua.edu.cn/help/AUR/" target="_blank" rel="noopener">清华源 AUR 镜像</a>：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">yay --aururl <span class="string">"https://aur.tuna.tsinghua.edu.cn"</span> --save</span><br></pre></td></tr></table></figure>

<p>修改的配置文件位于 <code>~/.config/yay/config.json</code> ，还可通过以下命令查看修改过的配置:</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">yay -P -g</span><br></pre></td></tr></table></figure>

<p>yay的兼容Pacman命令</p>
<p>yay 安装命令不需要加 <code>sudo</code>。</p>
<h3 id="参考文档"><a href="#参考文档" class="headerlink" title="参考文档"></a>参考文档</h3><p><a href="https://wiki.archlinux.org/index.php/Arch_User_Repository_(%E7%AE%80%E4%BD%93%E4%B8%AD%E6%96%87)" target="_blank" rel="noopener">AUR wiki</a></p>
<p><a href="https://zh.wikipedia.org/wiki/Manjaro_Linux" target="_blank" rel="noopener">Manjaro wiki</a></p>
]]></content>
      <categories>
        <category>Linux</category>
      </categories>
      <tags>
        <tag>Manjaro</tag>
        <tag>Linux</tag>
      </tags>
  </entry>
  <entry>
    <title>Java配合Groovy实现动态公式计算</title>
    <url>/post/7b75058.html</url>
    <content><![CDATA[<p>最近项目用到了动态公式计算，也就是页面配置一个公式，服务端进行计算。我们后端使用Java，Java处理这部分公式可以使用表达式引擎Aviator，Jexl。也可以以使用JVM上的脚本语言像Jython，Groovy。表达式引擎不是一门单独的语言，局限性比较高，由于我对Python比较熟悉，最开始准备使用Jython，查了查资料发现和Java配合不是很好，最终决定使用Groovy，Groovy兼容Java的语法，不熟悉的地方可以直接使用Java语法。</p>
<a id="more"></a>

<p>可以使用<code>GroovyShell</code>根据公式求值</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 绑定公式中的变量和对应的值</span></span><br><span class="line">Binding binding = <span class="keyword">new</span> Binding()</span><br><span class="line">binding.setVariable(<span class="string">"a"</span>,<span class="number">1</span>)</span><br><span class="line">binding.setVariable(<span class="string">"b"</span>,<span class="number">2</span>)</span><br><span class="line"></span><br><span class="line">GroovyShell shell = <span class="keyword">new</span> GroovyShell(binding);</span><br><span class="line"></span><br><span class="line"><span class="comment">// 进行计算 返回值是3</span></span><br><span class="line">def result = shell.evaluate(<span class="string">'a + b'</span>)</span><br></pre></td></tr></table></figure>


<p>接口使用Java定义</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">interface</span> <span class="title">DynamicFormulaService</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * 根据公式和source中的值计算</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@param</span> source 源数据</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@param</span> formulaMap 公式</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@return</span></span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    List&lt;Map&lt;String, Object&gt;&gt; compute(List&lt;Map&lt;String, Object&gt;&gt; source , Map&lt;String, String&gt; formulaMap);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><code>source</code>为要被统计的源数据列表，<code>key</code>为公式中变量，如</p>
<p><code>{ price : 1.5 , quantity : 400 , total : null }</code></p>
<p><code>formulaMap</code>中的key为结果，value为公式，如</p>
<p><code>{ total : price * quantity }</code></p>
<p>返回的结果为<code>{ price : 1.5 , quantity : 400 , total : 600 }</code></p>
<p>在计算数值之前，会先遍历<code>formulaMap</code>，将<code>value</code>中有计算公式的数据进行递归拆分，完成嵌套公式解析</p>
<p>如<code>{a : b * c , b : d * e}</code>解析的结果为<code>{a : ( d * e ) * c , b : d * e}</code></p>
<p>具体的实现使用Groovy</p>
<figure class="highlight groovy"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">DynamicFormulaServiceImpl</span> <span class="keyword">implements</span> <span class="title">DynamicFormulaService</span> &#123;</span></span><br><span class="line"></span><br><span class="line">    <span class="meta">@Autowired</span></span><br><span class="line">    <span class="keyword">private</span> IBaseTargetService baseTargetService;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 匹配加减乘除符号</span></span><br><span class="line">    <span class="keyword">def</span> pattern = Pattern.compile(<span class="string">'[\\+\\-\\*\\/\\(\\)]'</span>)</span><br><span class="line">    <span class="comment">// 加减乘除符号</span></span><br><span class="line">    <span class="keyword">def</span> formulaString = <span class="string">'''+-*/()'''</span></span><br><span class="line"></span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    List&lt;Map&lt;String, Object&gt;&gt; compute(List&lt;Map&lt;String, Object&gt;&gt; source, Map&lt;String, String&gt; formulaMap) &#123;</span><br><span class="line">        formulaMap.each &#123; k, v -&gt;</span><br><span class="line">            <span class="comment">// 递归格式化公式</span></span><br><span class="line">            formulaMap.put(k,formatFormula(formulaMap, k))</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        println formulaMap</span><br><span class="line"></span><br><span class="line">        <span class="keyword">def</span> result = <span class="keyword">this</span>.compute(source, formulaMap)</span><br><span class="line"></span><br><span class="line">        <span class="keyword">return</span> result</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">def</span> formatFormula(Map&lt;String, String&gt; formulaMap, String key) &#123;</span><br><span class="line">        String formula = formulaMap.get(key)</span><br><span class="line">        <span class="comment">// 是否含有公式</span></span><br><span class="line">        <span class="keyword">if</span> (!formula || formula.matches(pattern)) <span class="keyword">return</span> key</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 将变量用空格分隔方便替换</span></span><br><span class="line">        formula = <span class="string">" $&#123;formula&#125; "</span></span><br><span class="line">        formulaString.chars.each &#123; c -&gt;</span><br><span class="line">            formula = formula.replace(c.toString(), <span class="string">" $&#123;c&#125; "</span>.toString())</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        formula.splitEachLine(pattern, &#123; v -&gt;</span><br><span class="line">            v.each &#123;</span><br><span class="line">                k -&gt;</span><br><span class="line">                    k = k.trim()</span><br><span class="line">                    <span class="keyword">if</span> (StringUtils.isEmpty(k) || StringUtils.isNumeric(k)) <span class="keyword">return</span></span><br><span class="line">                    <span class="comment">// 精确匹配替换</span></span><br><span class="line">                    formula = formula.replaceFirst(<span class="string">" $&#123;k&#125; "</span>, formatFormula(formulaMap, k))</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;)</span><br><span class="line">        <span class="keyword">return</span> <span class="string">"($&#123;formula&#125;)"</span></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">def</span> compute(List&lt;Map&lt;String, Object&gt;&gt; source, formulaMap) &#123;</span><br><span class="line">        source.each &#123; e -&gt;</span><br><span class="line">            Binding binding = <span class="keyword">new</span> Binding(e)</span><br><span class="line">            formulaMap.each &#123; k, v -&gt;</span><br><span class="line">                <span class="keyword">if</span> (e.containsKey(k)) &#123;</span><br><span class="line">                    GroovyShell shell = <span class="keyword">new</span> GroovyShell(binding);</span><br><span class="line">                    <span class="keyword">def</span> result = shell.evaluate(v)</span><br><span class="line">                    println <span class="string">"$&#123;k&#125; : $&#123;result&#125;"</span></span><br><span class="line">                    e.put(k, result)</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> source</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><code>DynamicFormulaServiceImpl</code>还不能直接使用，要将它作为一个bean交给Spring统一管理，Spring也提供了对Groovy的支持</p>
<p>appcontextContext.xml</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;?xml version="1.0" encoding="UTF-8"?&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">beans</span> <span class="attr">xmlns</span>=<span class="string">"http://www.springframework.org/schema/beans"</span> <span class="attr">xmlns:xsi</span>=<span class="string">"http://www.w3.org/2001/XMLSchema-instance"</span></span></span><br><span class="line"><span class="tag">       <span class="attr">xmlns:lang</span>=<span class="string">"http://www.springframework.org/schema/lang"</span></span></span><br><span class="line"><span class="tag">       <span class="attr">xsi:schemaLocation</span>=<span class="string">"</span></span></span><br><span class="line"><span class="tag"><span class="string">        http://www.springframework.org/schema/beans https://www.springframework.org/schema/beans/spring-beans.xsd</span></span></span><br><span class="line"><span class="tag"><span class="string">        http://www.springframework.org/schema/lang https://www.springframework.org/schema/lang/spring-lang.xsd"</span>&gt;</span></span><br><span class="line"></span><br><span class="line">    <span class="tag">&lt;<span class="name">lang:groovy</span> <span class="attr">id</span>=<span class="string">"dynamicFormulaService"</span> <span class="attr">script-source</span>=<span class="string">"classpath:groovy/DynamicFormulaServiceImpl.groovy"</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">lang:groovy</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">beans</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>Spring Framework 4.0 也支持<code>beans{}</code>写法</p>
<p>applicationContext.groovy</p>
<figure class="highlight groovy"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> groovy.DynamicFormulaServiceImpl</span><br><span class="line">beans &#123;</span><br><span class="line">    dynamicFormulaService(DynamicFormulaServiceImpl)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>好了，现在我们的动态公式功能就可以使用了，由于Groovy我也是第一次使用，很多东西都是现用现查，用起来还不太灵活。之后我会深入学习一下，把我感觉有趣的东西写出来。</p>
<h3 id="参考文档"><a href="#参考文档" class="headerlink" title="参考文档"></a>参考文档</h3><p><a href="https://docs.spring.io/spring-framework/docs/current/spring-framework-reference/languages.html#groovy" target="_blank" rel="noopener">Spring Apache Groovy</a></p>
<p><a href="https://docs.spring.io/spring-boot/docs/current/reference/html/spring-boot-cli.html#cli-groovy" target="_blank" rel="noopener">Developing Applications with the Groovy Beans DSL</a></p>
<p><a href="https://docs.spring.io/spring-framework/docs/current/spring-framework-reference/core.html#groovy-bean-definition-dsl" target="_blank" rel="noopener">The Groovy Bean Definition DSL</a></p>
]]></content>
      <categories>
        <category>Java</category>
      </categories>
      <tags>
        <tag>Groovy</tag>
        <tag>Spring</tag>
      </tags>
  </entry>
  <entry>
    <title>Docker复现RUN命令</title>
    <url>/post/89a34fac.html</url>
    <content><![CDATA[<p>如果有这样以一个场景。我使用docker run启动了了一个redis容器，而docker run的参数有很多，由于时间太久，我已经忘记了当初的启动参数，也并没有使用docker-compose这样的编排工具。现在我如何找回此前的启动命令？</p>
<a id="more"></a>


<p>我可以使用<code>inspect</code>命令查看容器的元数据，根据元数据反推出运行的参数。</p>
<figure class="highlight sh"><table><tr><td class="code"><pre><span class="line">docker inspect 1cc825a43022</span><br><span class="line"></span><br><span class="line">[</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="string">"Id"</span>: <span class="string">"1cc825a430226187c957b18c3006b4e3346cf143f43513ee71647011bbd98cfe"</span>,</span><br><span class="line">        <span class="string">"Created"</span>: <span class="string">"2019-10-12T05:54:40.635365198Z"</span>,</span><br><span class="line">        <span class="string">"Path"</span>: <span class="string">"docker-entrypoint.sh"</span>,</span><br><span class="line">        <span class="string">"Args"</span>: [</span><br><span class="line">            <span class="string">"redis-server"</span>,</span><br><span class="line">            <span class="string">"--appendonly"</span>,</span><br><span class="line">            <span class="string">"yes"</span></span><br><span class="line">        ],</span><br><span class="line">        <span class="string">"State"</span>: &#123;</span><br><span class="line">            <span class="string">"Status"</span>: <span class="string">"running"</span>,</span><br><span class="line">            <span class="string">"Running"</span>: <span class="literal">true</span>,</span><br><span class="line">            <span class="string">"Paused"</span>: <span class="literal">false</span>,</span><br><span class="line">            <span class="string">"Restarting"</span>: <span class="literal">false</span>,</span><br><span class="line">            <span class="string">"OOMKilled"</span>: <span class="literal">false</span>,</span><br><span class="line">            ...</span><br></pre></td></tr></table></figure>

<p>但实际使用不是很方便，因为inspect返回的数据实在太多，找到数据后还要手工的反推出启动时的命令，费时又容易出错。这个时候我们可以借助<code>runlike</code>这个小工具。</p>
<p><code>runlike</code>使用python开发，我们直接使用pip安装</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">pip install runlike</span><br><span class="line"></span><br><span class="line"><span class="comment"># 安装成功后直接运行，使用`-p`参数可将返回值美观一点</span></span><br><span class="line">runlike -p 1cc825a43022</span><br><span class="line"></span><br><span class="line">docker run \</span><br><span class="line">        --name=jolly_mirzakhani \</span><br><span class="line">        --hostname=1cc825a43022 \</span><br><span class="line">        --env=PATH=/usr/<span class="built_in">local</span>/sbin:/usr/<span class="built_in">local</span>/bin:/usr/sbin:/usr/bin:/sbin:/bin \</span><br><span class="line">        --env=GOSU_VERSION=1.11 \</span><br><span class="line">        --env=REDIS_VERSION=4.0.14 \</span><br><span class="line">        --env=REDIS_DOWNLOAD_URL=http://download.redis.io/releases/redis-4.0.14.tar.gz \</span><br><span class="line">        --env=REDIS_DOWNLOAD_SHA=1e1e18420a86cfb285933123b04a82e1ebda20bfb0a289472745a087587e93a7 \</span><br><span class="line">        --volume=/usr/<span class="built_in">local</span>/docker/data:/data \</span><br><span class="line">        --volume=/data \</span><br><span class="line">        -p 6379:6379 \</span><br><span class="line">        --restart=always \</span><br><span class="line">        --detach=<span class="literal">true</span> \</span><br><span class="line">        redis:4 \</span><br><span class="line">        redis-server --appendonly yes</span><br></pre></td></tr></table></figure>

]]></content>
      <categories>
        <category>Linux</category>
      </categories>
      <tags>
        <tag>Docker</tag>
      </tags>
  </entry>
  <entry>
    <title>Windows下的包管理工具</title>
    <url>/post/82ab8e0c.html</url>
    <content><![CDATA[<p>Centos安装软件可使用yum，Ubuntu上可使用apt，MacOS可使用brew。Windows平台也有开发者开发对应的安装程序——Scoop，Scoop类似MacOS平台的brew，没有brew强大，但常见开发环境都可以轻松搞定。</p>
<p>使用scoop安装常用软件，可直接敲命令</p>
<figure class="highlight powershell"><table><tr><td class="code"><pre><span class="line">scoop install git curl <span class="number">7</span>zip</span><br></pre></td></tr></table></figure>

<a id="more"></a>

<h3 id="安装Scoop"><a href="#安装Scoop" class="headerlink" title="安装Scoop"></a>安装Scoop</h3><p>设置安装目录，非必须，默认安装在用户目录</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">$env:SCOOP&#x3D;&#39;D:\ProgramFile\scoop&#39;</span><br><span class="line">[environment]::setEnvironmentVariable(&#39;SCOOP&#39;,$env:SCOOP,&#39;User&#39;)</span><br></pre></td></tr></table></figure>

<p>在 PowerShell 中输入下面内容，保证允许本地脚本的执行</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">set-executionpolicy remotesigned -scope currentuser</span><br></pre></td></tr></table></figure>

<p>安装scoop</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">iex (new-object net.webclient).downloadstring(&#39;https:&#x2F;&#x2F;get.scoop.sh&#39;)</span><br></pre></td></tr></table></figure>



<p>Scoop 可以利用<a href="https://github.com/aria2/aria2" target="_blank" rel="noopener"><code>aria2</code></a> 进行多线程下载。</p>
<figure class="highlight powershell"><table><tr><td class="code"><pre><span class="line">scoop install aria2</span><br></pre></td></tr></table></figure>

<p>可以使用<code>scoop config</code>对aria2进行配置</p>
<ul>
<li>aria2-enabled (default: true)</li>
<li><a href="https://aria2.github.io/manual/en/html/aria2c.html#cmdoption-retry-wait" target="_blank" rel="noopener">aria2-retry-wait</a> (default: 2)</li>
<li><a href="https://aria2.github.io/manual/en/html/aria2c.html#cmdoption-s" target="_blank" rel="noopener">aria2-split</a> (default: 5)</li>
<li><a href="https://aria2.github.io/manual/en/html/aria2c.html#cmdoption-x" target="_blank" rel="noopener">aria2-max-connection-per-server</a> (default: 5)</li>
<li><a href="https://aria2.github.io/manual/en/html/aria2c.html#cmdoption-k" target="_blank" rel="noopener">aria2-min-split-size</a> (default: 5M)</li>
</ul>
<p>代理设置 </p>
<figure class="highlight powershell"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 设置代理</span></span><br><span class="line">scoop config proxy localhost:<span class="number">1081</span></span><br><span class="line"><span class="comment"># 删除代理</span></span><br><span class="line">scoop config rm proxy</span><br></pre></td></tr></table></figure>

<p>添加<code>buckets</code><br>buckets类似Linux中的源，默认包含<code>main</code>,可手动添加其他bucket</p>
<figure class="highlight powershell"><table><tr><td class="code"><pre><span class="line">scoop bucket add extras</span><br><span class="line">scoop bucket add versions</span><br><span class="line">scoop bucket add dorado https://github.com/h404bi/dorado</span><br></pre></td></tr></table></figure>
<p>bucket收录列表</p>
<p><a href="https://rasa.github.io/scoop-directory/by-score.html" target="_blank" rel="noopener">https://rasa.github.io/scoop-directory/by-score.html</a></p>
]]></content>
      <tags>
        <tag>工具</tag>
      </tags>
  </entry>
  <entry>
    <title>基于Java NIO实现SOCKS代理协议</title>
    <url>/post/e99ab4dc.html</url>
    <content><![CDATA[<p>SOCKS是一种网络传输协议，主要用于客户端与外网服务器之间通讯的中间传递。<br>当防火墙后的客户端要访问外部的服务器时，就跟SOCKS代理服务器连接。这个代理服务器控制客户端访问外网的资格，允许的话，就将客户端的请求发往外部的服务器。</p>
<a id="more"></a>

<h3 id="SOCKS协议解析"><a href="#SOCKS协议解析" class="headerlink" title="SOCKS协议解析"></a>SOCKS协议解析</h3><p>SOCKS代理需要以下几个步骤</p>
<p>1 握手，客户端与代理协商SOCKS5版本及账号密码</p>
<p>2 请求建立连接，客户端将发送要请求的远程地址发送给代理</p>
<p>3 发送数据包给代理服务，代理进行转发后，将目标服务的数据返回</p>
<p><img src="https://img.yjll.art/img/sock5.png" alt="sock5"></p>
<p>根据OSI模型，SOCKS是会话层的协议，位于表示层与传输层之间。</p>
<h3 id="OSI网络模型"><a href="#OSI网络模型" class="headerlink" title="OSI网络模型"></a>OSI网络模型</h3><p><img src="https://img.yjll.art/img/OSI_model.jpg" alt="OSI_model"></p>
<h3 id="Shadowsocks工作原理"><a href="#Shadowsocks工作原理" class="headerlink" title="Shadowsocks工作原理"></a>Shadowsocks工作原理</h3><p>Shadowsocks是基于Python开发的科学上网软件。Shadowsocks实现了SOCKS协议，SOCKS协议本身是没有加密功能的，为了防止GFW根据报文对数据进行拦截，所以要对数据进行加密。<br>Shadowsocks分为 <code>SS Local</code> 和 <code>SS Server</code>。</p>
<ul>
<li><code>SS Local</code>负责通过SOCKS协议与客户端交互，将客户端(浏览器)请求的数据加密发送给<code>SS Server</code>和将<code>SS Server</code>响应的数据解密返回给客户端。</li>
<li><code>SS Server</code>负责将<code>SS Local</code>请求的数据解密，发送给目标网站(例如google.com)，并将目标网站的响应进行加密返回。</li>
</ul>
<p><img src="https://img.yjll.art/img/Shadowsocks.jpg" alt="Shadowsocks"></p>
<h3 id="NIO实现"><a href="#NIO实现" class="headerlink" title="NIO实现"></a>NIO实现</h3><p>首先我们要先创建一个选择器和一个ServerSocketChannel，ServerSocketChannel订阅ACCEPT事件注册到选择器上，<code>selector.select()</code>会阻塞直到有请求到来时。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">execute</span><span class="params">()</span> <span class="keyword">throws</span> IOException </span>&#123;</span><br><span class="line"></span><br><span class="line">    log.info(<span class="string">"starting server at port &#123;&#125;"</span>, ssConfig.getLocalPort());</span><br><span class="line"></span><br><span class="line">    Selector selector = Selector.open();</span><br><span class="line">    ServerSocketChannel serverSocketChannel = ServerSocketChannel.open();</span><br><span class="line">    serverSocketChannel.socket().bind(<span class="keyword">new</span> InetSocketAddress(ssConfig.getLocalPort()));</span><br><span class="line">    <span class="comment">// 非阻塞</span></span><br><span class="line">    serverSocketChannel.configureBlocking(<span class="keyword">false</span>);</span><br><span class="line">    <span class="comment">// 订阅事件，当有请求时激活</span></span><br><span class="line">    serverSocketChannel.register(selector, SelectionKey.OP_ACCEPT);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">while</span> (<span class="keyword">true</span>) &#123;</span><br><span class="line">        <span class="comment">// 阻塞线程，直到有订阅的事件</span></span><br><span class="line">        <span class="keyword">int</span> count = selector.select();</span><br><span class="line">        <span class="keyword">if</span> (count &lt;= <span class="number">0</span>) <span class="keyword">continue</span>;</span><br><span class="line"></span><br><span class="line">        Iterator&lt;SelectionKey&gt; iterator = selector.selectedKeys().iterator();</span><br><span class="line">        <span class="keyword">while</span> (iterator.hasNext()) &#123;</span><br><span class="line"></span><br><span class="line">            SelectionKey selectionKey = iterator.next();</span><br><span class="line">            iterator.remove();</span><br><span class="line">            </span><br><span class="line">            <span class="keyword">if</span> (!selectionKey.isValid()) <span class="keyword">continue</span>;</span><br><span class="line"></span><br><span class="line">            <span class="comment">// 一个新的连接进来时，</span></span><br><span class="line">            <span class="keyword">if</span> (selectionKey.isAcceptable()) &#123;</span><br><span class="line">                ServerSocketChannel channel = (ServerSocketChannel) selectionKey.channel();</span><br><span class="line">                <span class="comment">// 新进来的SocketChannel                    </span></span><br><span class="line">                SocketChannel socketChannel = channel.accept();</span><br><span class="line">                socketChannel.configureBlocking(<span class="keyword">false</span>);</span><br><span class="line">                <span class="comment">// 我们希望从中获取数据，我们注册读事件，当数据可读时会通知我们</span></span><br><span class="line">                <span class="comment">// 并绑定一个处理器，当数据可读时，使用这个处理器进行数据处理</span></span><br><span class="line">                socketChannel.register(selector, SelectionKey.OP_READ, <span class="keyword">new</span> ChannelHandler(ssConfig, selector, socketChannel));</span><br><span class="line">            &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">                <span class="comment">// 交由对应的处理器来处理</span></span><br><span class="line">                ChannelHandler channelHandler = (ChannelHandler) selectionKey.attachment();</span><br><span class="line">                <span class="keyword">if</span> (Objects.nonNull(channelHandler)) &#123;</span><br><span class="line">                    channelHandler.handle(selectionKey);</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>处理器将根据事件状态进行分发</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">handle</span><span class="params">(SelectionKey key)</span> </span>&#123;</span><br><span class="line">    SocketChannel channel = (SocketChannel) key.channel();</span><br><span class="line">    <span class="keyword">try</span> &#123;</span><br><span class="line">        <span class="comment">// 与浏览器交互的Channel</span></span><br><span class="line">        <span class="keyword">if</span> (localSocketChannel == channel) &#123;</span><br><span class="line">            <span class="comment">// 当有request时，读取数据</span></span><br><span class="line">            <span class="keyword">if</span> (key.isReadable()) &#123;</span><br><span class="line">                onLocalRead(channel);</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="comment">// 与远程服务交互的Channel</span></span><br><span class="line">        &#125; <span class="keyword">else</span> <span class="keyword">if</span> (remoteSocketChannel == channel) &#123;</span><br><span class="line">            <span class="comment">// 远程可连接时，发送加密数据包</span></span><br><span class="line">            <span class="keyword">if</span> (key.isConnectable()) &#123;</span><br><span class="line">                onRemoteConnect(key, channel);</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="comment">// 当远程返回数据时，进行解密，将解密数据返回给浏览器</span></span><br><span class="line">            <span class="keyword">if</span> (key.isReadable()) &#123;</span><br><span class="line">                onRemoteRead(channel);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125; <span class="keyword">catch</span> (Exception e) &#123;</span><br><span class="line">        log.error(e.toString(), e);</span><br><span class="line">        <span class="keyword">this</span>.stage = SocksStage.STAGE_DESTROYED;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 销毁</span></span><br><span class="line">    <span class="keyword">if</span> (<span class="keyword">this</span>.stage == SocksStage.STAGE_DESTROYED) &#123;</span><br><span class="line">        destroy();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>与浏览器交互的Channel可读取时表示有新的数据到来，需从Channel读取数据，并写入响应数据</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">private</span> <span class="keyword">void</span> <span class="title">onLocalRead</span><span class="params">(SocketChannel socketChannel)</span> <span class="keyword">throws</span> IOException </span>&#123;</span><br><span class="line">        SocksStage socksStage = <span class="keyword">this</span>.stage;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">int</span> read = socketChannel.read(byteBuffer);</span><br><span class="line">        <span class="keyword">byte</span>[] readData = ByteBuffers.convertByte(byteBuffer);</span><br><span class="line">        <span class="keyword">if</span> (readData.length == <span class="number">0</span>) &#123;</span><br><span class="line">            <span class="keyword">this</span>.stage = SocksStage.STAGE_DESTROYED;</span><br><span class="line">            <span class="keyword">return</span>;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 通过状态控制socks5交互的步骤</span></span><br><span class="line">        <span class="keyword">switch</span> (socksStage) &#123;</span><br><span class="line">            <span class="comment">/*</span></span><br><span class="line"><span class="comment">                +----+----------+----------+</span></span><br><span class="line"><span class="comment">                |VER | NMETHODS | METHODS  |</span></span><br><span class="line"><span class="comment">                +----+----------+----------+</span></span><br><span class="line"><span class="comment">                | 1  |    1     |  1~255   |</span></span><br><span class="line"><span class="comment">                +----+----------+----------+</span></span><br><span class="line"><span class="comment">             */</span></span><br><span class="line">            <span class="comment">// 握手，告知支持的协议</span></span><br><span class="line">            <span class="keyword">case</span> STAGE_INIT:</span><br><span class="line"></span><br><span class="line">                socketChannel.write(ByteBuffer.wrap(<span class="keyword">new</span> <span class="keyword">byte</span>[]&#123;<span class="number">0x05</span>, <span class="number">0x00</span>&#125;));</span><br><span class="line">                <span class="keyword">this</span>.stage = SocksStage.STAGE_ADDR;</span><br><span class="line">                <span class="keyword">break</span>;</span><br><span class="line"></span><br><span class="line">            <span class="comment">/*</span></span><br><span class="line"><span class="comment">                +----+-----+-------+------+----------+----------+</span></span><br><span class="line"><span class="comment">                |VER | CMD |  RSV  | ATYP | DST.ADDR | DST.PORT |</span></span><br><span class="line"><span class="comment">                +----+-----+-------+------+----------+----------+</span></span><br><span class="line"><span class="comment">                | 1  |  1  |   1   |  1   | Variable |    2     |</span></span><br><span class="line"><span class="comment">                +----+-----+-------+------+----------+----------+</span></span><br><span class="line"><span class="comment">             */</span></span><br><span class="line"></span><br><span class="line">            <span class="comment">// request中有要访问的域名，此处需要与远程建立连接，订阅OP_CONNECT事件，当可连接时通知到我们。</span></span><br><span class="line">            <span class="keyword">case</span> STAGE_ADDR:</span><br><span class="line"></span><br><span class="line">                remoteSocketChannel = SocketChannel.open();</span><br><span class="line">                remoteSocketChannel.configureBlocking(<span class="keyword">false</span>);</span><br><span class="line">                remoteSocketChannel.register(<span class="keyword">this</span>.selector, SelectionKey.OP_CONNECT, <span class="keyword">this</span>);</span><br><span class="line">                remoteSocketChannel.connect(<span class="keyword">new</span> InetSocketAddress(ssConfig.getServer(), ssConfig.getServerPort()));</span><br><span class="line"></span><br><span class="line">                socketChannel.write(ByteBuffer.wrap(<span class="keyword">new</span> <span class="keyword">byte</span>[]&#123;<span class="number">0x05</span>, <span class="number">0x00</span>, <span class="number">0x00</span>, <span class="number">0x01</span>, <span class="number">0x00</span>, <span class="number">0x00</span>, <span class="number">0x00</span>, <span class="number">0x00</span>, <span class="number">0x10</span>, <span class="number">0x10</span>&#125;));</span><br><span class="line">                <span class="keyword">this</span>.stage = SocksStage.STAGE_CONNECTING;</span><br><span class="line">                <span class="comment">// 0x05 0x01 0x00 0x03 0x0a b'google.com'  0x00 0x50</span></span><br><span class="line">                <span class="comment">// 0x0a 表示域名长度</span></span><br><span class="line">                Socks5Utils.parseAddr(readData);</span><br><span class="line">                <span class="comment">// 去掉无用数据,缓存向服务端请求的数据</span></span><br><span class="line">                <span class="keyword">this</span>.byteQueue.add(Arrays.copyOfRange(readData, <span class="number">3</span>, read));</span><br><span class="line">                <span class="keyword">break</span>;</span><br><span class="line">            <span class="keyword">case</span> STAGE_CONNECTING:</span><br><span class="line">                <span class="comment">// 远程未连接，但是客户端数据已经到达，将数据缓存到队列中</span></span><br><span class="line">                <span class="keyword">this</span>.byteQueue.add(readData);</span><br><span class="line">                <span class="keyword">break</span>;</span><br><span class="line">            <span class="keyword">case</span> STAGE_STREAM:</span><br><span class="line">                <span class="comment">// 将数据加密发送给远程服务器</span></span><br><span class="line">                <span class="keyword">byte</span>[] encrypt = <span class="keyword">this</span>.cryptHelper.encrypt(readData);</span><br><span class="line">                <span class="keyword">this</span>.remoteSocketChannel.write(ByteBuffer.wrap(encrypt));</span><br><span class="line">                <span class="keyword">break</span>;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure>

<p>与浏览器交互的第2步时，会与远程建立连接，订阅连接事件。此处为可连接时的处理，将数据推送给远程，并订阅读取事件准备接受远程服务器的响应</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">private</span> <span class="keyword">void</span> <span class="title">onRemoteConnect</span><span class="params">(SelectionKey key, SocketChannel socketChannel)</span> <span class="keyword">throws</span> IOException </span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (socketChannel.finishConnect()) &#123;</span><br><span class="line">        key.interestOps(SelectionKey.OP_READ);</span><br><span class="line">        <span class="keyword">this</span>.stage = SocksStage.STAGE_STREAM;</span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">byte</span>[] bytes : byteQueue) &#123;</span><br><span class="line">            <span class="keyword">byte</span>[] encrypt = <span class="keyword">this</span>.cryptHelper.encrypt(bytes);</span><br><span class="line">            socketChannel.write(ByteBuffer.wrap(encrypt));</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>远程服务可读时表示有数据返回，读取并将数据解密，返回给浏览器</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">private</span> <span class="keyword">void</span> <span class="title">onRemoteRead</span><span class="params">(SocketChannel socketChannel)</span> <span class="keyword">throws</span> IOException </span>&#123;</span><br><span class="line">    socketChannel.read(byteBuffer);</span><br><span class="line">    <span class="keyword">byte</span>[] readData = ByteBuffers.convertByte(byteBuffer);</span><br><span class="line">    <span class="keyword">if</span> (readData.length == <span class="number">0</span>) &#123;</span><br><span class="line">        <span class="keyword">this</span>.stage = SocksStage.STAGE_DESTROYED;</span><br><span class="line">        <span class="keyword">return</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">byte</span>[] decrypt = <span class="keyword">this</span>.cryptHelper.decrypt(readData);</span><br><span class="line">    <span class="keyword">this</span>.localSocketChannel.write(ByteBuffer.wrap(decrypt));</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="参考文档"><a href="#参考文档" class="headerlink" title="参考文档"></a>参考文档</h3><p><a href="https://zh.wikipedia.org/wiki/SOCKS" target="_blank" rel="noopener">wiki</a></p>
<p><a href="https://github.com/shadowsocks/shadowsocks/tree/master" target="_blank" rel="noopener">Shadowsocks</a></p>
]]></content>
      <categories>
        <category>Java</category>
      </categories>
      <tags>
        <tag>NIO</tag>
        <tag>SOCKS</tag>
      </tags>
  </entry>
  <entry>
    <title>Spring Cloud Gateway 登录拦截</title>
    <url>/post/a9515f36.html</url>
    <content><![CDATA[<p><code>Spring Cloud Gateway</code>为Spring官方基于Spring 5.0、Spring Boot2.0和Project Reactor等技术开发的网关，鉴权是网关常用的功能，本文介绍基于<code>Spring Cloud Gateway</code>的登录拦截功能。</p>
<a id="more"></a>

<p><code>GlobalFilter</code>为全局过滤器的标识接口，我这个拦截是针对所有服务，所以使用了全局过滤器。</p>
<p>实现<code>Ordered</code>为了告诉<code>Gateway</code>我这个过滤器的优先级，<code>getOrder</code>方法返回值越大优先级越低，越后执行。</p>
<p>还有要注意的是<code>Spring Cloud Gateway</code>底层使用的是<code>webflux</code>，基于响应式编程，我们也要避免代码中出现阻塞I/O的调用，所以我从Redis中取token使用的是非阻塞的<code>ReactiveRedisTemplate</code>。</p>
<blockquote>
<p>Talk is cheap,show me the code</p>
</blockquote>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Component</span></span><br><span class="line"><span class="meta">@Slf</span>4j</span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">TokenFilter</span> <span class="keyword">implements</span> <span class="title">GlobalFilter</span>, <span class="title">Ordered</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">final</span> String TOKEN_HEAD = <span class="string">"token"</span>;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * Redis中token前缀</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">final</span> String PREFIX = <span class="string">"CASECODE:USER:TOKEN:"</span>;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="keyword">long</span> TIMEOUT = <span class="number">60</span> * <span class="number">30</span>;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * 需排除的接口</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">final</span> Set&lt;String&gt; exclude_path = ImmutableSet.of(</span><br><span class="line">            <span class="string">"/sep/user/login"</span>, <span class="string">"/sep/user/getCheckCode/"</span></span><br><span class="line">    );</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">    <span class="meta">@Autowired</span></span><br><span class="line">    <span class="keyword">private</span> ReactiveRedisTemplate&lt;String, String&gt; redisTemplate;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> Mono&lt;Void&gt; <span class="title">filter</span><span class="params">(ServerWebExchange exchange, GatewayFilterChain chain)</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">return</span> Mono.just(exchange)</span><br><span class="line">                .map(ServerWebExchange::getRequest)</span><br><span class="line">                .flatMap(serverHttpRequest -&gt; &#123;</span><br><span class="line">                    String path = serverHttpRequest.getPath().toString();</span><br><span class="line">                    log.info(<span class="string">"path:&#123;&#125;"</span>,path);</span><br><span class="line"></span><br><span class="line">                    <span class="comment">// 排除不需要登录的接口</span></span><br><span class="line">                    <span class="keyword">if</span> (exclude_path.stream().anyMatch(path::contains)) &#123;</span><br><span class="line">                        log.info(<span class="string">"无需登录"</span>);</span><br><span class="line">                        <span class="keyword">return</span> chain.filter(exchange);</span><br><span class="line">                    &#125;</span><br><span class="line"></span><br><span class="line">                    <span class="comment">// 从HEAD中取token</span></span><br><span class="line">                    List&lt;String&gt; tokens = serverHttpRequest.getHeaders().get(TOKEN_HEAD);</span><br><span class="line">                    <span class="comment">// 无TOKEN</span></span><br><span class="line">                    <span class="keyword">if</span> (CollectionUtils.isEmpty(tokens)) &#123;</span><br><span class="line">                        <span class="keyword">return</span> <span class="keyword">this</span>.buildLoginResp(exchange);</span><br><span class="line">                    &#125;</span><br><span class="line"></span><br><span class="line">                    String token = Iterables.getOnlyElement(tokens);</span><br><span class="line"></span><br><span class="line">                    <span class="comment">// 拼接前缀做为RedisKey</span></span><br><span class="line">                    String redisKey = PREFIX + token;</span><br><span class="line"></span><br><span class="line">                    <span class="keyword">return</span> redisTemplate.opsForValue().get(redisKey)</span><br><span class="line">                            .defaultIfEmpty(StringUtils.EMPTY)</span><br><span class="line">                            .flatMap(user -&gt; &#123;</span><br><span class="line">                                log.info(<span class="string">"\nTOKEN:&#123;&#125;\nVALUE:&#123;&#125;"</span>, token, user);</span><br><span class="line">                                <span class="comment">// redis中有该用户</span></span><br><span class="line">                                <span class="keyword">if</span> (StringUtils.isNotEmpty(user)) &#123;</span><br><span class="line">                                    <span class="keyword">return</span> redisTemplate.expire(redisKey, Duration.of(TIMEOUT, ChronoUnit.SECONDS))</span><br><span class="line">                                            .then(chain.filter(exchange));</span><br><span class="line">                                &#125;</span><br><span class="line">                                <span class="keyword">return</span> <span class="keyword">this</span>.buildLoginResp(exchange);</span><br><span class="line">                            &#125;);</span><br><span class="line">                &#125;);</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">int</span> <span class="title">getOrder</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">private</span> Mono&lt;Void&gt; <span class="title">buildLoginResp</span><span class="params">(ServerWebExchange exchange)</span> </span>&#123;</span><br><span class="line">        ServerHttpResponse response = exchange.getResponse();</span><br><span class="line"></span><br><span class="line">        Map&lt;String, Object&gt; map = <span class="keyword">new</span> HashMap&lt;&gt;();</span><br><span class="line">        map.put(<span class="string">"code"</span>, <span class="number">401</span>);</span><br><span class="line">        map.put(<span class="string">"message"</span>, <span class="string">"无效token"</span>);</span><br><span class="line"></span><br><span class="line">        String json = <span class="keyword">new</span> Gson().toJson(map);</span><br><span class="line">        <span class="keyword">byte</span>[] bytes = json.getBytes(StandardCharsets.UTF_8);</span><br><span class="line">        DataBuffer buffer = response.bufferFactory().wrap(bytes);</span><br><span class="line">        response.setStatusCode(HttpStatus.OK);</span><br><span class="line">        <span class="keyword">return</span> response.writeWith(Flux.just(buffer));</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



]]></content>
      <categories>
        <category>Java</category>
      </categories>
      <tags>
        <tag>Spring Cloud</tag>
        <tag>Gateway</tag>
      </tags>
  </entry>
  <entry>
    <title>Spring Cloud OpenFeign 上传文件</title>
    <url>/post/4c4b951a.html</url>
    <content><![CDATA[<p><a href="https://github.com/OpenFeign/feign" target="_blank" rel="noopener">Feign</a>是一个声明式的伪Http客户端，它使得写Http客户端变得更简单。</p>
<p><code>Spring Cloud OpenFeign</code>是<code>Spring Could</code>服务间调用的基础组件,将Feign进行了封装，更便于使用，本文使用<code>Spring Cloud OpenFeign</code>进行文件上传。</p>
<a id="more"></a>

<blockquote>
<p><code>spring-cloud-openfeign</code>版本<code>2.1.1.RELEASE</code></p>
</blockquote>
<p>声明使用Form表单形式提交</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Configuration</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">MultipartSupportConfig</span> </span>&#123;</span><br><span class="line">    <span class="meta">@Bean</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> Encoder <span class="title">feignFormEncoder</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">new</span> SpringFormEncoder();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><code>consumes</code>指定<code>MediaType</code>为<code>multipart/form-data</code></p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@FeignClient</span>(name = <span class="string">"test"</span>, url = <span class="string">"http://localhost:8141"</span>, path = <span class="string">"p/c"</span>, configuration = MultipartSupportConfig<span class="class">.<span class="keyword">class</span>)</span></span><br><span class="line"><span class="class"><span class="title">public</span> <span class="title">interface</span> <span class="title">UploadFileClient</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@RequestMapping</span>(path = <span class="string">"uploadPrintedCodesFile"</span>, consumes = MULTIPART_FORM_DATA_VALUE)</span><br><span class="line">    <span class="function">Object <span class="title">uploadPrintedCodesFile</span><span class="params">(UploadData uploadData)</span></span>;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>我将文件放在了对象中作为form表单中的一项进行提交</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Autowired</span></span><br><span class="line">   <span class="keyword">private</span> UploadFileClient uploadFileClient;</span><br><span class="line"></span><br><span class="line">   <span class="meta">@Test</span></span><br><span class="line">   <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">test</span><span class="params">()</span></span>&#123;</span><br><span class="line"></span><br><span class="line">       MultipartFile result = <span class="keyword">new</span> MockMultipartFile(name,</span><br><span class="line">               originalFileName, contentType, content);</span><br><span class="line"></span><br><span class="line">       UploadData uploadData = <span class="keyword">new</span> UploadData();</span><br><span class="line">       uploadData.setFactoryNo(<span class="string">"test"</span>);</span><br><span class="line">       uploadData.setFile(result);</span><br><span class="line">       uploadFileClient.uploadPrintedCodesFile(uploadData);</span><br><span class="line"></span><br><span class="line">   &#125;</span><br></pre></td></tr></table></figure>

<p>如果只是想提交文件，方法可以这样写,将<code>MultipartFile</code>传入即可</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@FeignClient</span>(name = <span class="string">"test"</span>, url = <span class="string">"http://localhost:8141"</span>, path = <span class="string">"p/c"</span>, configuration = MultipartSupportConfig<span class="class">.<span class="keyword">class</span>)</span></span><br><span class="line"><span class="class"><span class="title">public</span> <span class="title">interface</span> <span class="title">UploadFileClient</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@RequestMapping</span>(path = <span class="string">"uploadPrintedCodesFile"</span>, consumes = MULTIPART_FORM_DATA_VALUE)</span><br><span class="line">    <span class="function">Object <span class="title">uploadPrintedCodesFile</span><span class="params">(MultipartFile file)</span></span>;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



]]></content>
      <categories>
        <category>Java</category>
      </categories>
      <tags>
        <tag>Spring Cloud</tag>
        <tag>Feign</tag>
      </tags>
  </entry>
  <entry>
    <title>Docker使用宿主代理</title>
    <url>/post/21f939f5.html</url>
    <content><![CDATA[<p><img src="https://img.yjll.art/img/20200428102310.png" alt="20200428102310"></p>
<a id="more"></a>

<p>在docker中安装软件，下载地址在外国，下载速度奇慢，这个时候想通过宿主的代理访问。</p>
<p>Docker启动容器，默认使用bridge模式分配网络，会在宿主机安装一个虚拟网卡docker0。我们通过<code>ifconfig</code>命令查看一下</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">docker0: flags&#x3D;4163&lt;UP,BROADCAST,RUNNING,MULTICAST&gt;  mtu 1500</span><br><span class="line">        inet 172.17.0.1  netmask 255.255.0.0  broadcast 172.17.255.255</span><br><span class="line">        inet6 fe80::42:caff:fefe:2f7e  prefixlen 64  scopeid 0x20&lt;link&gt;</span><br><span class="line">        ether 02:42:ca:fe:2f:7e  txqueuelen 0  (Ethernet)</span><br><span class="line">        RX packets 7941959  bytes 525663055 (525.6 MB)</span><br><span class="line">        RX errors 0  dropped 0  overruns 0  frame 0</span><br><span class="line">        TX packets 8654991  bytes 11073643780 (11.0 GB)</span><br><span class="line">        TX errors 0  dropped 0 overruns 0  carrier 0  collisions 0</span><br></pre></td></tr></table></figure>

<p>对于容器来说，宿主的IP地址为<code>172.17.0.1</code>，我们进入容器进行代理配置</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">export http_proxy&#x3D;http:172.17.0.1:1081</span><br><span class="line">export https_proxy&#x3D;http:172.17.0.1:1081</span><br></pre></td></tr></table></figure>

<p>好了，可以愉快的乱搞了。</p>
]]></content>
      <categories>
        <category>Linux</category>
      </categories>
      <tags>
        <tag>Docker</tag>
      </tags>
  </entry>
  <entry>
    <title>RDS MySQL 物理备份文件恢复到自建数据库</title>
    <url>/post/236702d.html</url>
    <content><![CDATA[<p>阿里云提供了定期备份数据库的功能，可以在阿里云的控制台将数据库恢复到备份点，那我们想将这部分数据导入到我们自建的数据库怎么操作呢？官方推荐使用开源软件Percona Xtrabackup对数据库进行恢复，Percona Xtrabackup目前没有Windows版本，但是我们可以使用Docker进行恢复。</p>
<a id="more"></a>

<h3 id="准备工作"><a href="#准备工作" class="headerlink" title="准备工作"></a>准备工作</h3><p>在阿里云的后台下载备份文件</p>
<p><img src="https://img.yjll.art/img/bak.png" alt="bak"></p>
<p>生产环境的数据库使用的Mysql 8.0.16，我直接通过docker运行，并直接进入容器内部。</p>
<pre><code>docker run -v /root/bak.xb:/root/bak.xb  -p 3308:3306 -it mysql:8.0.16 bash</code></pre><p>先更换<a href="https://mirrors.163.com/.help/debian.html" target="_blank" rel="noopener">163源</a>方便安装软件</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">cat &gt;&gt;&#x2F;etc&#x2F;apt&#x2F;sources.list&lt;&lt;EOF</span><br><span class="line">deb http:&#x2F;&#x2F;mirrors.163.com&#x2F;debian&#x2F; stretch main non-free contrib</span><br><span class="line">deb http:&#x2F;&#x2F;mirrors.163.com&#x2F;debian&#x2F; stretch-updates main non-free contrib</span><br><span class="line">deb http:&#x2F;&#x2F;mirrors.163.com&#x2F;debian&#x2F; stretch-backports main non-free contrib</span><br><span class="line">deb-src http:&#x2F;&#x2F;mirrors.163.com&#x2F;debian&#x2F; stretch main non-free contrib</span><br><span class="line">deb-src http:&#x2F;&#x2F;mirrors.163.com&#x2F;debian&#x2F; stretch-updates main non-free contrib</span><br><span class="line">deb-src http:&#x2F;&#x2F;mirrors.163.com&#x2F;debian&#x2F; stretch-backports main non-free contrib</span><br><span class="line">deb http:&#x2F;&#x2F;mirrors.163.com&#x2F;debian-security&#x2F; stretch&#x2F;updates main non-free contrib</span><br><span class="line">deb-src http:&#x2F;&#x2F;mirrors.163.com&#x2F;debian-security&#x2F; stretch&#x2F;updates main non-free contrib</span><br><span class="line">EOF</span><br></pre></td></tr></table></figure>

<p>安装我们的主角percona-xtrabackup用户恢复文件，直接下载deb包进行安装</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line">apt -y install lsb-release wget aria2 vim</span><br><span class="line"></span><br><span class="line">wget https:&#x2F;&#x2F;repo.percona.com&#x2F;apt&#x2F;percona-release_latest.$(lsb_release -sc)_all.deb</span><br><span class="line"></span><br><span class="line">dpkg -i percona-release_latest.$(lsb_release -sc)_all.deb</span><br><span class="line"></span><br><span class="line">apt-get update</span><br><span class="line"></span><br><span class="line">apt-get -y install percona-xtrabackup-80</span><br></pre></td></tr></table></figure>


<p>创建解压目录，官方文档中使用的这个路径，我懒得改了，就创建了一个</p>
<pre><code>mkdir -p /home/mysql/data</code></pre><p>开始解压</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">先安装qpress解压软件</span><br><span class="line">aria2c http:&#x2F;&#x2F;www.quicklz.com&#x2F;qpress-11-linux-x64.tar</span><br><span class="line">tar xvf qpress-11-linux-x64.tar</span><br><span class="line">chmod 775 qpress</span><br><span class="line">cp qpress &#x2F;usr&#x2F;bin</span><br><span class="line"></span><br><span class="line">cat bak.xb | xbstream -x -v -C &#x2F;home&#x2F;mysql&#x2F;data</span><br><span class="line"></span><br><span class="line">xtrabackup --decompress --remove-original --target-dir&#x3D;&#x2F;home&#x2F;mysql&#x2F;data</span><br></pre></td></tr></table></figure>


<p>解压后进行数据恢复</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line"># 删除原数据库中数据</span><br><span class="line">rm -rf &#x2F;var&#x2F;lib&#x2F;mysql&#x2F;*</span><br><span class="line"></span><br><span class="line">xtrabackup --prepare --target-dir&#x3D;&#x2F;home&#x2F;mysql&#x2F;data</span><br><span class="line">xtrabackup --datadir&#x3D;&#x2F;var&#x2F;lib&#x2F;mysql --copy-back --target-dir&#x3D;&#x2F;home&#x2F;mysql&#x2F;data</span><br></pre></td></tr></table></figure>

<p>自建数据库不支持如下参数，需要注释掉。</p>
<p>vi /home/mysql/data/backup-my.cnf</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">#innodb_log_checksum_algorithm</span><br><span class="line">#innodb_fast_checksum</span><br><span class="line">#innodb_log_block_size</span><br><span class="line">#innodb_doublewrite_file</span><br><span class="line">#rds_encrypt_data</span><br><span class="line">#innodb_encrypt_algorithm</span><br><span class="line">#redo_log_version</span><br><span class="line">#master_key_id</span><br><span class="line">#server_uuid</span><br></pre></td></tr></table></figure>

<p>启动</p>
<pre><code>mysqld_safe --defaults-file=/home/mysql/data/backup-my.cnf --user=root --datadir=/home/mysql/data &amp;</code></pre><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">2020-04-28T09:43:49.658819Z mysqld_safe Logging to &#39;&#x2F;home&#x2F;mysql&#x2F;data&#x2F;14864923d3cb.err&#39;.</span><br><span class="line">2020-04-28T09:43:49.703474Z mysqld_safe Starting mysqld daemon with databases from &#x2F;home&#x2F;mysql&#x2F;data</span><br></pre></td></tr></table></figure>

<p>如果mysql服务没起来，可以查看/home/mysql/data/14864923d3cb.err这个日志文件</p>
<p>实际使用遇到过的问题</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">2020-08-18T06:18:32.304152Z 0 [ERROR] [MY-010095] [Server] Failed to access directory for --secure-file-priv. Please make sure that directory exists and is accessible by MySQL Server. Supplied value : &#x2F;var&#x2F;lib&#x2F;mysql-files</span><br></pre></td></tr></table></figure>
<p><code>--secure-file-priv</code> 默认使用 <code>/var/lib/mysql-files</code>这个目录，我们更改一下</p>
<pre><code>vim /home/mysql/data/backup-my.cnf
secure-file-priv=/home/mysql/data</code></pre><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">2020-08-18T06:20:49.765666Z 1 [ERROR] [MY-011087] [Server] Different lower_case_table_names settings for server (&#39;0&#39;) and data dictionary (&#39;1&#39;).</span><br></pre></td></tr></table></figure>
<p>大小写配置问题</p>
<pre><code>lower_case_table_names=1</code></pre><p>再次启动后我们登录一下</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">root@a8a32828e279:&#x2F;home&#x2F;mysql&#x2F;data# mysql -upro -p            </span><br><span class="line">Enter password: </span><br><span class="line">Welcome to the MySQL monitor.  Commands end with ; or \g.</span><br><span class="line">Your MySQL connection id is 17</span><br><span class="line">Server version: 8.0.16 MySQL Community Server - GPL</span><br><span class="line"></span><br><span class="line">Copyright (c) 2000, 2019, Oracle and&#x2F;or its affiliates. All rights reserved.</span><br><span class="line"></span><br><span class="line">Oracle is a registered trademark of Oracle Corporation and&#x2F;or its</span><br><span class="line">affiliates. Other names may be trademarks of their respective</span><br><span class="line">owners.</span><br><span class="line"></span><br><span class="line">Type &#39;help;&#39; or &#39;\h&#39; for help. Type &#39;\c&#39; to clear the current input statement.</span><br><span class="line"></span><br><span class="line">mysql&gt; show databases</span><br><span class="line">    -&gt; ;</span><br><span class="line">+--------------------+</span><br><span class="line">| Database           |</span><br><span class="line">+--------------------+</span><br><span class="line">| pro                |</span><br><span class="line">| information_schema |</span><br><span class="line">| mysql              |</span><br><span class="line">+--------------------+</span><br><span class="line">3 rows in set (0.01 sec)</span><br><span class="line"></span><br><span class="line">mysql&gt;</span><br></pre></td></tr></table></figure>
<p>OK，大功告成</p>
<h3 id="参考资料"><a href="#参考资料" class="headerlink" title="参考资料"></a>参考资料</h3><p><a href="https://help.aliyun.com/knowledge_detail/41817.html" target="_blank" rel="noopener">https://help.aliyun.com/knowledge_detail/41817.html</a></p>
]]></content>
      <categories>
        <category>Linux</category>
      </categories>
      <tags>
        <tag>Docker</tag>
        <tag>Aliyun</tag>
        <tag>Mysql</tag>
      </tags>
  </entry>
  <entry>
    <title>漫谈Guava之Cache（一）</title>
    <url>/post/96f462f9.html</url>
    <content><![CDATA[<p>计算机的世界中，缓存无处不在，最常见的如CPU高速缓存，<code>CPU Cache</code>是用于减少<a href="https://zh.wikipedia.org/wiki/中央处理器" target="_blank" rel="noopener">处理器</a>访问内存所需平均时间的部件。在金字塔式<a href="https://zh.wikipedia.org/w/index.php?title=存储体系&action=edit&redlink=1" target="_blank" rel="noopener">存储体系</a>中它位于自顶向下的第二层，仅次于<a href="https://zh.wikipedia.org/wiki/寄存器" target="_blank" rel="noopener">CPU寄存器</a>。其容量远小于<a href="https://zh.wikipedia.org/wiki/内存" target="_blank" rel="noopener">内存</a>，但速度却可以接近处理器的频率。</p>
<p>当处理器发出内存访问请求时，会先查看缓存内是否有请求数据。如果存在（命中），则不经访问内存直接返回该数据；如果不存在（失效），则要先把内存中的相应数据载入缓存，再将其返回处理器。</p>
<p>这和我们日常开发中的很多场景相似，例如调用第三方接口取数据，微服务间取数据，甚至从数据库中取数据。这些IO操作所消耗的时间对于CPU来说是很久的。如果这些数据是读多写少，也就是说数据不会轻易改变，那我们可以把这部分数据进行缓存，取数据前先访问缓存，在缓存中查不到时再走原来的逻辑。</p>
<a id="more"></a>

<p>日常开发中我们可能会使用<code>ConcurrentHashMap</code>存数据，替代缓存使用，简单场景这样使用没什么问题，只是<code>ConcurrentHashMap</code>设计的目的不是做缓存，所以很多Cache应该有的很多功能，<code>ConcurrentHashMap</code>并不直接支持，类如设置key的过期时间，内存不足时释放缓存等。</p>
<p>本文介绍Guava的Cache使用，为该系列的第一部分。</p>
<p>我先写一个简单的例子，创建一个缓存，该缓存最多存100个元素，超时时间为10分钟</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">Cache&lt;String, String&gt; cache = CacheBuilder.newBuilder()</span><br><span class="line">            .maximumSize(<span class="number">100</span>)</span><br><span class="line">            .expireAfterWrite(<span class="number">10</span>, TimeUnit.MINUTES)</span><br><span class="line">            .build();</span><br><span class="line"></span><br><span class="line">cache.getIfPresent(key)</span><br><span class="line"></span><br><span class="line">cache.get(key, () -&gt; defaultValue)</span><br></pre></td></tr></table></figure>

<h3 id="CacheLoader"><a href="#CacheLoader" class="headerlink" title="CacheLoader"></a>CacheLoader</h3><p>上文中的<code>cache.get(key, () -&gt; defaultValue)</code>会在<code>cache</code>中没这条数据的情况下，根据我们写的策略重新计算，并将数据保存在缓存中一份。对于这种情况可以使用<code>CacheLoader</code>来统一处理。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">LoadingCache&lt;Key, Graph&gt; graphs = CacheBuilder.newBuilder()</span><br><span class="line">      .expireAfterAccess(<span class="number">10</span>, TimeUnit.MINUTES)</span><br><span class="line">      .maximumSize(<span class="number">1000</span>)</span><br><span class="line">      .build(</span><br><span class="line">          <span class="keyword">new</span> CacheLoader&lt;Key, Graph&gt;() &#123;</span><br><span class="line">            <span class="function"><span class="keyword">public</span> Graph <span class="title">load</span><span class="params">(Key key)</span> </span>&#123; <span class="comment">// no checked exception</span></span><br><span class="line">              <span class="keyword">return</span> createExpensiveGraph(key);</span><br><span class="line">            &#125;</span><br><span class="line">          &#125;);</span><br><span class="line">...</span><br><span class="line"><span class="keyword">return</span> graphs.getUnchecked(key);</span><br></pre></td></tr></table></figure>



<h2 id="三种淘汰策略"><a href="#三种淘汰策略" class="headerlink" title="三种淘汰策略"></a>三种淘汰策略</h2><blockquote>
<p>Guava provides three basic types of eviction: size-based eviction, time-based eviction, and reference-based eviction.</p>
</blockquote>
<h3 id="容量大小"><a href="#容量大小" class="headerlink" title="容量大小"></a>容量大小</h3><p>前文使用的<code>CacheBuilder.maximumSize(long)</code> 便可限制最大缓存数量。<br>如缓冲的每条数据有不同的权重的话可自己写<code>Weigher</code>策略，并设置最大<code>重量</code>。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">LoadingCache&lt;Key, Graph&gt; graphs = CacheBuilder.newBuilder()</span><br><span class="line">       .maximumWeight(<span class="number">100000</span>)</span><br><span class="line">       .weigher(<span class="keyword">new</span> Weigher&lt;Key, Graph&gt;() &#123;</span><br><span class="line">          <span class="function"><span class="keyword">public</span> <span class="keyword">int</span> <span class="title">weigh</span><span class="params">(Key k, Graph g)</span> </span>&#123;</span><br><span class="line">            <span class="keyword">return</span> g.vertices().size();</span><br><span class="line">          &#125;</span><br><span class="line">        &#125;)</span><br><span class="line">       .build(</span><br><span class="line">           <span class="keyword">new</span> CacheLoader&lt;Key, Graph&gt;() &#123;</span><br><span class="line">             <span class="function"><span class="keyword">public</span> Graph <span class="title">load</span><span class="params">(Key key)</span> </span>&#123; <span class="comment">// no checked exception</span></span><br><span class="line">               <span class="keyword">return</span> createExpensiveGraph(key);</span><br><span class="line">             &#125;</span><br><span class="line">           &#125;);</span><br></pre></td></tr></table></figure>

<h3 id="时间"><a href="#时间" class="headerlink" title="时间"></a>时间</h3><p><code>CacheBuilder</code> provides two approaches to timed eviction:</p>
<ul>
<li><code>expireAfterAccess(long, TimeUnit)</code> 读或写多长时间后，缓存失效</li>
<li><code>expireAfterWrite(long, TimeUnit)</code> 值更改后多长时间后，缓存失效</li>
</ul>
<h3 id="引用"><a href="#引用" class="headerlink" title="引用"></a>引用</h3><p>使用JVM的垃圾回收，可被动的缓存失效。</p>
<ul>
<li><code>CacheBuilder.weakKeys()</code> 对key使用弱引用包装</li>
<li><code>CacheBuilder.weakValues()</code> 对value使用弱引用包装</li>
<li><code>CacheBuilder.softValues()</code> 对soft使用软引用包装</li>
</ul>
<h2 id="Removal-Listeners"><a href="#Removal-Listeners" class="headerlink" title="Removal Listeners"></a>Removal Listeners</h2><p>如果我们想对淘汰的元素进行处理，我们可以写一个<code>RemovalListener</code>对Cache进行监听。例如我们将DB连接存入cache中，当缓存失效时，我们需要关闭数据库连接，这部分逻辑就可以通过<code>RemovalListener</code>来完成</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">CacheLoader&lt;Key, DatabaseConnection&gt; loader = <span class="keyword">new</span> CacheLoader&lt;Key, DatabaseConnection&gt; () &#123;</span><br><span class="line">  <span class="function"><span class="keyword">public</span> DatabaseConnection <span class="title">load</span><span class="params">(Key key)</span> <span class="keyword">throws</span> Exception </span>&#123;</span><br><span class="line">    <span class="keyword">return</span> openConnection(key);</span><br><span class="line">  &#125;</span><br><span class="line">&#125;;</span><br><span class="line">RemovalListener&lt;Key, DatabaseConnection&gt; removalListener = <span class="keyword">new</span> RemovalListener&lt;Key, DatabaseConnection&gt;() &#123;</span><br><span class="line">  <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">onRemoval</span><span class="params">(RemovalNotification&lt;Key, DatabaseConnection&gt; removal)</span> </span>&#123;</span><br><span class="line">    DatabaseConnection conn = removal.getValue();</span><br><span class="line">    conn.close(); <span class="comment">// tear down properly</span></span><br><span class="line">  &#125;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line">CacheBuilder.newBuilder()</span><br><span class="line">  .expireAfterWrite(<span class="number">2</span>, TimeUnit.MINUTES)</span><br><span class="line">  .removalListener(removalListener)</span><br><span class="line">  .build(loader);</span><br></pre></td></tr></table></figure>

<h2 id="Refresh"><a href="#Refresh" class="headerlink" title="Refresh"></a>Refresh</h2><p>对于失效性比较强的元素，我们可以配置刷新的策略。刷新和淘汰不同，刷新元素是异步操作，刷新未完成时返回还是原数据，刷新成功则返回新数据。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// Some keys don't need refreshing, and we want refreshes to be done asynchronously.</span></span><br><span class="line">LoadingCache&lt;Key, Graph&gt; graphs = CacheBuilder.newBuilder()</span><br><span class="line">       .maximumSize(<span class="number">1000</span>)</span><br><span class="line">       .refreshAfterWrite(<span class="number">1</span>, TimeUnit.MINUTES)</span><br><span class="line">       .build(</span><br><span class="line">           <span class="keyword">new</span> CacheLoader&lt;Key, Graph&gt;() &#123;</span><br><span class="line">             <span class="function"><span class="keyword">public</span> Graph <span class="title">load</span><span class="params">(Key key)</span> </span>&#123; <span class="comment">// no checked exception</span></span><br><span class="line">               <span class="keyword">return</span> getGraphFromDatabase(key);</span><br><span class="line">             &#125;</span><br><span class="line"></span><br><span class="line">             <span class="function"><span class="keyword">public</span> ListenableFuture&lt;Graph&gt; <span class="title">reload</span><span class="params">(<span class="keyword">final</span> Key key, Graph prevGraph)</span> </span>&#123;</span><br><span class="line">               <span class="keyword">if</span> (neverNeedsRefresh(key)) &#123;</span><br><span class="line">                 <span class="keyword">return</span> Futures.immediateFuture(prevGraph);</span><br><span class="line">               &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">                 <span class="comment">// asynchronous!</span></span><br><span class="line">                 ListenableFutureTask&lt;Graph&gt; task = ListenableFutureTask.create(<span class="keyword">new</span> Callable&lt;Graph&gt;() &#123;</span><br><span class="line">                   <span class="function"><span class="keyword">public</span> Graph <span class="title">call</span><span class="params">()</span> </span>&#123;</span><br><span class="line">                     <span class="keyword">return</span> getGraphFromDatabase(key);</span><br><span class="line">                   &#125;</span><br><span class="line">                 &#125;);</span><br><span class="line">                 executor.execute(task);</span><br><span class="line">                 <span class="keyword">return</span> task;</span><br><span class="line">               &#125;</span><br><span class="line">             &#125;</span><br><span class="line">           &#125;);</span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>Java</category>
      </categories>
      <tags>
        <tag>Guava</tag>
        <tag>Cache</tag>
        <tag>缓存</tag>
      </tags>
  </entry>
  <entry>
    <title>Spring Cloud Gateway 分布式限流</title>
    <url>/post/c7e397c2.html</url>
    <content><![CDATA[<p>前文我们介绍过通过Guava中的RateLimiter进行限流，但Guava中的工具只适用同一JVM中的操作，想要在分布式环境中进行接口限流我们还需要引入中间件，本文介绍基于Redis的接口限流。</p>
<a id="more"></a>



<h3 id="Spring-Cloud-Gateway"><a href="#Spring-Cloud-Gateway" class="headerlink" title="Spring Cloud Gateway"></a>Spring Cloud Gateway</h3><p><code>Spring Cloud Gateway</code>为Spring官方基于Spring 5.0、Spring Boot2.0和Project Reactor等技术开发的网关，限流作为网关应用中常用功能，<code>Gateway</code>也提供了一个基于Redis的分布式过滤器，简单配置，即可在网关层进行流量限制。</p>
<blockquote>
<p>注：本文的<code>Spring Cloud</code> 版本为<code>Greenwich.SR1</code></p>
</blockquote>
<p><code>Spring Cloud Gateway</code>的路由配置支持配置文件和Java代码配置，为了直观展示源码，本文使用Java代码编写路由。</p>
<ul>
<li>RedisRateLimiter 基于Redis的过滤器</li>
<li>setBurstCapacity() 设置令牌桶的容量</li>
<li>setReplenishRate() 设置每秒可取的令牌数量</li>
<li>setKeyResolver(pathKeyResolver) 配置过滤key的策略，本文使用路径作为过滤的key</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Component</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">PathKeyResolver</span> <span class="keyword">implements</span> <span class="title">KeyResolver</span> </span>&#123;</span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> Mono&lt;String&gt; <span class="title">resolve</span><span class="params">(ServerWebExchange exchange)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> Mono.just(exchange.getRequest().getPath().toString());</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="meta">@Configuration</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">RouteLocatorConfig</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Autowired</span></span><br><span class="line">    <span class="keyword">private</span> PathKeyResolver pathKeyResolver;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Bean</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> RouteLocator <span class="title">customerRouteLocator</span><span class="params">(RouteLocatorBuilder builder)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> builder.routes()</span><br><span class="line">                .route(<span class="string">"admin"</span>, r -&gt; r.path(<span class="string">"/admin/**"</span>)</span><br><span class="line">                        .filters(f -&gt; f.requestRateLimiter()</span><br><span class="line">                                .rateLimiter(RedisRateLimiter<span class="class">.<span class="keyword">class</span>, <span class="title">c</span> -&gt; <span class="title">c</span>.<span class="title">setBurstCapacity</span>(1).<span class="title">setReplenishRate</span>(1))</span></span><br><span class="line"><span class="class">                                .<span class="title">configure</span>(<span class="title">c</span> -&gt; <span class="title">c</span>.<span class="title">setKeyResolver</span>(<span class="title">pathKeyResolver</span>))</span></span><br><span class="line"><span class="class">                        )</span></span><br><span class="line">                        .uri("lb://ADMIN")</span><br><span class="line">                )</span><br><span class="line">                .build();</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>RouteLocator配置完成后限流器就可以正常使用了，每个路径符合<code>/admin/**</code>的请求都会经过<code>RequestRateLimiterGatewayFilterFactory</code>的<code>apply()</code>方法。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Override</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> GatewayFilter <span class="title">apply</span><span class="params">(Config config)</span> </span>&#123;</span><br><span class="line">   <span class="comment">// 取过滤key的策略，我们这里使用上文自定义的 PathKeyResolver</span></span><br><span class="line">   KeyResolver resolver = getOrDefault(config.keyResolver, defaultKeyResolver);</span><br><span class="line">   <span class="comment">// 限流器，我们这里使用 RedisRateLimiter</span></span><br><span class="line">   RateLimiter&lt;Object&gt; limiter = getOrDefault(config.rateLimiter,</span><br><span class="line">         defaultRateLimiter);</span><br><span class="line">   <span class="comment">// 解析的key为空时是否拦截 默认拦截</span></span><br><span class="line">   <span class="keyword">boolean</span> denyEmpty = getOrDefault(config.denyEmptyKey, <span class="keyword">this</span>.denyEmptyKey);</span><br><span class="line">   HttpStatusHolder emptyKeyStatus = HttpStatusHolder</span><br><span class="line">         .parse(getOrDefault(config.emptyKeyStatus, <span class="keyword">this</span>.emptyKeyStatusCode));</span><br><span class="line"></span><br><span class="line">   <span class="keyword">return</span> (exchange, chain) -&gt; &#123;</span><br><span class="line">      Route route = exchange</span><br><span class="line">            .getAttribute(ServerWebExchangeUtils.GATEWAY_ROUTE_ATTR);</span><br><span class="line">	 <span class="comment">// 根据我们写的过滤策略计算出key</span></span><br><span class="line">      <span class="keyword">return</span> resolver.resolve(exchange).defaultIfEmpty(EMPTY_KEY).flatMap(key -&gt; &#123;</span><br><span class="line">         <span class="keyword">if</span> (EMPTY_KEY.equals(key)) &#123;</span><br><span class="line">            <span class="keyword">if</span> (denyEmpty) &#123;</span><br><span class="line">               setResponseStatus(exchange, emptyKeyStatus);</span><br><span class="line">               <span class="keyword">return</span> exchange.getResponse().setComplete();</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">return</span> chain.filter(exchange);</span><br><span class="line">         &#125;</span><br><span class="line">         <span class="comment">// key 不为空时走限流逻辑，这里使用的RateLimiter的isAllowed()方法</span></span><br><span class="line">         <span class="keyword">return</span> limiter.isAllowed(route.getId(), key).flatMap(response -&gt; &#123;</span><br><span class="line"></span><br><span class="line">            <span class="keyword">for</span> (Map.Entry&lt;String, String&gt; header : response.getHeaders()</span><br><span class="line">                  .entrySet()) &#123;</span><br><span class="line">               exchange.getResponse().getHeaders().add(header.getKey(),</span><br><span class="line">                     header.getValue());</span><br><span class="line">            &#125;</span><br><span class="line"> </span><br><span class="line">            <span class="keyword">if</span> (response.isAllowed()) &#123;</span><br><span class="line">               <span class="keyword">return</span> chain.filter(exchange);</span><br><span class="line">            &#125;</span><br><span class="line"></span><br><span class="line">            setResponseStatus(exchange, config.getStatusCode());</span><br><span class="line">            <span class="keyword">return</span> exchange.getResponse().setComplete();</span><br><span class="line">         &#125;);</span><br><span class="line">      &#125;);</span><br><span class="line">   &#125;;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><code>resolver.resolve(exchange)</code>实际调用<code>RedisRateLimiter</code>限流器的<code>isAllowed</code>方法</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Override</span></span><br><span class="line"><span class="meta">@SuppressWarnings</span>(<span class="string">"unchecked"</span>)</span><br><span class="line"><span class="function"><span class="keyword">public</span> Mono&lt;Response&gt; <span class="title">isAllowed</span><span class="params">(String routeId, String id)</span> </span>&#123;</span><br><span class="line">   <span class="keyword">if</span> (!<span class="keyword">this</span>.initialized.get()) &#123;</span><br><span class="line">      <span class="keyword">throw</span> <span class="keyword">new</span> IllegalStateException(<span class="string">"RedisRateLimiter is not initialized"</span>);</span><br><span class="line">   &#125;</span><br><span class="line"></span><br><span class="line">   Config routeConfig = loadConfiguration(routeId);</span><br><span class="line"></span><br><span class="line">   <span class="comment">// How many requests per second do you want a user to be allowed to do?</span></span><br><span class="line">    <span class="comment">// 每秒可取多少令牌</span></span><br><span class="line">   <span class="keyword">int</span> replenishRate = routeConfig.getReplenishRate();</span><br><span class="line"></span><br><span class="line">   <span class="comment">// How much bursting do you want to allow?</span></span><br><span class="line">    <span class="comment">// 令牌桶的容量</span></span><br><span class="line">   <span class="keyword">int</span> burstCapacity = routeConfig.getBurstCapacity();</span><br><span class="line"></span><br><span class="line">   <span class="keyword">try</span> &#123;</span><br><span class="line">       <span class="comment">// 生成redis的key,request_rate_limiter.&#123;id&#125;.tokens和request_rate_limiter.&#123;id&#125;.timestamp</span></span><br><span class="line">      List&lt;String&gt; keys = getKeys(id);</span><br><span class="line"></span><br><span class="line">      <span class="comment">// The arguments to the LUA script. time() returns unixtime in seconds.</span></span><br><span class="line">      List&lt;String&gt; scriptArgs = Arrays.asList(replenishRate + <span class="string">""</span>,</span><br><span class="line">            burstCapacity + <span class="string">""</span>, Instant.now().getEpochSecond() + <span class="string">""</span>, <span class="string">"1"</span>);</span><br><span class="line">      <span class="comment">// allowed, tokens_left = redis.eval(SCRIPT, keys, args)</span></span><br><span class="line">       <span class="comment">// redis中支持lua脚本原子性执行,该脚本在Bean初始化时装配进来</span></span><br><span class="line">      Flux&lt;List&lt;Long&gt;&gt; flux = <span class="keyword">this</span>.redisTemplate.execute(<span class="keyword">this</span>.script, keys,</span><br><span class="line">            scriptArgs);</span><br><span class="line">      <span class="comment">// .log("redisratelimiter", Level.FINER);</span></span><br><span class="line">      <span class="keyword">return</span> flux.onErrorResume(throwable -&gt; Flux.just(Arrays.asList(<span class="number">1L</span>, -<span class="number">1L</span>)))</span><br><span class="line">            .reduce(<span class="keyword">new</span> ArrayList&lt;Long&gt;(), (longs, l) -&gt; &#123;</span><br><span class="line">               longs.addAll(l);</span><br><span class="line">               <span class="keyword">return</span> longs;</span><br><span class="line">            &#125;).map(results -&gt; &#123;</span><br><span class="line">          		<span class="comment">// 	根据lua脚本返回值判断是否允许通过</span></span><br><span class="line">               <span class="keyword">boolean</span> allowed = results.get(<span class="number">0</span>) == <span class="number">1L</span>;</span><br><span class="line">               Long tokensLeft = results.get(<span class="number">1</span>);</span><br><span class="line"></span><br><span class="line">               Response response = <span class="keyword">new</span> Response(allowed,</span><br><span class="line">                     getHeaders(routeConfig, tokensLeft));</span><br><span class="line"></span><br><span class="line">               <span class="keyword">if</span> (log.isDebugEnabled()) &#123;</span><br><span class="line">                  log.debug(<span class="string">"response: "</span> + response);</span><br><span class="line">               &#125;</span><br><span class="line">               <span class="keyword">return</span> response;</span><br><span class="line">            &#125;);</span><br><span class="line">   &#125;</span><br><span class="line">   <span class="keyword">catch</span> (Exception e) &#123;</span><br><span class="line">      <span class="comment">/*</span></span><br><span class="line"><span class="comment">       * We don't want a hard dependency on Redis to allow traffic. Make sure to set</span></span><br><span class="line"><span class="comment">       * an alert so you know if this is happening too much. Stripe's observed</span></span><br><span class="line"><span class="comment">       * failure rate is 0.01%.</span></span><br><span class="line"><span class="comment">       */</span></span><br><span class="line">      log.error(<span class="string">"Error determining if user allowed from redis"</span>, e);</span><br><span class="line">   &#125;</span><br><span class="line">   <span class="keyword">return</span> Mono.just(<span class="keyword">new</span> Response(<span class="keyword">true</span>, getHeaders(routeConfig, -<span class="number">1L</span>)));</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><code>this.script</code>为Bean初始化时装配进来，<code>GatewayRedisAutoConfiguration#redisRequestRateLimiterScript</code>将lua脚本从文件中读取并注册成Bean</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Bean</span></span><br><span class="line"><span class="meta">@SuppressWarnings</span>(<span class="string">"unchecked"</span>)</span><br><span class="line"><span class="function"><span class="keyword">public</span> RedisScript <span class="title">redisRequestRateLimiterScript</span><span class="params">()</span> </span>&#123;</span><br><span class="line">   DefaultRedisScript redisScript = <span class="keyword">new</span> DefaultRedisScript&lt;&gt;();</span><br><span class="line">   redisScript.setScriptSource(<span class="keyword">new</span> ResourceScriptSource(</span><br><span class="line">         <span class="keyword">new</span> ClassPathResource(<span class="string">"META-INF/scripts/request_rate_limiter.lua"</span>)));</span><br><span class="line">   redisScript.setResultType(List<span class="class">.<span class="keyword">class</span>)</span>;</span><br><span class="line">   <span class="keyword">return</span> redisScript;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><code>request_rate_limiter.lua</code>中的代码，与前文所讲的Guava中的RateLimiter实现大同小异</p>
<figure class="highlight lua"><table><tr><td class="code"><pre><span class="line"><span class="keyword">local</span> tokens_key = KEYS[<span class="number">1</span>]</span><br><span class="line"><span class="keyword">local</span> timestamp_key = KEYS[<span class="number">2</span>]</span><br><span class="line"><span class="comment">--redis.log(redis.LOG_WARNING, "tokens_key " .. tokens_key)</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">local</span> rate = <span class="built_in">tonumber</span>(ARGV[<span class="number">1</span>])</span><br><span class="line"><span class="keyword">local</span> capacity = <span class="built_in">tonumber</span>(ARGV[<span class="number">2</span>])</span><br><span class="line"><span class="keyword">local</span> now = <span class="built_in">tonumber</span>(ARGV[<span class="number">3</span>])</span><br><span class="line"><span class="keyword">local</span> requested = <span class="built_in">tonumber</span>(ARGV[<span class="number">4</span>])</span><br><span class="line"></span><br><span class="line"><span class="keyword">local</span> fill_time = capacity/rate</span><br><span class="line"><span class="comment">-- 计算超时时间</span></span><br><span class="line"><span class="keyword">local</span> ttl = <span class="built_in">math</span>.<span class="built_in">floor</span>(fill_time*<span class="number">2</span>)</span><br><span class="line"></span><br><span class="line"><span class="comment">--redis.log(redis.LOG_WARNING, "rate " .. ARGV[1])</span></span><br><span class="line"><span class="comment">--redis.log(redis.LOG_WARNING, "capacity " .. ARGV[2])</span></span><br><span class="line"><span class="comment">--redis.log(redis.LOG_WARNING, "now " .. ARGV[3])</span></span><br><span class="line"><span class="comment">--redis.log(redis.LOG_WARNING, "requested " .. ARGV[4])</span></span><br><span class="line"><span class="comment">--redis.log(redis.LOG_WARNING, "filltime " .. fill_time)</span></span><br><span class="line"><span class="comment">--redis.log(redis.LOG_WARNING, "ttl " .. ttl)</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">local</span> last_tokens = <span class="built_in">tonumber</span>(redis.call(<span class="string">"get"</span>, tokens_key))</span><br><span class="line"><span class="keyword">if</span> last_tokens == <span class="literal">nil</span> <span class="keyword">then</span></span><br><span class="line">  last_tokens = capacity</span><br><span class="line"><span class="keyword">end</span></span><br><span class="line"><span class="comment">--redis.log(redis.LOG_WARNING, "last_tokens " .. last_tokens)</span></span><br><span class="line"><span class="comment">-- 最后刷新时间</span></span><br><span class="line"><span class="keyword">local</span> last_refreshed = <span class="built_in">tonumber</span>(redis.call(<span class="string">"get"</span>, timestamp_key))</span><br><span class="line"><span class="keyword">if</span> last_refreshed == <span class="literal">nil</span> <span class="keyword">then</span></span><br><span class="line">  last_refreshed = <span class="number">0</span></span><br><span class="line"><span class="keyword">end</span></span><br><span class="line"><span class="comment">--redis.log(redis.LOG_WARNING, "last_refreshed " .. last_refreshed)</span></span><br><span class="line"><span class="comment">-- 和上次请求的时间差</span></span><br><span class="line"><span class="keyword">local</span> delta = <span class="built_in">math</span>.<span class="built_in">max</span>(<span class="number">0</span>, now-last_refreshed)</span><br><span class="line"><span class="comment">-- 计算令牌桶中token数量</span></span><br><span class="line"><span class="keyword">local</span> filled_tokens = <span class="built_in">math</span>.<span class="built_in">min</span>(capacity, last_tokens+(delta*rate))</span><br><span class="line"><span class="comment">-- 桶中令牌数量是否足够</span></span><br><span class="line"><span class="keyword">local</span> allowed = filled_tokens &gt;= requested</span><br><span class="line"><span class="keyword">local</span> new_tokens = filled_tokens</span><br><span class="line"><span class="keyword">local</span> allowed_num = <span class="number">0</span></span><br><span class="line"><span class="keyword">if</span> allowed <span class="keyword">then</span></span><br><span class="line">  <span class="comment">-- 桶中令牌被取走</span></span><br><span class="line">  new_tokens = filled_tokens - requested</span><br><span class="line">  allowed_num = <span class="number">1</span></span><br><span class="line"><span class="keyword">end</span></span><br><span class="line"></span><br><span class="line"><span class="comment">--redis.log(redis.LOG_WARNING, "delta " .. delta)</span></span><br><span class="line"><span class="comment">--redis.log(redis.LOG_WARNING, "filled_tokens " .. filled_tokens)</span></span><br><span class="line"><span class="comment">--redis.log(redis.LOG_WARNING, "allowed_num " .. allowed_num)</span></span><br><span class="line"><span class="comment">--redis.log(redis.LOG_WARNING, "new_tokens " .. new_tokens)</span></span><br><span class="line"><span class="comment">-- 剩余令牌数量</span></span><br><span class="line">redis.call(<span class="string">"setex"</span>, tokens_key, ttl, new_tokens)</span><br><span class="line"><span class="comment">-- 刷新时间设为当前</span></span><br><span class="line">redis.call(<span class="string">"setex"</span>, timestamp_key, ttl, now)</span><br><span class="line"></span><br><span class="line"><span class="keyword">return</span> &#123; allowed_num, new_tokens &#125;</span><br></pre></td></tr></table></figure>

<h3 id="Redisson"><a href="#Redisson" class="headerlink" title="Redisson"></a>Redisson</h3><p>Redisson是一个在Redis的基础上实现的Java驻内存数据网格（In-Memory Data Grid）。它不仅提供了一系列的分布式的Java常用对象，还提供了许多分布式服务。本文使用Redisson提供的RRateLimiter实现分布式限流。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">RRateLimiterDemo</span> </span>&#123;</span><br><span class="line">	</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> RRateLimiter rateLimiter = RedisClient.INSTANCE.getRateLimiter(<span class="string">"rate_limiter"</span>);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">static</span> &#123;</span><br><span class="line">        rateLimiter.trySetRate(RateType.PER_CLIENT, <span class="number">2</span>, <span class="number">1</span>, RateIntervalUnit.SECONDS);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> <span class="keyword">throws</span> InterruptedException </span>&#123;</span><br><span class="line"></span><br><span class="line">        ExecutorService executorService = Executors.newFixedThreadPool(<span class="number">100</span>);</span><br><span class="line">        <span class="comment">// 5个线程同时调用，模拟多个请求同时访问</span></span><br><span class="line">        IntStream.rangeClosed(<span class="number">1</span>, <span class="number">5</span>).forEach(e -&gt; &#123;</span><br><span class="line">            executorService.submit(() -&gt; acquire(e));</span><br><span class="line">        &#125;);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">acquire</span><span class="params">(<span class="keyword">int</span> i)</span> </span>&#123;</span><br><span class="line">        <span class="comment">// acquire方法默认取一个令牌，桶中令牌不够的话会阻塞线程</span></span><br><span class="line">        rateLimiter.acquire();</span><br><span class="line">        System.out.printf(<span class="string">"当前系统时间:%s\n"</span>, LocalDateTime.now());</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>静态代码块中为设置限流器的速率，每秒生成两个令牌。下面是控制台打印的结果，可以看出来时间间隔和预想的一致。</p>
<p>该限流器也是通过lua脚本实现限流，实现逻辑与之前介绍的大同小异，我就不做过多展示了。</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">当前系统时间:2020-04-08T16:49:40.425</span><br><span class="line">当前系统时间:2020-04-08T16:49:40.425</span><br><span class="line">当前系统时间:2020-04-08T16:49:41.420</span><br><span class="line">当前系统时间:2020-04-08T16:49:41.421</span><br><span class="line">当前系统时间:2020-04-08T16:49:42.422</span><br></pre></td></tr></table></figure>

<h3 id="结语"><a href="#结语" class="headerlink" title="结语"></a>结语</h3><p>Redis限流器不仅仅今天介绍的这两种，分布式限流器也不限于Redis，但通过这两篇介绍限流器的文章可以发现他们的原理都是相通的，我们了解其中的思想即可。</p>
]]></content>
      <categories>
        <category>Java</category>
      </categories>
      <tags>
        <tag>Spring Cloud</tag>
        <tag>Gateway</tag>
        <tag>limiter</tag>
      </tags>
  </entry>
  <entry>
    <title>漫谈限流器</title>
    <url>/post/38378326.html</url>
    <content><![CDATA[<p>​    日常开发中经常会出现一瞬间接口被超频次访问的这种情况，例如电商秒杀活动，定点抢红包等。短时间高并发可能会拖慢系统的响应速度，引起网络超时，一个服务不可用进而影响其他服务也不可用，甚至导致雪崩。</p>
<p>​    限流是最直接有效的控制手段，将系统处理不过来的请求拦截在核心逻辑之外。限流器实现方法有<code>漏桶算法(Leaky Bucket),</code>令牌桶算法(Token Bucket)`等。</p>
<a id="more"></a>

<ul>
<li>漏斗算法 理解起来比较简单，水(即请求)流入固定大小的漏斗内，漏斗也已一定的速度流出水，当水满时停止流入,进行限流。</li>
<li>令牌桶算法 同样是固定大小的桶，定期往桶中投令牌，有新请求时需从桶中取令牌，当桶中无令牌可取时，则拒绝。</li>
</ul>
<p>本文使用<code>Guava</code>中的<code>RateLimiter</code>进行限流，我使用的Guava的版本为<code>28.2-jre</code>，不同版本api可能略有偏差。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">GuavaLimiterDemo</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 每秒生成2个令牌</span></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> RateLimiter rateLimiter = RateLimiter.create(<span class="number">2</span>);</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span><span class="keyword">throws</span> InterruptedException </span>&#123;</span><br><span class="line">        ExecutorService executorService = Executors.newFixedThreadPool(<span class="number">100</span>);</span><br><span class="line">        <span class="comment">// 5个线程同时调用，模拟多个请求同时访问</span></span><br><span class="line">        IntStream.rangeClosed(<span class="number">1</span>, <span class="number">5</span>).forEach(e -&gt; &#123;</span><br><span class="line">            executorService.submit(() -&gt; acquire(e));</span><br><span class="line">        &#125;);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">double</span> <span class="title">acquire</span><span class="params">(<span class="keyword">int</span> i)</span></span>&#123;</span><br><span class="line">        <span class="comment">// acquire方法默认取一个令牌，桶中令牌不够的话会阻塞线程,返回值为线程阻塞时间</span></span><br><span class="line">        <span class="keyword">double</span> acquire = rateLimiter.acquire();</span><br><span class="line">        System.out.printf(<span class="string">"当前系统时间:%s,等待秒数:%s\n"</span>,LocalDateTime.now(),acquire);</span><br><span class="line">        <span class="keyword">return</span> acquire;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>​    调用静态方法<code>create()</code>创建限流器,参数为每秒生产令牌的数量</p>
<p>​    <code>acquire()</code>方法默认取一个令牌，可用调用重载的方法取多个令牌，如果桶中无令牌该方法会阻塞，开发过程中要注意大量线程挂起的问题，慎用。</p>
<p>​    <code>tryAcquire()</code>方法可解决上述问题，取一个令牌，如桶中无令牌直接返回false。<br><code>tryAcquire(int permits, long timeout, TimeUnit unit)</code>可传入超时时间，超过这段时间桶中还是无令牌可取的话返回false。</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">当前系统时间:2020-03-25T13:30:37.757,等待秒数:0.0</span><br><span class="line">当前系统时间:2020-03-25T13:30:38.195,等待秒数:0.458741</span><br><span class="line">当前系统时间:2020-03-25T13:30:38.694,等待秒数:0.958729</span><br><span class="line">当前系统时间:2020-03-25T13:30:39.194,等待秒数:1.458717</span><br><span class="line">当前系统时间:2020-03-25T13:30:39.695,等待秒数:1.958704</span><br></pre></td></tr></table></figure>

<p>​    这是控制台打印的结果，每秒放入两个令牌的桶，同时有5个线程访问。基本每0.5秒放行一个请求。</p>
<h3 id="Guava-RateLimiter-实现原理"><a href="#Guava-RateLimiter-实现原理" class="headerlink" title="Guava RateLimiter 实现原理"></a>Guava RateLimiter 实现原理</h3><p><img src="https://img.yjll.art/img/20200327141557.png" alt=""></p>
<p>​    RateLimiter限流器实现了两种算法<code>SmoothWarmingUp(漏斗算法)</code>和<code>SmoothBursty(令牌桶算法)</code>，默认使用<code>SmoothBursty</code>。</p>
<p>​    静态方法<code>RateLimiter.create()</code>默认使用子类<code>SmoothBursty</code>创建限流器</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// permitsPerSecond 创建时传入的单位时间内发放令牌数量</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">static</span> RateLimiter <span class="title">create</span><span class="params">(<span class="keyword">double</span> permitsPerSecond)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">return</span> create(permitsPerSecond, SleepingStopwatch.createFromSystemTimer());</span><br><span class="line">  &#125;</span><br><span class="line"><span class="comment">// stopwatch guava实现的时间监视器 默认SleepingStopwatch.createFromSystemTimer()</span></span><br><span class="line"><span class="function"><span class="keyword">static</span> RateLimiter <span class="title">create</span><span class="params">(<span class="keyword">double</span> permitsPerSecond, SleepingStopwatch stopwatch)</span> </span>&#123;</span><br><span class="line">    <span class="comment">// maxBurstSeconds 最大缓存时间，默认1.0</span></span><br><span class="line">    RateLimiter rateLimiter = <span class="keyword">new</span> SmoothBursty(stopwatch, <span class="number">1.0</span> <span class="comment">/* maxBurstSeconds */</span>);</span><br><span class="line">    <span class="comment">// 初始化时设置投放令牌的速率</span></span><br><span class="line">    rateLimiter.setRate(permitsPerSecond);</span><br><span class="line">    <span class="keyword">return</span> rateLimiter;</span><br><span class="line">  &#125;</span><br></pre></td></tr></table></figure>

<p>​    <code>setRate</code>底层调用<code>doSetRate</code>设置速率</p>
<p>​    <code>setRate</code>为设置投放令牌的速率，调用时对以下值进行更改：</p>
<ul>
<li><p>storedPermits 当前令牌数量，速率更改时会根据新旧速率的比例对原桶中令牌的数量进行更改。初始化时为0</p>
</li>
<li><p>stableIntervalMicros 每生产一个令牌需要的毫秒数</p>
</li>
<li><p>maxPermits 桶中最多可存储的令牌数量，可根据maxBurstSeconds * permitsPerSecond 计算</p>
</li>
<li><p>nextFreeTicketMicros 下一次可取令牌的时间，当前时间早于该时间表示可取元素，与之的差表示放入令牌且不取出令牌的时间，进而再跟据stableIntervalMicros可算出该时间段内桶中增加了多少令牌(受限于桶的大小)</p>
</li>
</ul>
<p>​    在初始化时或手动更改速率时调用。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// permitsPerSecond 创建时传入的单位时间内发放令牌数量</span></span><br><span class="line"><span class="comment">// nowMicros 从创建到现在经过的时间</span></span><br><span class="line"><span class="function"><span class="keyword">final</span> <span class="keyword">void</span> <span class="title">doSetRate</span><span class="params">(<span class="keyword">double</span> permitsPerSecond, <span class="keyword">long</span> nowMicros)</span> </span>&#123;</span><br><span class="line">    resync(nowMicros);</span><br><span class="line">    <span class="keyword">double</span> stableIntervalMicros = SECONDS.toMicros(<span class="number">1L</span>) / permitsPerSecond;</span><br><span class="line">    <span class="keyword">this</span>.stableIntervalMicros = stableIntervalMicros;</span><br><span class="line">    doSetRate(permitsPerSecond, stableIntervalMicros);</span><br><span class="line">  &#125;</span><br><span class="line"><span class="comment">// stableIntervalMicros 每生产一个令牌，需要经过多少毫秒</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">doSetRate</span><span class="params">(<span class="keyword">double</span> permitsPerSecond, <span class="keyword">double</span> stableIntervalMicros)</span> </span>&#123;</span><br><span class="line">      <span class="keyword">double</span> oldMaxPermits = <span class="keyword">this</span>.maxPermits;</span><br><span class="line">      maxPermits = maxBurstSeconds * permitsPerSecond;</span><br><span class="line">      <span class="keyword">if</span> (oldMaxPermits == Double.POSITIVE_INFINITY) &#123;</span><br><span class="line">        <span class="comment">// if we don't special-case this, we would get storedPermits == NaN, below</span></span><br><span class="line">        storedPermits = maxPermits;</span><br><span class="line">      &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">        storedPermits =</span><br><span class="line">            (oldMaxPermits == <span class="number">0.0</span>)</span><br><span class="line">                ? <span class="number">0.0</span> <span class="comment">// initial state</span></span><br><span class="line">                : storedPermits * maxPermits / oldMaxPermits;</span><br><span class="line">      &#125;</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure>

<p>​    速率更改或者从桶中取令牌时会调用<code>resync</code>方法</p>
<p>​    <code>resync(nowMicros)</code>将当前时间与<code>nextFreeTicketMicros</code>作比较，如当前时间在<code>nextFreeTicketMicros</code>之后，则重新计算桶中令牌的数量，并重置<code>nextFreeTicketMicros</code>的值。从这里可以看出Guava Limiter设计的绝妙之处，不需要单独开线程向桶中添加令牌，而是惰性的在取令牌时计算一下这时间内桶中添加了几个令牌，在根据桶中令牌数量进行限流。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">resync</span><span class="params">(<span class="keyword">long</span> nowMicros)</span> </span>&#123;</span><br><span class="line">  <span class="comment">// if nextFreeTicket is in the past, resync to now</span></span><br><span class="line">  <span class="keyword">if</span> (nowMicros &gt; nextFreeTicketMicros) &#123;</span><br><span class="line">    <span class="keyword">double</span> newPermits = (nowMicros - nextFreeTicketMicros) / coolDownIntervalMicros();</span><br><span class="line">    <span class="comment">// 防止添加后的令牌数量超过桶的极限</span></span><br><span class="line">    storedPermits = min(maxPermits, storedPermits + newPermits);</span><br><span class="line">    nextFreeTicketMicros = nowMicros;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>​    取数据调用<code>acquire()</code>方法，桶中无令牌的话会阻塞</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">double</span> <span class="title">acquire</span><span class="params">(<span class="keyword">int</span> permits)</span> </span>&#123;</span><br><span class="line">  <span class="comment">// 需等待的时间</span></span><br><span class="line">  <span class="keyword">long</span> microsToWait = reserve(permits);</span><br><span class="line">  <span class="comment">// 线程挂起</span></span><br><span class="line">  stopwatch.sleepMicrosUninterruptibly(microsToWait);</span><br><span class="line">  <span class="keyword">return</span> <span class="number">1.0</span> * microsToWait / SECONDS.toMicros(<span class="number">1L</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>​    <code>acquire()</code>最终会调用<code>reserveEarliestAvailable()</code></p>
<p>​    令牌桶限流器的一个优势是可以减缓突发情况带来的问题，允许单次请求的令牌数量超出剩余令牌数量，但是多拿的令牌数要从后边的补，正所谓“前人挖坑，后人填坑”。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">final</span> <span class="keyword">long</span> <span class="title">reserveEarliestAvailable</span><span class="params">(<span class="keyword">int</span> requiredPermits, <span class="keyword">long</span> nowMicros)</span> </span>&#123;</span><br><span class="line">    <span class="comment">// 前文提到过，在取数据之前计算桶中令牌数量</span></span><br><span class="line">    resync(nowMicros);</span><br><span class="line">    <span class="keyword">long</span> returnValue = nextFreeTicketMicros;</span><br><span class="line">    <span class="keyword">double</span> storedPermitsToSpend = min(requiredPermits, <span class="keyword">this</span>.storedPermits);</span><br><span class="line">    <span class="comment">// 计算突发情况多取的令牌数量</span></span><br><span class="line">    <span class="keyword">double</span> freshPermits = requiredPermits - storedPermitsToSpend;</span><br><span class="line">    <span class="keyword">long</span> waitMicros =</span><br><span class="line">        storedPermitsToWaitTime(<span class="keyword">this</span>.storedPermits, storedPermitsToSpend)</span><br><span class="line">            + (<span class="keyword">long</span>) (freshPermits * stableIntervalMicros);</span><br><span class="line">	<span class="comment">// 超发的情况，下一次可取令牌的时间往后延</span></span><br><span class="line">    <span class="keyword">this</span>.nextFreeTicketMicros = LongMath.saturatedAdd(nextFreeTicketMicros, waitMicros);</span><br><span class="line">    <span class="keyword">this</span>.storedPermits -= storedPermitsToSpend;</span><br><span class="line">    <span class="keyword">return</span> returnValue;</span><br><span class="line">  &#125;</span><br></pre></td></tr></table></figure>



]]></content>
      <categories>
        <category>Java</category>
      </categories>
      <tags>
        <tag>Guava</tag>
        <tag>limiter</tag>
      </tags>
  </entry>
</search>
